---
milestone: v1.0
audited: 2026-05-15T14:30:00Z
status: tech_debt
scores:
  requirements: 28/28
  phases: 7/7
  integration: 7/7
  flows: intact
gaps: {}
tech_debt:
  - phase: cross-phase
    items:
      - "WR-01: the `_DIV_*` divergence id-sets in tests/test_triton_scan_strict.py (and the monarch fp32 + butterfly bwd strict grids) cover the FULL parametrize cross-product, so test_scan_fwd/bwd_strict_matches_reference, the monarch fp32 fwd/bwd grids, and the butterfly bwd grid are 100% deselected under `pytest -q -m \"not divergence\"`. A future kernel regression that stays within ~5e-4 abs would not be caught by the standard green gate. Documented in 07-REVIEW.md, 07-VERIFICATION.md, AUDIT-REPORT.md section (c); on the v2 radar as KRN-02. Deliberate Wave-2 decision — the <5e-4 tight-TF32 bound sits at the autotune-config-dependent TF32 floor."
  - phase: 00-project (PROJECT.md)
    items:
      - "5 stale `— Pending` rows in the PROJECT.md Key Decisions table (baseline selection, ground-truth choice, fwd+bwd scope, tiered tolerance, fix-in-milestone) were never closed to a final outcome at milestone end. The decisions themselves are reflected in REQUIREMENTS.md / AUDIT-REPORT.md / the test suite; only the documentation table is stale. Phase 2 VERIFICATION.md flagged this; Phase 7 did not revisit it."
  - phase: all-phases (Nyquist)
    items:
      - "No `*-VALIDATION.md` exists for any of the 7 phases — Nyquist validation was never run this milestone. For a test-audit milestone the parity test suite is itself the coverage instrument, but the GSD Nyquist artifact is absent. Run `/gsd-validate-phase <N>` retroactively if formal Nyquist coverage records are wanted."
deferred_to_v2:
  - "KRN-01 (gru-triton-e0l): Monarch bwd kernel-tiling redesign for consumer-GPU SMEM / tl.dot K<16 limit"
  - "KRN-02 (TF32 family): input_precision='ieee' kernel rewrite to eliminate the TF32 tl.dot/tl.sum reduction-order divergence"
  - "ACT-01: per-channel min_max observer implemented correctly (vs fenced)"
  - "ACT-02: LSQ / PACT learnable activation scales"
  - "PERF-01 / PERF-02: cuDNN comparison + QAT-overhead bench re-validation"
nyquist:
  compliant_phases: 0
  partial_phases: 0
  missing_phases: 7
  overall: MISSING
---

# v1.0 Milestone Audit — Native-PyTorch Parity Audit

**Audited:** 2026-05-15
**Status:** tech_debt — all 28 requirements satisfied, 0 blockers; accumulated non-blocking debt surfaced for review before archive.

## Summary

The v1.0 milestone is a brownfield correctness audit of the `gru-triton` GRU/QAT
library. All 7 phases are complete and verified; all 28 v1 requirements are
satisfied and checked off in `REQUIREMENTS.md`; cross-phase integration is fully
wired with no broken E2E flows; all 14 surfaced bd findings are closed; and the
milestone-closing `AUDIT-REPORT.md` exists at the repo root. No blockers prevent
milestone completion. Three documented, non-blocking tech-debt items are recorded
below for an explicit accept-or-address decision.

## Requirements Coverage — 28/28 satisfied

3-source cross-reference (VERIFICATION.md status + SUMMARY frontmatter +
REQUIREMENTS.md traceability) confirms every requirement satisfied:

| Group | IDs | Phase | Status |
|-------|-----|-------|--------|
| REF | REF-01..05 | 1 | Satisfied (VERIFICATION: passed) |
| TRI | TRI-01..06 | 2 | Satisfied (VERIFICATION: human_needed — Option C tiered-tolerance disposition, an accepted user decision logged in PROJECT.md) |
| STR | STR-01..03 | 3 | Satisfied (VERIFICATION: passed) |
| QNT | QNT-01..04 | 4 | Satisfied (VERIFICATION: passed_with_major_caveats — per-cluster h_scale_mult dispositions in 04-DISPOSITION.md, an accepted user decision) |
| CAL | CAL-01..03 | 5 | Satisfied (VERIFICATION: passed) |
| EDG | EDG-01..04 | 6 | Satisfied (VERIFICATION: passed) |
| RPT | RPT-01..03 | 7 | Satisfied (VERIFICATION: passed) |

No unsatisfied requirements. No orphaned requirements (every REQ-ID appears in a
phase VERIFICATION.md). Note: Phase 6 `06-01-SUMMARY.md` and Phase 7
`07-01-SUMMARY.md` have empty `requirements-completed` frontmatter, but the
corresponding requirements (EDG-01..04, RPT span) are verified `passed` in the
phase VERIFICATION.md files and checked off in REQUIREMENTS.md — a frontmatter
population gap, not a coverage gap.

## Phase Verification — 7/7

| Phase | Status | Note |
|-------|--------|------|
| 01 reference-path-parity | passed | — |
| 02 triton-fast-path-parity | human_needed | Option C tiered-tolerance disposition — accepted decision (PROJECT.md Key Decisions, line 123) |
| 03 structured-pytorch-fallback | passed | — |
| 04 quant-on-bit-identity | passed_with_major_caveats | Per-cluster h_scale_mult dispositions — accepted decision (04-DISPOSITION.md) |
| 05 calibration-freeze-lifecycle | passed | — |
| 06 edge-case-sweeps | passed | — |
| 07 audit-report-findings-handling | passed | Human items re-confirmed by orchestrator (green gates + lint) |

The two non-`passed` statuses are documented, intentional dispositions accepted
by the user before this audit — not unverified work.

## Cross-Phase Integration — WIRED, 0 blockers

All 28 requirement-to-test links verified end-to-end. The audit E2E chain —
reference path (P1) → Triton parity (P2) → structured fallback (P3) → quant-on
(P4) → calibration lifecycle (P5) → edge cases (P6) → audit report (P7) — has no
broken link. Phase 1 translation helpers are imported by Phase 6; Phase 2 strict
files are extended by Phase 4 and consumed by Phase 5; the Phase 4
`04-DISPOSITION.md` tolerance contract is inherited verbatim by Phase 5 CAL-03;
all 9 ACCEPTED-DIVERGENCE bd issues and the Phase 7 `divergence` marker correctly
aggregate the Phase 2/4 findings into AUDIT-REPORT.md.

## Tech Debt (non-blocking — review before archive)

1. **WR-01 — whole-grid divergence marking.** The `divergence` marker
   deselects the dense/monarch fp32 strict fwd/bwd grids and the butterfly bwd
   grid entirely from `pytest -q -m "not divergence"`. Future regressions within
   ~5e-4 abs would pass the green gate silently. Deliberate, documented Wave-2
   decision (autotune-config-dependent TF32 floor); on the v2 radar as KRN-02.

2. **PROJECT.md Key Decisions — 5 stale `— Pending` rows.** Pre-work strategic
   decisions never closed to a final outcome. Documentation hygiene only — the
   decisions are reflected everywhere else.

3. **Nyquist — 0/7 phases have VALIDATION.md.** Nyquist validation was never run.
   The parity test suite is the de-facto coverage instrument for a test-audit
   milestone, but the formal GSD artifact is absent.

## v2 Deferrals (intentional scope-out, recorded in REQUIREMENTS.md v2)

KRN-01 (e0l kernel-tiling), KRN-02 (TF32 input_precision='ieee' rewrite),
ACT-01 (per-channel min_max observer), ACT-02 (LSQ/PACT), PERF-01/02 (bench
re-validation). These are deliberate v2 scope, not milestone gaps.

## Verdict

**tech_debt** — the milestone is substantively complete and may be archived.
No blockers. Three non-blocking debt items are surfaced above for an explicit
accept-or-address decision; all three are already documented in their
originating artifacts and (for WR-01) on the v2 roadmap.
