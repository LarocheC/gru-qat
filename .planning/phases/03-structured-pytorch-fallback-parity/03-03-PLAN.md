---
phase: 03-structured-pytorch-fallback-parity
plan: 03
type: execute
wave: 3
depends_on:
  - 03-01
  - 03-02
files_modified:
  - tests/test_structure_parity.py
  - .planning/codebase/TESTING.md
  - .planning/phases/03-structured-pytorch-fallback-parity/03-SUMMARY.md
autonomous: true
requirements:
  - STR-03
must_haves:
  truths:
    - "When `_import_torch_structured` is monkeypatched to raise ImportError, `make_structured_linear(cfg, ...)` for kind ∈ {'monarch', 'butterfly'} raises ImportError with a message containing 'torch-structured'."
    - "When the LDR import path (`from torch_structured.structured.layers import LDRSubdiagonal` in `src/gru_qat/structure.py:164`) is made to fail, `make_structured_linear(cfg, ...)` for kind='ldr' raises ImportError with 'torch-structured' in the message."
    - "When `_import_torch_structured` is monkeypatched to raise ImportError, `make_structured_linear(cfg, ...)` for kind ∈ {'dense', 'diagonal', 'circulant'} returns a working layer that produces finite output (local impls do NOT depend on torch-structured)."
    - "All Phase 3 tests pass on a single `pytest tests/test_structure_parity.py -q` run (fast tier) and `pytest tests/test_structure_parity.py -m slow -q` (slow tier)."
    - "TESTING.md updated: 'Mocking' section no longer claims 'No monkeypatching' — it documents the Phase 3 exception (or lifts the restriction with a 'use sparingly' clause)."
    - "Phase-exit `03-SUMMARY.md` exists with REQ closure (STR-01/02/03), bd issue tally, locked-files integrity, max abs diff datums, hand-off to Phase 4."
    - "tests/test_parity.py and tests/test_layer_parity.py git diff empty across all Phase 3 commits (D-28 / D-38)."
    - "tests/test_structure.py git diff empty across all Phase 3 commits (D-38)."
  artifacts:
    - path: "tests/test_structure_parity.py"
      provides: "STR-03 section: _raise_missing_torch_structured helper, test_missing_torch_structured_raises_clear_error (parametrized monarch/butterfly), test_missing_ldr_raises_clear_error (LDR-specific, sys.modules trick), test_local_impls_work_without_torch_structured (parametrized circulant/diagonal/dense)."
      contains: "def _raise_missing_torch_structured; monkeypatch.setattr; test_missing_torch_structured_raises_clear_error; test_missing_ldr_raises_clear_error; test_local_impls_work_without_torch_structured"
    - path: ".planning/codebase/TESTING.md"
      provides: "Updated Mocking section reflecting that Phase 3 introduced pytest's monkeypatch fixture for STR-03; restriction lifted or scoped."
      contains: "monkeypatch"
    - path: ".planning/phases/03-structured-pytorch-fallback-parity/03-SUMMARY.md"
      provides: "Phase-exit SUMMARY: REQ closure for STR-01/02/03, bd tally, locked-file integrity verification, max-abs-diff datums, hand-off to Phase 4."
      contains: "STR-01.*STR-02.*STR-03.*Phase 4"
  key_links:
    - from: "test_missing_torch_structured_raises_clear_error"
      to: "make_structured_linear in src/gru_qat/structure.py:118-174"
      via: "monkeypatch.setattr('gru_qat.structure._import_torch_structured', _raise_missing_torch_structured)"
      pattern: "monkeypatch.setattr"
    - from: "test_missing_ldr_raises_clear_error"
      to: "src/gru_qat/structure.py:160-172 (LDR-specific import branch)"
      via: "Option C — sys.modules manipulation OR a sentinel module that raises on attribute access (bypasses _import_torch_structured)"
      pattern: "sys.modules"
    - from: ".planning/codebase/TESTING.md"
      to: "tests/test_structure_parity.py (Phase 3 STR-03 monkeypatch usage)"
      via: "Mocking section update documenting the new convention"
      pattern: "monkeypatch"
---

<objective>
Close STR-03 by asserting (1) all optional-`torch-structured` kinds (monarch, butterfly, ldr) raise `ImportError` with a clear "torch-structured" install hint when the dependency is missing, and (2) all local-impl kinds (dense, diagonal, circulant) continue to work without the dependency. Also: update `.planning/codebase/TESTING.md` to reflect that Phase 3 introduces pytest's `monkeypatch` fixture (the codebase had zero monkeypatch usage before Phase 3 — see PATTERNS.md "NEW PATTERN FLAG"). Then write the phase-exit SUMMARY closing all three STR-* requirements, recording bd findings, verifying locked-file integrity, and handing off to Phase 4.

Purpose: STR-03 closes the third and final Phase 3 requirement. The phase-exit SUMMARY is the audit artifact that downstream phases (Phase 4 = quant-on bit-identity) consume to understand which Phase 3 assertions can be trusted.

Output:
- `tests/test_structure_parity.py` extended with STR-03 section at the bottom (below the LDR section from plan 03-02).
- `.planning/codebase/TESTING.md` updated to lift or scope the "Mocking: None" restriction.
- `.planning/phases/03-structured-pytorch-fallback-parity/03-SUMMARY.md` written.
- All Phase 3 tests pass on CPU at `pytest tests/test_structure_parity.py -q` and `-m slow -q`.
- `tests/test_parity.py`, `tests/test_layer_parity.py`, `tests/test_structure.py` UNCHANGED across all Phase 3 commits.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/REQUIREMENTS.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-structured-pytorch-fallback-parity/03-CONTEXT.md
@.planning/phases/03-structured-pytorch-fallback-parity/03-PATTERNS.md
@.planning/phases/03-structured-pytorch-fallback-parity/03-01-SUMMARY.md
@.planning/phases/03-structured-pytorch-fallback-parity/03-02-SUMMARY.md
@.planning/codebase/TESTING.md
@src/gru_qat/structure.py
@tests/test_structure_parity.py
@tests/test_parity.py

<interfaces>
Target paths under audit (DO NOT modify in this plan unless STR-03 surfaces a real bug):

From `src/gru_qat/structure.py:60-69` — `_import_torch_structured()` lazy-import helper:
  - Soft-imports `torch_structured`; on ImportError re-raises a wrapped `ImportError("torch-structured is required for structured GRU weights. Install with: pip install 'gru-qat[structured]'") from e`.
  - This is the monkeypatch target for monarch + butterfly path testing.

From `src/gru_qat/structure.py:160-172` — LDR-specific import branch in `make_structured_linear`:
  - Directly imports `from torch_structured.structured.layers import LDRSubdiagonal` (NOT via `_import_torch_structured`).
  - On ImportError re-raises `ImportError("torch-structured is required for kind='ldr'. Install with: pip install 'gru-qat[structured]'") from e`.
  - This is the LDR-specific bypass; the `_import_torch_structured` monkeypatch will NOT affect this branch. Plan must handle LDR separately.

From `src/gru_qat/structure.py:140-145` (monarch): calls `ts = _import_torch_structured(); return ts.monarch.blockdiag_linear.BlockdiagLinear(...)`. Patching `_import_torch_structured` makes this raise.

From `src/gru_qat/structure.py:151-158` (butterfly): same pattern as monarch. Patching `_import_torch_structured` makes this raise.

Local-impl kinds (do NOT need `torch-structured`):
- `dense`: lines 132-133 — returns `nn.Linear` directly.
- `diagonal`: lines 137-138 — returns `_DiagonalLinear` (local).
- `circulant`: lines 146-149 — returns `_CirculantLinear` (local). Per the source comment at line 147-148: "matches torch_structured.factory._CirculantLinear so we don't depend on a private symbol from there".

LDR-specific testing approach (from PATTERNS.md "Planner: resolve the LDR import-path divergence" lines 506-523 and CONTEXT.md "Critical implementation note from PATTERNS.md"):

CONTEXT says the planner recommends "Option C": a separate sub-test for LDR using a `sys.modules`-pop pattern instead of the `_import_torch_structured` monkeypatch. Use Option C: parametrize the main test over only `monarch` and `butterfly` (via `_import_torch_structured` monkeypatch), then have a separate `test_missing_ldr_raises_clear_error` that uses `monkeypatch.setitem(sys.modules, "torch_structured.structured.layers", _SentinelModule())` (or similar) so the direct `from torch_structured.structured.layers import LDRSubdiagonal` at `src/gru_qat/structure.py:164` raises.

From `tests/test_parity.py:186` and `tests/test_structure.py:243, 251, 257`: established `pytest.raises(<Error>, match="...")` idiom — use `pytest.raises(ImportError, match=r"torch-structured")`.

From PATTERNS.md "STR-03 missing-dep test (D-34)" lines 437-549: full template for the monkeypatch helper `_raise_missing_torch_structured` and the parametrized test bodies.

From `.planning/codebase/TESTING.md:201-203`: current "Mocking: None. No `unittest.mock`, no `pytest-mock`, no monkeypatching." — must be updated.
</interfaces>

Phase 3 boundary reminders (this plan):

- Phase 3 audits ONLY Circulant + LDR per-step PyTorch paths at fp32 with Identity quantizers, plus STR-03 graceful-degradation.
- This plan (03-03) covers STR-03 + TESTING.md update + phase-exit SUMMARY.
- CUDA NOT required. No `@cuda_only` decorators.
- Phase 3 INTRODUCES `monkeypatch` to the codebase. The `monkeypatch` fixture is pytest built-in (no new dependency).
- Two-commit failing-test-before-fix (D-37): if a graceful-degradation test surfaces a bug (e.g., the LDR-specific import raises an `AttributeError` instead of `ImportError`, or the message doesn't include "torch-structured"), file `bd create`, Commit A is the failing test, Commit B is the fix in `src/gru_qat/structure.py`.
- Locked files (D-28 / D-38): `tests/test_parity.py`, `tests/test_layer_parity.py`, `tests/test_structure.py` git diffs must be empty.
- Hand-off (D-39): hand-rolled references and test infrastructure stay in `tests/`. No `src/` modifications unless a finding requires one.
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: STR-03 monkeypatch tests (monarch + butterfly + LDR + local-impl companion)</name>
  <read_first>
    - tests/test_structure_parity.py (as left by plan 03-02)
    - src/gru_qat/structure.py (lines 60-69 for `_import_torch_structured`; lines 118-174 for `make_structured_linear`; lines 160-172 for the LDR-specific import path bypass)
    - tests/test_parity.py (lines 180-210: the `pytest.raises(..., match=...)` idiom)
    - tests/test_structure.py (lines 235-261: existing shape-validator error-path tests using `pytest.raises`)
    - .planning/phases/03-structured-pytorch-fallback-parity/03-PATTERNS.md (section "STR-03 missing-dep test (D-34)" lines 437-549; "STR-03 companion test (D-34)" lines 527-549)
    - .planning/phases/03-structured-pytorch-fallback-parity/03-CONTEXT.md (D-34 + the "Critical implementation note from PATTERNS.md" — Option C is recommended for LDR)
    - pytest docs reference: https://docs.pytest.org/en/stable/how-to/monkeypatch.html (`monkeypatch.setattr`, `monkeypatch.setitem`)
  </read_first>
  <behavior>
    - `tests/test_structure_parity.py` ends with a NEW STR-03 section (75-char ASCII divider labeled `STR-03 section: graceful-degradation tests`).
    - The section adds `import sys` to the top-of-file imports (for the LDR `sys.modules` manipulation).
    - A module-level helper `_raise_missing_torch_structured() -> None` exists. Returns `None` annotation in the signature even though it never returns (it always raises). Raises `ImportError` with a message that BYTE-FOR-BYTE matches the production message at `src/gru_qat/structure.py:65-68`: `"torch-structured is required for structured GRU weights. Install with: pip install 'gru-qat[structured]'"`. (Matching verbatim makes the test assert the user-facing string, not just any ImportError.)
    - Test 1: `test_missing_torch_structured_raises_clear_error(kind, monkeypatch)` parametrized over `kind ∈ ["monarch", "butterfly"]` (NOT "ldr" — LDR has its own test below). Body: `monkeypatch.setattr("gru_qat.structure._import_torch_structured", _raise_missing_torch_structured)`. Build `cfg = StructureConfig(kind=kind, nblocks=4, butterfly_nblocks=1)`. Assert: `with pytest.raises(ImportError, match=r"torch-structured"): make_structured_linear(cfg, 32, 32)`.
    - Test 2: `test_missing_ldr_raises_clear_error(monkeypatch)` — NOT parametrized; LDR-specific (Option C from CONTEXT/PATTERNS). Body uses `monkeypatch.setitem(sys.modules, ...)` to make `from torch_structured.structured.layers import LDRSubdiagonal` at `src/gru_qat/structure.py:164` fail. Two reasonable approaches — executor picks the simplest that works:
      (a) Set `sys.modules["torch_structured.structured.layers"]` to a sentinel object whose `__getattr__` raises `ImportError` (or to `None`, which causes the import machinery to raise `ImportError("import of torch_structured.structured.layers halted; None in sys.modules")` on subsequent `from` imports).
      (b) Use `monkeypatch.delitem(sys.modules, "torch_structured.structured.layers", raising=False)` THEN `monkeypatch.setattr` the relevant package attribute to a sentinel whose attribute access raises.
      Assert: `cfg = StructureConfig(kind="ldr", ldr_rank=2); with pytest.raises(ImportError, match=r"torch-structured"): make_structured_linear(cfg, 32, 32)`.
    - Test 3: `test_local_impls_work_without_torch_structured(kind, monkeypatch)` parametrized over `kind ∈ ["dense", "diagonal", "circulant"]`. Body: `monkeypatch.setattr("gru_qat.structure._import_torch_structured", _raise_missing_torch_structured)`. Build `cfg = StructureConfig(kind=kind)`. Build `layer = make_structured_linear(cfg, 32, 32)`. Build `x = torch.randn(4, 32); y = layer(x)`. Assert `torch.isfinite(y).all()` and `y.shape == (4, 32)`.
    - `pytest tests/test_structure_parity.py -m "not slow" -q` collects 82 (from plan 03-02) + 2 (monarch + butterfly) + 1 (LDR-specific) + 3 (dense + diagonal + circulant local impls) = 88 fast tests; all pass.
    - `pytest tests/test_structure_parity.py -m slow -q` collects 24 (from plan 03-02) and all pass.
    - If LDR error path raises a non-ImportError or the message doesn't match: D-37 protocol — Commit A failing test, `bd create`, Commit B fix in `src/gru_qat/structure.py:160-172`.
  </behavior>
  <action>Extend `tests/test_structure_parity.py`. Add `import sys` to the top-of-file imports if not already present. Below the LDR section from plan 03-02, add a 75-char divider labeled `STR-03 section: graceful-degradation tests`. Define the module-level helper `_raise_missing_torch_structured` returning `None` per the <behavior> spec — verbatim production message string. Add Test 1: `@pytest.mark.parametrize("kind", ["monarch", "butterfly"])` over `def test_missing_torch_structured_raises_clear_error(kind: str, monkeypatch: pytest.MonkeyPatch) -> None`. Inside: monkeypatch `_import_torch_structured`, build `cfg = StructureConfig(kind=kind, nblocks=4, butterfly_nblocks=1)`, assert with `pytest.raises(ImportError, match=r"torch-structured")`. Add Test 2: `def test_missing_ldr_raises_clear_error(monkeypatch: pytest.MonkeyPatch) -> None`. Use the `sys.modules` trick (Option C). Pick the approach that produces a clean `ImportError` containing `"torch-structured"` in its message — recommended starting approach: `monkeypatch.setitem(sys.modules, "torch_structured.structured.layers", None)` causes Python's import machinery to raise on the subsequent `from torch_structured.structured.layers import LDRSubdiagonal`. If that doesn't yield the right error class/message, try a sentinel module class whose `__getattr__` raises ImportError. Once one works, lock it. Add Test 3: `@pytest.mark.parametrize("kind", ["dense", "diagonal", "circulant"])` over `def test_local_impls_work_without_torch_structured(kind: str, monkeypatch: pytest.MonkeyPatch) -> None`. Monkeypatch `_import_torch_structured`; build layer; check finite + shape. Do NOT modify the circulant or LDR sections from plans 03-01 / 03-02. If LDR test 2 surfaces a finding (e.g., the actual ImportError from `src/gru_qat/structure.py:166-169` doesn't match `r"torch-structured"` — note the message DOES include the literal string per the source, so this should pass; but if the `sys.modules` trick raises a different ImportError before hitting the wrapper, the wrapper might not run), the executor handles per D-37: Commit A is the failing test, `bd create`, Commit B fixes the production path to ensure the wrapper consistently raises with the install hint.</action>
  <verify>
    <automated>cd /home/claroche/gru-triton && uv run pytest tests/test_structure_parity.py -m "not slow" --collect-only -q 2>&1 | tail -3 && uv run pytest tests/test_structure_parity.py -m "not slow" -q 2>&1 | tail -15 && uv run pytest tests/test_structure_parity.py -m slow -q 2>&1 | tail -5 && uv run ruff check tests/test_structure_parity.py</automated>
  </verify>
  <acceptance_criteria>
    - `uv run pytest tests/test_structure_parity.py -m "not slow" --collect-only -q | tail -3` reports 88 tests collected (82 from plans 03-01 + 03-02 plus 2 + 1 + 3 = 6 new STR-03 tests).
    - `uv run pytest tests/test_structure_parity.py -m "not slow" -q` exits 0 (all 88 fast tests pass).
    - `uv run pytest tests/test_structure_parity.py -m slow -q` exits 0 (all 24 slow tests still pass).
    - `grep -c "def _raise_missing_torch_structured" tests/test_structure_parity.py` returns 1.
    - `grep -c "def test_missing_torch_structured_raises_clear_error" tests/test_structure_parity.py` returns 1.
    - `grep -c "def test_missing_ldr_raises_clear_error" tests/test_structure_parity.py` returns 1.
    - `grep -c "def test_local_impls_work_without_torch_structured" tests/test_structure_parity.py` returns 1.
    - `grep -c "monkeypatch.setattr" tests/test_structure_parity.py` returns >= 2 (Tests 1 + 3 monkeypatch `_import_torch_structured`).
    - `grep -c "sys.modules\|monkeypatch.setitem\|monkeypatch.delitem" tests/test_structure_parity.py` returns >= 1 (Test 2's LDR-specific approach).
    - `grep -c "import sys" tests/test_structure_parity.py` returns 1.
    - `grep -c "pytest.raises(ImportError, match=" tests/test_structure_parity.py` returns >= 2 (Tests 1 and 2 both raise-and-match).
    - `grep -c "@pytest.mark.xfail" tests/test_structure_parity.py` returns 0.
    - `uv run ruff check tests/test_structure_parity.py` exits 0.
    - `git diff tests/test_parity.py tests/test_layer_parity.py tests/test_structure.py` is empty.
    - If any finding surfaced: a `bd show <id>` exists for it; commit history shows Commit A → Commit B in the right order.
  </acceptance_criteria>
  <done>STR-03 section added with 4 test functions (2 monarch+butterfly + 1 LDR + 3 dense/diagonal/circulant via parametrize). All 88 fast tests pass. All 24 slow tests pass. Locked files unchanged. No xfail.</done>
</task>

<task type="auto">
  <name>Task 2: Update TESTING.md "Mocking" section + write phase-exit SUMMARY</name>
  <read_first>
    - .planning/codebase/TESTING.md (lines 201-203: current "Mocking: None" block)
    - .planning/phases/03-structured-pytorch-fallback-parity/03-CONTEXT.md (full file — for the "must_haves" inventory)
    - .planning/phases/03-structured-pytorch-fallback-parity/03-01-SUMMARY.md (plan 03-01's findings — pulled into phase SUMMARY)
    - .planning/phases/03-structured-pytorch-fallback-parity/03-02-SUMMARY.md (plan 03-02's findings)
    - .planning/phases/01-reference-path-parity-vs-nn-gru/01-VERIFICATION.md OR a similarly-named phase-exit SUMMARY from Phase 1 (model for SUMMARY structure; if Phase 2 has a comparable artifact prefer that — search `ls .planning/phases/02-*/`)
    - `bd ready` and `bd list --status closed` output (to count Phase 3 bd issues filed and closed)
  </read_first>
  <behavior>
    - `.planning/codebase/TESTING.md` "Mocking" section (lines 201-203 in the original) is rewritten to reflect the Phase 3 introduction of `monkeypatch`. New text says (paraphrased — the executor wordsmiths): the suite uses `pytest`'s built-in `monkeypatch` fixture (no `unittest.mock`, no `pytest-mock`) for ONE specific purpose: simulating missing optional dependencies in STR-03 graceful-degradation tests (`tests/test_structure_parity.py`). All other tests exercise real PyTorch / real Triton kernels. The `monkeypatch` fixture resets cleanly between tests via pytest, so no test pollution risk. If new uses of `monkeypatch` are added, justify in the test docstring why a real-dependency test isn't feasible.
    - The `_underscore_prefixed` Identity-quantizer note in the current TESTING.md remains intact.
    - `.planning/phases/03-structured-pytorch-fallback-parity/03-SUMMARY.md` is created at the phase root (`.planning/phases/03-structured-pytorch-fallback-parity/03-SUMMARY.md`). Contents include:
      1. **Frontmatter** YAML block with `phase: 3`, `phase_name: structured-pytorch-fallback-parity`, `status: complete`, `closed_at: <iso date>`, `requirements_closed: [STR-01, STR-02, STR-03]`.
      2. **Audit verdict** — one-line PASS/FIX/FINDING for each of STR-01, STR-02, STR-03.
      3. **Tier-by-tier max abs diff datums** — circulant fwd max abs diff (worst case across the grid), circulant bwd, LDR fwd, LDR bwd. These come from the 03-01-SUMMARY and 03-02-SUMMARY artifacts.
      4. **bd findings tally** — table with columns `bd_id`, `requirement`, `summary`, `root_cause`, `fix_commit`, `regression_test`. Empty if no findings; populated from `bd list` filtered to Phase 3 issues.
      5. **Locked-file integrity verification** — output of `git diff $(git merge-base HEAD <phase-3-base>)..HEAD -- tests/test_parity.py tests/test_layer_parity.py tests/test_structure.py` (must be empty). Record the actual command and result.
      6. **New patterns introduced in Phase 3** — bullet list: (a) `monkeypatch` fixture (TESTING.md updated), (b) `torch.fft.fft` / `torch.fft.ifft` full-complex FFT (first appearance in tests), (c) two-reference cross-check methodology (Toeplitz vs FFT before either compared to production), (d) external-library-as-spec read (LDR slow-Krylov reconstruction).
      7. **Hand-off to Phase 4** — explicit note that Phase 4 (Quant-on bit-identity) now has trusted circulant + LDR per-step paths to compare against; phase 4 will exercise these paths with non-Identity quantizers. Flag: `_LDRLinear` does NOT have in-kernel fake-quant (it uses `torch_structured`'s forward directly), so Phase 4 must apply the output-side fake-quant pattern documented in `src/gru_qat/structure.py:14-20` module docstring.
      8. **Velocity datum** — count of net-new test cases added in Phase 3 (88 fast + 24 slow = 112 cases). Compare to estimate in PATTERNS.md / CONTEXT.md.
    - All Phase 3 tests still pass on a clean `pytest tests/test_structure_parity.py -q` run after the docs are committed.
  </behavior>
  <action>Update `.planning/codebase/TESTING.md`: edit the "Mocking" section near line 201 to reflect the Phase 3 introduction of `monkeypatch`. Preserve the existing line about the Identity quantizer being the only "mock-like" pattern. Add new prose covering: pytest's `monkeypatch` is used in `tests/test_structure_parity.py` for STR-03 graceful-degradation tests; no `unittest.mock`, no `pytest-mock`; new uses must justify in docstring. Then create `.planning/phases/03-structured-pytorch-fallback-parity/03-SUMMARY.md` per the <behavior> structure: YAML frontmatter, audit verdict table, max-abs-diff datums (read from 03-01-SUMMARY and 03-02-SUMMARY), bd tally (run `bd list --status closed` and `bd list --status open` filtered to Phase 3 issues — use a label or title-prefix query), locked-file integrity check (run the `git diff` against the phase-base and paste the actual output; should be empty), new patterns bullet list, hand-off to Phase 4, velocity datum. Use the existing Phase 1 / Phase 2 phase-exit SUMMARY as a structural analog if present at `.planning/phases/01-*/*-SUMMARY.md` or `.planning/phases/02-*/*-SUMMARY.md`. Do NOT modify `src/`. Do NOT modify the test files except for trivial typo fixes if you find any (and only if uncovered during the final pytest run — no speculative changes). After the SUMMARY is written, run the final verification commands: full pytest, ruff, mypy, and locked-file diff.</action>
  <verify>
    <automated>cd /home/claroche/gru-triton && uv run pytest tests/test_structure_parity.py -q 2>&1 | tail -10 && uv run pytest tests/test_structure_parity.py -m slow -q 2>&1 | tail -5 && uv run pytest tests/test_structure.py -q 2>&1 | tail -5 && uv run ruff check src tests 2>&1 | tail -5 && uv run mypy 2>&1 | tail -5 && git diff --stat tests/test_parity.py tests/test_layer_parity.py tests/test_structure.py 2>&1 | tail -3</automated>
  </verify>
  <acceptance_criteria>
    - `ls .planning/phases/03-structured-pytorch-fallback-parity/03-SUMMARY.md` shows the file exists.
    - `grep -c "STR-01\|STR-02\|STR-03" .planning/phases/03-structured-pytorch-fallback-parity/03-SUMMARY.md` returns >= 3.
    - `grep -c "Phase 4" .planning/phases/03-structured-pytorch-fallback-parity/03-SUMMARY.md` returns >= 1 (hand-off section).
    - `grep -c "monkeypatch" .planning/codebase/TESTING.md` returns >= 1 (Mocking section updated).
    - `grep "No.*monkeypatching" .planning/codebase/TESTING.md` returns no match OR the line is contextualized (no longer a blanket prohibition).
    - `uv run pytest tests/test_structure_parity.py -q` exits 0 (88 fast tests pass).
    - `uv run pytest tests/test_structure_parity.py -m slow -q` exits 0 (24 slow tests pass).
    - `uv run pytest tests/test_structure.py -q` exits 0 (smoke tier still green).
    - `uv run ruff check src tests` exits 0.
    - `uv run mypy` exits 0 (src/gru_qat only, strict — should be untouched if no `src/` mods).
    - `git diff tests/test_parity.py tests/test_layer_parity.py tests/test_structure.py` is empty (D-28 / D-38 across ALL Phase 3 commits).
    - SUMMARY contains a bd tally table (even if empty) — `grep -c "bd_id\|bd-" .planning/phases/03-structured-pytorch-fallback-parity/03-SUMMARY.md` returns >= 1.
    - SUMMARY contains the "New patterns introduced in Phase 3" bullet list — `grep -c "torch.fft.fft\|monkeypatch\|cross-check\|external-library" .planning/phases/03-structured-pytorch-fallback-parity/03-SUMMARY.md` returns >= 2.
  </acceptance_criteria>
  <done>TESTING.md "Mocking" section reflects Phase 3 reality. Phase-exit SUMMARY exists with REQ closure, bd tally, locked-file integrity, new patterns, hand-off to Phase 4, velocity datum. All Phase 3 tests pass. Ruff + mypy green. Locked files unchanged.</done>
</task>

</tasks>

<verification>
- `uv run pytest tests/test_structure_parity.py -q` passes with 88 fast tests (27 circulant + 1 LDR micro + 27 LDR fwd + 27 LDR bwd + 2 monarch/butterfly missing-dep + 1 LDR missing-dep + 3 local-impl-without-dep).
- `uv run pytest tests/test_structure_parity.py -m slow -q` passes with 24 slow tests.
- `uv run pytest tests/test_structure.py -q` still passes (smoke tier intact).
- `uv run ruff check src tests` is green.
- `uv run mypy` is green (src/gru_qat strict; tests untyped).
- `git diff <phase-3-base>..HEAD -- tests/test_parity.py tests/test_layer_parity.py tests/test_structure.py` is empty across ALL Phase 3 commits.
- `.planning/codebase/TESTING.md` "Mocking" section reflects the Phase 3 `monkeypatch` introduction.
- `.planning/phases/03-structured-pytorch-fallback-parity/03-SUMMARY.md` exists and contains all eight required sections.
- `grep -c "@pytest.mark.xfail" tests/test_structure_parity.py` returns 0.
</verification>

<success_criteria>
- STR-03 closed: monarch + butterfly + LDR all raise `ImportError` with "torch-structured" in the message when the dependency is missing; circulant + diagonal + dense work without it.
- LDR-specific import-path divergence (CONTEXT D-34 / PATTERNS "Critical implementation note") handled cleanly via Option C (separate `sys.modules`-based test).
- TESTING.md "Mocking" restriction lifted/scoped to reflect the Phase 3 reality.
- Phase-exit SUMMARY closes STR-01 + STR-02 + STR-03 with audit verdicts, max-abs-diff datums, bd findings tally, locked-file integrity verification, new-patterns bullet list, hand-off to Phase 4, velocity datum.
- Phase 4 is now unblocked: trusted circulant + LDR per-step paths are pinned at < 1e-5 against independent references; quant-on bit-identity work can proceed.
</success_criteria>

<output>
After completion, the phase is closed. The phase-exit SUMMARY at `.planning/phases/03-structured-pytorch-fallback-parity/03-SUMMARY.md` is the final artifact. STATE.md will be updated by the orchestrator after this plan completes; this plan does NOT touch STATE.md directly.
</output>
