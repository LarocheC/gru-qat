---
phase: 03-structured-pytorch-fallback-parity
plan: 02
type: execute
wave: 2
depends_on:
  - 03-01
files_modified:
  - tests/test_structure_parity.py
autonomous: true
requirements:
  - STR-02
must_haves:
  truths:
    - "Hand-rolled dense H×H matrix M built from LDR's (subd_A, subd_B, G, H) factors using the explicit slow Krylov construction agrees with _LDRLinear forward at < 1e-5 abs across (B, H, rank) ∈ {1, 4, 32} × {8, 32, 128} × {1, 4, 8} (fast) and H=512 (slow), with rank ≤ H."
    - "Verifier-driven micro-validation on a single (H=8, rank=2) case agrees with `_LDRLinear` at < 1e-5 before the parametrized grid runs (catches transpose-convention bugs in `_build_ldr_matrix_from_factors`)."
    - "_LDRLinear autograd gradients on all four factors (subd_A, subd_B, G, H) match hand-rolled-reference autograd gradients at < 1e-5 abs over the same grid."
    - "Plan 03-01's circulant tests still pass (regression check)."
    - "tests/test_parity.py and tests/test_layer_parity.py git diff empty after this plan's commits (D-28 / D-38)."
    - "tests/test_structure.py git diff empty after this plan's commits (D-38)."
  artifacts:
    - path: "tests/test_structure_parity.py"
      provides: "LDR section appended below circulant: per-section pytest.importorskip('torch_structured') guard, _build_ldr_matrix_from_factors helper, FAST_LDR_GRID / SLOW_LDR_GRID, test_handrolled_ldr_matches_production_micro one-shot validation, test_ldr_matches_handrolled_reference (fast+slow), test_ldr_backward_matches_autograd_reference (fast+slow)."
      contains: "def _build_ldr_matrix_from_factors; FAST_LDR_GRID = ; test_handrolled_ldr_matches_production_micro; test_ldr_matches_handrolled_reference; test_ldr_backward_matches_autograd_reference"
  key_links:
    - from: "_build_ldr_matrix_from_factors (slow Krylov + sum over rank)"
      to: "_LDRLinear / LDRSubdiagonal production path"
      via: "M @ x_b matches layer(x_b) for matching factors; transpose convention pinned via a micro-check"
      pattern: "for i in range\\(r\\)"
    - from: "test_ldr_backward_matches_autograd_reference"
      to: "autograd-grad on 4 leaves (subd_A_ref/prod, subd_B_ref/prod, G_ref/prod, H_ref/prod)"
      via: "detach-clone-twice per leaf; shared g; per-tensor named-failure loop with 4 entries"
      pattern: "subd_A.*subd_B.*G.*H_factor"
---

<objective>
Pin `_LDRLinear` (`src/gru_qat/structure.py:239-247`, which wraps `torch_structured.structured.layers.LDRSubdiagonal`) against a hand-rolled full-H×H matrix constructed from the LDR displacement-rank formula, using the EXPLICIT slow Krylov form from `torch_structured/structured/krylov.py:264-272` (NOT the FFT-based fast `krylov_multiply` that the production path uses). Compare forward and autograd backward at < 1e-5 abs across the shape × rank grid in CONTEXT D-36.

Purpose: STR-02 closes by asserting algebraic equivalence between the production FFT-accelerated LDR matmul and a textbook Krylov-summation construction. Without this, any future change to `torch-structured`'s `subdiag_mult` or refactor of `_LDRLinear` could silently break the math; the existing `test_structure.py` smoke tier (which only asserts finite-output + gradient-flow + loss-decrease) would not catch it.

Output:
- `tests/test_structure_parity.py` extended (NOT rewritten) with an LDR section below the circulant section:
  - A per-section `pytest.importorskip("torch_structured")` guard placed before the LDR-specific imports.
  - Module-level helper `_build_ldr_matrix_from_factors(subd_A, subd_B, G, H) -> torch.Tensor`.
  - `FAST_LDR_GRID` and `SLOW_LDR_GRID` module-level constants.
  - A one-shot micro-validation test `test_handrolled_ldr_matches_production_micro` to pin the transpose convention.
  - Two parametrized test families: `test_ldr_matches_handrolled_reference` (fast + slow siblings), `test_ldr_backward_matches_autograd_reference` (fast + slow siblings).
- All LDR test functions pass on CPU at < 1e-5 abs.
- Plan 03-01's circulant tests (27 fast + 6 slow) still pass.
- `tests/test_parity.py`, `tests/test_layer_parity.py`, `tests/test_structure.py` UNCHANGED.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/03-structured-pytorch-fallback-parity/03-CONTEXT.md
@.planning/phases/03-structured-pytorch-fallback-parity/03-PATTERNS.md
@.planning/phases/03-structured-pytorch-fallback-parity/03-01-SUMMARY.md
@.planning/codebase/TESTING.md
@.planning/codebase/CONVENTIONS.md
@src/gru_qat/structure.py
@tests/test_structure_parity.py
@tests/test_layer_parity.py

<interfaces>
Production target — DO NOT modify in this plan (D-39).

From `src/gru_qat/structure.py` (lines 239-247):
`class _LDRLinear(nn.Module)`:
  - `__init__(self, ldr: nn.Module) -> None`: stores `self.ldr = ldr` (an `LDRSubdiagonal` instance built by the factory at lines 160-172).
  - `forward(self, x: torch.Tensor) -> torch.Tensor`: returns `self.ldr(x)`.

Factory branch for `kind="ldr"` (lines 160-172): imports `LDRSubdiagonal` lazily from `torch_structured.structured.layers` (INDEPENDENT of `_import_torch_structured` — relevant for plan 03-03's STR-03 handling, not this plan's concern). Constructs `LDRSubdiagonal(layer_size=in_features, r=cfg.ldr_rank, bias=bias)`.

External library spec source (READ in Task 0):

From `torch_structured/structured/layers.py:211-225`: `class LDRSubdiagonal(StructuredLinear)`. Constructor signature `__init__(self, layer_size, r, bias=True, init_stddev=None)`. Parameters: `self.subd_A = nn.Parameter(torch.ones(layer_size - 1))`, `self.subd_B = nn.Parameter(torch.ones(layer_size - 1))`, `self.G = nn.Parameter(torch.empty(r, layer_size))` (init via `nn.init.normal_`), `self.H = nn.Parameter(torch.empty(r, layer_size))` (init via `nn.init.normal_`). Forward calls `kry.subdiag_mult(self.subd_A, self.subd_B, self.G, self.H, x)`.

From `torch_structured/structured/krylov.py:245-259`: `subdiag_mult(subd_A, subd_B, G, H, x)` — docstring says it computes `sum over i in [0, r) of K_A(G[i]) @ K_B(H[i]).T @ x` (verify by reading code).

From `torch_structured/structured/krylov.py:264-272`: slow explicit `Krylov(linear_map, v, m=None)` — returns the `n × n` matrix whose columns are `[v, A@v, A^2@v, ..., A^{n-1}@v]`. Independence from production's FFT path is the audit's whole point.

Analog for the helper pattern:

From `tests/test_structure_parity.py` (created in plan 03-01): module preamble + circulant helpers + `FAST_CIRC_GRID` / `SLOW_CIRC_GRID` already in place. Append LDR section BELOW the existing content; do not refactor or reorder the circulant section.

From `tests/test_layer_parity.py:546-557`: the per-tensor named-failure loop with multiple entries — model the 4-entry LDR loop on this exact shape.
</interfaces>

Phase 3 boundary reminders (this plan):

- Phase 3 audits ONLY Circulant + LDR per-step PyTorch paths at fp32 with Identity quantizers.
- This plan (03-02) covers ONLY LDR. Circulant is plan 03-01 (done). STR-03 graceful-degradation is plan 03-03.
- CUDA NOT required (the production path runs on CPU).
- LDR DOES need `torch-structured`: add `pytest.importorskip("torch_structured")` BEFORE the LDR-specific imports — but INSIDE the LDR section of the file, not at module top (the circulant section must continue to run on machines without `torch-structured`). Use `# noqa: E402` on subsequent imports per existing convention in `tests/test_structure.py:27`.
- Two-commit failing-test-before-fix (D-37): if a parity test fails on first run, `bd create` per finding, Commit A is the failing test (test only), Commit B is the fix. `xfail` is FORBIDDEN.
- D-39 caveat for LDR: the production path lives in `torch-structured`, not `src/gru_qat`. If the bug is upstream, file a bd issue documenting the finding and either pin a `torch-structured` version or adjust `_LDRLinear` to compensate — executor decides at write time. Hand-rolled references stay in the test file.
- Locked files (D-28 / D-38): `tests/test_parity.py`, `tests/test_layer_parity.py`, `tests/test_structure.py` git diffs must be empty.
- D-40: `'highest'` precision was set at module top by plan 03-01; do not re-set or change it.
- The `_build_ldr_matrix_from_factors` helper introduces a NEW test pattern: building a reference impl by reading an EXTERNAL library's source. Surface this in the SUMMARY (plan 03-03 codifies the TESTING.md update).
</context>

<tasks>

<task type="auto">
  <name>Task 0: Locate torch-structured and read the LDR spec source</name>
  <read_first>
    - .planning/phases/03-structured-pytorch-fallback-parity/03-PATTERNS.md (section "LDR forward + backward parity tests (D-32)" and "NEW PATTERN FLAG (D-32 / external library spec)" lines 200-211)
    - .planning/phases/03-structured-pytorch-fallback-parity/03-CONTEXT.md (D-32, D-33, D-37, D-39)
    - DEVELOPMENT.md (verify the install URL is `https://github.com/LarocheC/torch-structured`)
  </read_first>
  <action>Locate the installed `torch_structured` library on disk via `python -c "import torch_structured; print(torch_structured.__file__)"`. From the resulting path, identify the directory containing `structured/layers.py` and `structured/krylov.py`. Open `structured/layers.py` and read the `LDRSubdiagonal` class (around lines 180-269 per PATTERNS.md) — confirm the exact parameter shapes for `subd_A`, `subd_B` (both `(layer_size - 1,)`), `G`, `H` (both `(r, layer_size)`), and confirm `forward(x)` calls `kry.subdiag_mult(self.subd_A, self.subd_B, self.G, self.H, x)`. Then open `structured/krylov.py` and read both `subdiag_mult` (around lines 245-259) and the explicit slow `Krylov(linear_map, v, m=None)` construction (around lines 264-272). Record the EXACT slow-Krylov formula and the EXACT summation/transpose convention used by `subdiag_mult` — specifically whether the formula sums `K_A(G[i]) @ K_B(H[i]).T @ x` or `K_A(G[i]) @ K_B(H[i]) @ x` over `i`. Read the code, not just the docstring. Note these for Task 1. No file modifications in this task.</action>
  <verify>
    <automated>cd /home/claroche/gru-triton && uv run python -c "import torch_structured, os; p = os.path.dirname(torch_structured.__file__); print(p); print(os.path.exists(os.path.join(p, 'structured', 'layers.py'))); print(os.path.exists(os.path.join(p, 'structured', 'krylov.py')))"</automated>
  </verify>
  <acceptance_criteria>
    - `uv run python -c "import torch_structured"` exits 0. If not, the executor runs `uv pip install git+https://github.com/LarocheC/torch-structured` first.
    - Verify output: real path to `torch_structured/structured/layers.py` and `torch_structured/structured/krylov.py`; both `os.path.exists` return `True`.
    - The executor has read `LDRSubdiagonal.__init__` and `LDRSubdiagonal.forward` and confirmed the parameter names (`subd_A`, `subd_B`, `G`, `H`) and shapes.
    - The executor has read `subdiag_mult` source and recorded whether the formula is `sum_i K_A(G[i]) @ K_B(H[i]).T @ x` or `sum_i K_A(G[i]) @ K_B(H[i]) @ x`.
    - The executor has read the slow `Krylov(linear_map, v, m=None)` source (around krylov.py:264-272) and confirmed it returns the column-stacked `[v, A@v, A^2@v, ..., A^{n-1}@v]` matrix.
    - No files modified: `git status` is clean (or unchanged from before this task).
  </acceptance_criteria>
  <done>External library located and the LDR + Krylov source has been read end-to-end. The exact transpose convention used in `subdiag_mult` is known. No files modified.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 1: Append LDR section guard, helper, and micro-validation test</name>
  <read_first>
    - tests/test_structure_parity.py (as left by plan 03-01)
    - tests/test_structure.py (lines 16-34: `warnings.filterwarnings("ignore", message=".*different CUDA versions.*")` block + `pytest.importorskip("torch_structured")` + `# noqa: E402` pattern — the analog for the LDR section guard)
    - .planning/phases/03-structured-pytorch-fallback-parity/03-PATTERNS.md (section "Helper construction pattern (D-29: Toeplitz + FFT reference)" — the `_build_ldr_matrix_from_factors` skeleton at lines 146-198, including the transpose-convention warning at lines 190-197)
    - The notes from Task 0 (what `subdiag_mult` actually does — `K_A @ K_B.T` vs `K_A @ K_B`)
    - src/gru_qat/structure.py (lines 239-247: `_LDRLinear`)
  </read_first>
  <behavior>
    - File `tests/test_structure_parity.py` ends with a NEW section delimited by an ASCII comment divider per CONVENTIONS.md "Section dividers" (75-char rule), labeled `LDR section (STR-02)`.
    - The LDR section opens with: (a) `warnings.filterwarnings("ignore", message=".*different CUDA versions.*")` (mirror `tests/test_structure.py:18-20` — `torch_structured` emits a CUDA-version mismatch warning that is noise here; `import warnings` must be added to the top-of-file imports), (b) `torch_structured = pytest.importorskip("torch_structured")`, (c) `from torch_structured.structured.layers import LDRSubdiagonal  # noqa: E402`.
    - The top-of-file `from gru_qat.structure import ...` line extended to also import `_LDRLinear` (or a separate import line for `_LDRLinear` added in the LDR section guarded by the importorskip).
    - Module-level helper `_build_ldr_matrix_from_factors(subd_A: torch.Tensor, subd_B: torch.Tensor, G: torch.Tensor, H: torch.Tensor) -> torch.Tensor` exists with full type annotations and docstring referencing `torch_structured/structured/krylov.py:264-272` (slow form) and `:245-259` (spec).
    - Helper builds `A` as an explicit `(n, n)` subdiagonal matrix: `A[i+1, i] = subd_A[i]` for `i ∈ [0, n-1)`; same for `B` from `subd_B`. Defines inner `_krylov_explicit(M, v)` that returns the column-stacked `(n, n)` matrix `[v, M @ v, M^2 @ v, ..., M^(n-1) @ v]`. Returns `M = sum over i in range(r) of K_A(G[i]) @ K_B(H[i]).T` (or `K_A(G[i]) @ K_B(H[i])` — whichever Task 0 confirmed matches `subdiag_mult`).
    - Module-level `FAST_LDR_GRID` and `SLOW_LDR_GRID` constants per CONTEXT D-36: `FAST_LDR_GRID = [(B, H, rank) for B in (1, 4, 32) for H in (8, 32, 128) for rank in (1, 4, 8) if rank <= H]` (27 cases). `SLOW_LDR_GRID = [(B, H, rank) for B in (1, 4, 32) for H in (512,) for rank in (1, 4, 8)]` (9 cases).
    - One-shot micro-validation test `test_handrolled_ldr_matches_production_micro` (NOT parametrized): seeds; builds `ldr = LDRSubdiagonal(layer_size=8, r=2, bias=False); layer = _LDRLinear(ldr)`; reads `subd_A, subd_B, G, H_factor` from `ldr`; computes `y_prod = layer(x)` on `x = torch.randn(4, 8)`; builds `M = _build_ldr_matrix_from_factors(subd_A, subd_B, G, H_factor)`; computes `y_ref` using the row-vector idiom matching the convention chosen above (`x @ M.T` if `subdiag_mult` outputs `M @ x_b`); asserts `(y_prod - y_ref).abs().max() < 1e-5`. The failure message includes the actual `max_diff` in scientific notation and a hint about the transpose convention.
    - Plan 03-01 circulant tests still pass: `uv run pytest tests/test_structure_parity.py -k circulant -m "not slow" -q` exits 0.
  </behavior>
  <action>Open `tests/test_structure_parity.py`. Add `import warnings` to the top-of-file imports (if not already imported from plan 03-01). Extend the existing `from gru_qat.structure import ...` line to also import `_LDRLinear`. After the existing circulant section (helpers, FAST/SLOW circ grids, three test functions and their slow siblings), add a 75-char ASCII divider comment block labeled `LDR section (STR-02)`. Place the `warnings.filterwarnings(...)` line, then `torch_structured = pytest.importorskip("torch_structured")`, then `from torch_structured.structured.layers import LDRSubdiagonal  # noqa: E402`. Define `_build_ldr_matrix_from_factors` per the <behavior> spec — full type annotations, docstring referencing the krylov.py line numbers, inner `_krylov_explicit` helper, sum-over-r loop producing `M`. The transpose convention (`@ K_B.T` vs `@ K_B`) is fixed based on Task 0's reading. Add `FAST_LDR_GRID` and `SLOW_LDR_GRID` module-level lists. Add the one-shot micro-validation test `test_handrolled_ldr_matches_production_micro`: `torch.manual_seed(0); ldr = LDRSubdiagonal(layer_size=8, r=2, bias=False); layer = _LDRLinear(ldr); subd_A = ldr.subd_A.detach().clone(); subd_B = ldr.subd_B.detach().clone(); G = ldr.G.detach().clone(); H_factor = ldr.H.detach().clone(); x = torch.randn(4, 8); y_prod = layer(x); M = _build_ldr_matrix_from_factors(subd_A, subd_B, G, H_factor); y_ref = x @ M.T` (the row-vector idiom matching the production's `M @ x_b` formulation; `x @ M.T == (M @ x.T).T`); assertion `assert (y_prod - y_ref).abs().max() < 1e-5`. If the micro-test fails because of transpose convention, the executor reconciles by either flipping `K_A @ K_B.T` to `K_A @ K_B` in the helper, OR using `x @ M` instead of `x @ M.T` — pick whichever matches; lock the choice. Do NOT modify the circulant section. Do NOT add the parametrized LDR fwd/bwd tests yet — Task 2 adds them.</action>
  <verify>
    <automated>cd /home/claroche/gru-triton && uv run pytest tests/test_structure_parity.py::test_handrolled_ldr_matches_production_micro -v 2>&1 | tail -10 && uv run pytest tests/test_structure_parity.py -k circulant -m "not slow" -q 2>&1 | tail -5 && uv run ruff check tests/test_structure_parity.py</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "def _build_ldr_matrix_from_factors" tests/test_structure_parity.py` returns 1.
    - `grep -c "pytest.importorskip(\"torch_structured\")" tests/test_structure_parity.py` returns 1 (located inside the LDR section, NOT at module top).
    - `grep -c "from torch_structured.structured.layers import LDRSubdiagonal" tests/test_structure_parity.py` returns 1.
    - `grep -c "FAST_LDR_GRID\|SLOW_LDR_GRID" tests/test_structure_parity.py` returns >= 2.
    - `grep -c "def test_handrolled_ldr_matches_production_micro" tests/test_structure_parity.py` returns 1.
    - `grep -c "_LDRLinear" tests/test_structure_parity.py` returns >= 1 (imported and used in the micro-test).
    - `uv run pytest tests/test_structure_parity.py::test_handrolled_ldr_matches_production_micro -q` exits 0.
    - `uv run pytest tests/test_structure_parity.py -k circulant -m "not slow" -q` exits 0 (circulant regression still passes — 9 self-consistency + 9 fwd + 9 bwd = 27 from plan 03-01 still green).
    - `uv run ruff check tests/test_structure_parity.py` exits 0.
    - `git diff tests/test_parity.py tests/test_layer_parity.py tests/test_structure.py` is empty.
    - `grep -c "@pytest.mark.xfail" tests/test_structure_parity.py` returns 0.
  </acceptance_criteria>
  <done>LDR section guard, helper `_build_ldr_matrix_from_factors`, FAST_LDR_GRID/SLOW_LDR_GRID, and the one-shot micro-validation test all added; the micro-test passes; transpose convention is pinned; circulant regression green.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: LDR forward + backward parametrized parity tests</name>
  <read_first>
    - tests/test_structure_parity.py (after Task 1)
    - tests/test_layer_parity.py (lines 480-557: backward parity body — model the detach-clone-twice + shared-g + 4-entry per-tensor named-failure loop on this)
    - .planning/phases/03-structured-pytorch-fallback-parity/03-PATTERNS.md (section "LDR forward + backward parity tests (D-32)" lines 392-433; "Shared Patterns" lines 553-628)
    - .planning/phases/03-structured-pytorch-fallback-parity/03-CONTEXT.md (D-32, D-36)
  </read_first>
  <behavior>
    - Two parametrized test families added below the Task 1 content:
      1. `test_ldr_matches_handrolled_reference(B, H, rank)` — parametrized over `FAST_LDR_GRID` (27 cases). Plus `_slow` sibling over `SLOW_LDR_GRID` (9 cases) decorated `@pytest.mark.slow`. Both: seed; `ldr = LDRSubdiagonal(layer_size=H, r=rank, bias=False); layer = _LDRLinear(ldr)`; read 4 factors; `x = torch.randn(B, H)`; `y_prod = layer(x); M = _build_ldr_matrix_from_factors(...); y_ref = x @ M.T` (or matching convention); assertion `(y_prod - y_ref).abs().max() < 1e-5` with failure message `f"ldr fwd max abs diff {max_diff:.4e} (B={B},H={H},rank={rank})"`.
      2. `test_ldr_backward_matches_autograd_reference(B, H, rank)` — parametrized over `FAST_LDR_GRID`. Plus `_slow` sibling over `SLOW_LDR_GRID` decorated `@pytest.mark.slow`. Both: seed; build `ldr_init = LDRSubdiagonal(layer_size=H, r=rank, bias=False)` to source initial factor values; create 8 leaves via detach-clone-twice (subd_A_ref/prod, subd_B_ref/prod, G_ref/prod, H_ref/prod), all `.requires_grad_(True)`; `x = torch.randn(B, H); g = torch.randn(B, H)`. Reference path: `M = _build_ldr_matrix_from_factors(subd_A_ref, subd_B_ref, G_ref, H_ref); y_ref = x @ M.T; y_ref.backward(g)`. Production path: build a fresh `LDRSubdiagonal` and replace its 4 parameters with the `_prod` leaves (similar to circulant's `layer.col = nn.Parameter(c_prod)` trick — assign `ldr_prod.subd_A = nn.Parameter(subd_A_prod)`, etc.); wrap with `_LDRLinear`; `y_prod = layer_prod(x); y_prod.backward(g)`. Per-tensor named-failure loop with 4 entries: `for name, ref_t, prod_t in [("subd_A", ...), ("subd_B", ...), ("G", ...), ("H", ...)]:` assert `(ref_t - prod_t).abs().max() < 1e-5` with failure message `f"{name} max abs diff {max_diff:.4e} (B={B},H={H},rank={rank})"`.
    - Each test function calls `torch.manual_seed(0)` at the top of its body.
    - Full type annotations on every test function (`-> None`).
    - `pytest tests/test_structure_parity.py -m "not slow" -q` passes; total fast tests = 27 (circulant from plan 03-01) + 1 (LDR micro from Task 1) + 27 (LDR fwd fast) + 27 (LDR bwd fast) = 82 cases collected.
    - `pytest tests/test_structure_parity.py -m slow -q` passes; total slow tests = 6 (circulant slow) + 9 (LDR fwd slow) + 9 (LDR bwd slow) = 24 cases collected.
    - If any test fails on first run: D-37 two-commit protocol — Commit A is the failing test, `bd create`, Commit B is the fix in `src/gru_qat/structure.py` OR a documented compensating change with a bd issue tracking the upstream finding (D-39 caveat for LDR — production code lives in `torch-structured`).
  </behavior>
  <action>Extend `tests/test_structure_parity.py` (Task 1 left the LDR section guard + helper + micro-validation test in place). Add two parametrized test families. For test 1 (forward): mirror the circulant forward parity test from plan 03-01 — single-line `@pytest.mark.parametrize("B,H,rank", FAST_LDR_GRID)` decorator above the fast variant; stacked `@pytest.mark.slow` + `@pytest.mark.parametrize("B,H,rank", SLOW_LDR_GRID)` above the slow variant. Body: seed, build `LDRSubdiagonal`, wrap with `_LDRLinear`, read 4 factors via `.detach().clone()`, build `M` via the helper, compute `y_prod = layer(x)` and `y_ref = x @ M.T` (or the convention locked in Task 1), assert at < 1e-5 with the formatted failure message above. For test 2 (backward via autograd): 4 separate leaves on each side via detach-clone-twice. The production-side leaf-installation trick: build `ldr_prod = LDRSubdiagonal(layer_size=H, r=rank, bias=False)` then assign `ldr_prod.subd_A = nn.Parameter(subd_A_prod)`, `ldr_prod.subd_B = nn.Parameter(subd_B_prod)`, `ldr_prod.G = nn.Parameter(G_prod)`, `ldr_prod.H = nn.Parameter(H_prod)`; wrap with `_LDRLinear(ldr_prod)`; this makes the 4 prod leaves the layer's actual learnable parameters, so `y_prod.backward(g)` populates `subd_A_prod.grad`, etc. (Need to add `import torch.nn as nn` to the file's top-of-file imports if not already present — verify with `grep "import torch.nn as nn" tests/test_structure_parity.py`; if absent, add it.) Shared downstream gradient `g = torch.randn(B, H)`. Per-tensor named-failure loop iterates the 4 entries. Each test function starts with `torch.manual_seed(0)`. Do NOT modify the circulant section, the Task 1 LDR section guard/helper/micro-test, or any locked files. If any case fails at first run, surface as Commit A (no fix), file `bd create`, Commit B follows with the appropriate fix (D-37 / D-39 caveat for LDR upstream bugs).</action>
  <verify>
    <automated>cd /home/claroche/gru-triton && uv run pytest tests/test_structure_parity.py -m "not slow" --collect-only -q 2>&1 | tail -5 && uv run pytest tests/test_structure_parity.py -m "not slow" -q 2>&1 | tail -15 && uv run pytest tests/test_structure_parity.py -m slow -q 2>&1 | tail -10 && uv run ruff check tests/test_structure_parity.py</automated>
  </verify>
  <acceptance_criteria>
    - `uv run pytest tests/test_structure_parity.py -m "not slow" --collect-only -q | tail -3` reports 82 tests collected (27 circulant + 1 LDR micro + 27 LDR fwd + 27 LDR bwd).
    - `uv run pytest tests/test_structure_parity.py -m slow --collect-only -q | tail -3` reports 24 tests collected (6 circulant slow + 9 LDR fwd slow + 9 LDR bwd slow).
    - `uv run pytest tests/test_structure_parity.py -m "not slow" -q` exits 0 (all 82 fast tests pass).
    - `uv run pytest tests/test_structure_parity.py -m slow -q` exits 0 (all 24 slow tests pass).
    - `grep -c "def test_ldr_matches_handrolled_reference" tests/test_structure_parity.py` returns 2 (fast + slow function definitions).
    - `grep -c "def test_ldr_backward_matches_autograd_reference" tests/test_structure_parity.py` returns 2.
    - `grep -c "@pytest.mark.slow" tests/test_structure_parity.py` returns 4 (2 circulant slow + 2 LDR slow siblings).
    - `grep -c "@pytest.mark.xfail" tests/test_structure_parity.py` returns 0.
    - The 4-entry per-tensor named-failure loop in the backward test contains `"subd_A"`, `"subd_B"`, `"G"`, `"H"` as the `name` values: `grep -c "\"subd_A\"" tests/test_structure_parity.py` returns >= 1; similarly for `"subd_B"`, `"G"`, `"H"`.
    - `uv run ruff check tests/test_structure_parity.py` exits 0.
    - `git diff tests/test_parity.py tests/test_layer_parity.py tests/test_structure.py` is empty.
    - If any test FAILED on first run: a `bd show <id>` exists with the failing test name as title; commit history shows Commit A (test only, fails) followed by Commit B (fix). For upstream bugs (D-39 caveat), the bd issue notes "upstream finding in torch-structured" and Commit B applies the compensating change with a comment pointing at the bd issue.
  </acceptance_criteria>
  <done>4 LDR parity test functions (2 fast + 2 slow siblings) added; 82 fast + 24 slow tests collected and pass at < 1e-5 abs; locked files unchanged; no xfail; D-37 protocol followed if any finding surfaced.</done>
</task>

</tasks>

<verification>
- `uv run pytest tests/test_structure_parity.py -q` (no-slow default) passes with 82 tests (27 circulant + 1 LDR micro + 27 LDR fwd + 27 LDR bwd).
- `uv run pytest tests/test_structure_parity.py -m slow -q` passes with 24 tests.
- `uv run pytest tests/test_structure.py -q` still passes (smoke tier intact).
- `uv run ruff check tests/test_structure_parity.py` is green.
- `git diff <phase-3-base>..HEAD -- tests/test_parity.py tests/test_layer_parity.py tests/test_structure.py` is empty.
- `grep -c "@pytest.mark.xfail" tests/test_structure_parity.py` returns 0.
- No `src/gru_qat/structure.py` modifications UNLESS a parity test surfaced a bug under D-37. If LDR surfaced an upstream finding in `torch-structured`, a bd issue documents it and either a version pin or `_LDRLinear` compensation lands in Commit B.
</verification>

<success_criteria>
- STR-02 closed: forward + autograd backward of `_LDRLinear` match a hand-rolled slow-Krylov reference at < 1e-5 abs over `H ∈ {8, 32, 128}` (fast) and `H = 512` (slow), at `B ∈ {1, 4, 32}` and `rank ∈ {1, 4, 8}` with `rank ≤ H`.
- The hand-rolled reference is provably independent of the production path: it reads only the slow `Krylov` explicit-column-stacking formula from `torch_structured/structured/krylov.py:264-272` and reconstructs the dense H×H matrix `M`, then multiplies `x @ M.T` (no FFT, no `subdiag_mult`).
- The one-shot micro-validation test on (H=8, rank=2) pins the transpose convention before the parametrized grid runs.
- Plan 03-01 circulant tests still green (regression check).
- Locked test files (`test_parity.py`, `test_layer_parity.py`, `test_structure.py`) unchanged.
- No `@pytest.mark.xfail` anywhere.
- If any parity finding surfaced: bd issue exists, Commit A precedes Commit B, both green.
</success_criteria>

<output>
After completion, create `.planning/phases/03-structured-pytorch-fallback-parity/03-02-SUMMARY.md` covering:
- What was built: LDR section guard, `_build_ldr_matrix_from_factors`, FAST/SLOW grid, micro-test, fast + slow LDR fwd + bwd test families.
- Tier-by-tier results: fast tier pass count + slow tier pass count + max abs diff observed for fwd and bwd (record actual worst-case across all parametrized cases).
- Any finding surfaced: bd issue ID, root cause (in `src/gru_qat/structure.py` OR upstream in `torch-structured` — note distinction explicitly), fix commit hash, regression-test path.
- External-library spec read confirmation: which `torch_structured` version was on disk; whether `subdiag_mult` used `@ K_B.T` or `@ K_B` (for future maintenance reference).
- Hand-off note to plan 03-03: file is now complete for fwd/bwd parity; plan 03-03 will add STR-03 graceful-degradation tests at the bottom of the file and write the phase-exit SUMMARY.
</output>
