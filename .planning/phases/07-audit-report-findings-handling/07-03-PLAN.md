---
phase: 07-audit-report-findings-handling
plan: 03
type: execute
wave: 3
depends_on: [07-01, 07-02]
files_modified:
  - .planning/REQUIREMENTS.md
autonomous: true
requirements: [RPT-01, RPT-02]
must_haves:
  truths:
    - "All 14 bd issues are CLOSED (or DEFERRED-v2 with a bd ref recorded in REQUIREMENTS.md) — `bd ready` returns zero issues"
    - "Each FIX-bucket issue (n20, 7rj, 4m6) is closed with a resolution note pointing at its fix commit + regression test"
    - "Each ACCEPTED-DIVERGENCE issue (in0, q3k, lqk, 5rk, mjy, lht, e7t, fpl, 6dz) is closed with a resolution note referencing the AUDIT-REPORT residual-divergences section"
    - "Each INDIVIDUAL issue (e0l hardware, u00 process) is closed with its dispositioned resolution note"
    - "A git-log audit confirms test-before-fix ordering for Phase 1-6 findings; gaps are documented (not history-rewritten)"
  artifacts:
    - path: ".planning/REQUIREMENTS.md"
      provides: "v2 bd-ref records for any deferred issue; updated traceability if touched"
      contains: "RPT-01"
  key_links:
    - from: "each of the 14 bd issues"
      to: "closed status with resolution note"
      via: "bd close <id> with resolution"
      pattern: "bd close"
---

<objective>
Drive `bd ready` to empty (D-10) by closing all 14 open bd issues with disposition-appropriate resolution notes, and audit the Phase 1-6 git history for test-before-fix ordering (D-09). This plan is its own wave because every resolution note must reference the FINAL post-fix state produced by Waves 1-2.

Purpose: ROADMAP criterion #4 requires `bd ready` show no unresolved findings — every issue CLOSED or DEFERRED-v2 with a bd ref in `REQUIREMENTS.md`. ACCEPTED-DIVERGENCE issues are CLOSED (not left open) with a resolution note. Criterion #1 (RPT-01) requires confirming the failing-test-before-fix discipline held across Phases 1-6; where it genuinely didn't, the gap is documented, NOT rewritten (D-09).
Output: 14 closed bd issues, a git-log test-before-fix audit result (handed to plan 07-04 for the AUDIT-REPORT), v2 deferral records in `REQUIREMENTS.md` if any issue is deferred.

This plan does NOT need a GPU — it is bd-issue closure, a git-log read, and a REQUIREMENTS.md edit.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/07-audit-report-findings-handling/07-CONTEXT.md
@.planning/phases/07-audit-report-findings-handling/07-RESEARCH.md
@.planning/REQUIREMENTS.md

<interfaces>
<!-- The 14 bd issues by bucket (from RESEARCH.md triage table — decisive, evidence-backed): -->
FIX (3):                  gru-triton-n20, gru-triton-7rj, gru-triton-4m6
ACCEPTED-DIVERGENCE (9):  gru-triton-in0, gru-triton-q3k, gru-triton-lqk, gru-triton-5rk,
                          gru-triton-mjy, gru-triton-lht (duplicate-of-mjy), gru-triton-e7t,
                          gru-triton-fpl, gru-triton-6dz
INDIVIDUAL (2):           gru-triton-e0l (hardware limit, _skip_if_monarch_bwd_hw_limit covers it),
                          gru-triton-u00 (process race, mitigated by single-plan discipline)

<!-- Closure verbs: `bd close <id>` after recording the resolution note. -->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Git-log test-before-fix audit (D-09) for Phases 1-6 findings</name>
  <read_first>
    - .planning/phases/07-audit-report-findings-handling/07-CONTEXT.md (D-09 — audit ordering, document gaps, do NOT rewrite history)
    - .planning/phases/07-audit-report-findings-handling/07-RESEARCH.md (RPT-01 row; Open Question 2 — Phases 1-3 predate D-37/D-50)
    - .planning/phases/01-*/01-SUMMARY.md, 02-*/02-SUMMARY.md, 03-*/03-SUMMARY.md, 04-*/04-SUMMARY.md, 05-*/05-SUMMARY.md, 06-*/06-SUMMARY.md (per-phase findings lists)
    - bd show <id> for each of the 14 issues (each carries a fix-commit and regression-test reference)
  </read_first>
  <action>
    For every Phase 1-6 finding (the 14 bd issues plus any in-phase fix recorded in the NN-SUMMARY files — e.g. QNT-04 `gru-triton-x15`, EDG-02 `gru-triton-ehf`/`c2a`), inspect `git log --oneline` for the relevant commits and confirm the failing-test (`test(...)`) commit SHA precedes the fix (`fix(...)`) commit SHA. Phases 4-6 followed D-37/D-50; Phases 1-3 predate it. Where the test-before-fix ordering is genuinely absent in history, RECORD the gap (which finding, which commits) — do NOT rewrite git history (D-09 explicit). Produce a structured ordering-audit result (a table: finding → test commit → fix commit → ordering OK/GAP) and write it to `.planning/phases/07-audit-report-findings-handling/07-git-log-audit.txt` so plan 07-04 can quote it directly into the AUDIT-REPORT per-phase summary.
  </action>
  <verify>
    <automated>test -f .planning/phases/07-audit-report-findings-handling/07-git-log-audit.txt && grep -qE 'OK|GAP' .planning/phases/07-audit-report-findings-handling/07-git-log-audit.txt</automated>
  </verify>
  <acceptance_criteria>
    - Artifact: `.planning/phases/07-audit-report-findings-handling/07-git-log-audit.txt` exists with a finding→test-commit→fix-commit→ordering table covering every Phase 1-6 finding
    - Source: each row cites concrete commit SHAs from `git log`
    - Behavior: no `git rebase`/`git commit --amend`/history-rewriting command is run (D-09 — gaps documented, not rewritten)
  </acceptance_criteria>
  <done>Every Phase 1-6 finding has a documented test-before-fix ordering verdict (OK or GAP) with cited SHAs; the audit result file is ready for plan 07-04.</done>
</task>

<task type="auto">
  <name>Task 2: Close all 14 bd issues with disposition-appropriate resolution notes (D-10)</name>
  <read_first>
    - .planning/phases/07-audit-report-findings-handling/07-CONTEXT.md (D-01..D-04, D-10 — bucket dispositions; `bd ready` must end empty; ACCEPTED-DIVERGENCE issues are CLOSED not left open)
    - .planning/phases/07-audit-report-findings-handling/07-RESEARCH.md (the per-issue triage table — the verbatim bucket + Phase-7-action for all 14)
    - bd show <id> for each of the 14 issues (current state + root-cause text)
    - .planning/REQUIREMENTS.md (v2 section — where any deferred-issue bd ref is recorded)
  </read_first>
  <action>
    Close all 14 issues so `bd ready` returns zero. Per bucket:
    - FIX (3): `gru-triton-n20`, `gru-triton-7rj`, `gru-triton-4m6` — close each with a resolution note pointing at its Wave 1/2 fix commit SHA + its regression-test path (`tests/test_calibration.py` for n20, `tests/test_scan_wrapper_validation.py` for 7rj, the mypy/ruff 0/0 gate for 4m6). `bd close gru-triton-n20`, `bd close gru-triton-7rj`, `bd close gru-triton-4m6`.
    - ACCEPTED-DIVERGENCE (9): `in0`, `q3k`, `lqk`, `5rk`, `mjy`, `e7t`, `fpl`, `6dz` — close each with a resolution note: root cause is the irreducible TF32 `tl.dot`/`tl.sum` reduction-order non-associativity, the `input_precision="ieee"` fix is out of scope (PROJECT.md Option C Key Decision), the case is now `divergence`-marked, and the resolution is documented in the AUDIT-REPORT residual-divergences section. `gru-triton-lht` — close as duplicate-of-`mjy` (RESEARCH.md: explicitly subsumed) with a note pointing at `mjy`. `bd close` each.
    - INDIVIDUAL (2): `gru-triton-e0l` — close as INDIVIDUAL-hardware: a genuine RTX 2000 Ada SMEM/`tl.dot` K<16 limit, covered by the existing `_skip_if_monarch_bwd_hw_limit` skip, kernel-tiling redesign deferred to v2. `gru-triton-u00` — close as INDIVIDUAL-process: a parallel-execution race, mitigated by the single-plan-per-shared-file discipline adopted in Phases 5-7, AUDIT-REPORT process-note. `bd close` each.
    For any issue whose remediation is genuinely deferred to v2 (e.g. the `e0l` kernel-tiling redesign), record a v2 bd reference in `.planning/REQUIREMENTS.md` v2 section so the deferral is traceable. Verify with `bd ready` returning zero issues and `bd list --status=open` empty.
  </action>
  <verify>
    <automated>bd ready 2>/dev/null | grep -qiE 'no .*issues|Ready: 0' || [ -z "$(bd list --status=open 2>/dev/null | grep '^○')" ]</automated>
  </verify>
  <acceptance_criteria>
    - Behavior: `bd ready` returns zero issues
    - Behavior: `bd list --status=open` shows no open issues
    - Source: `bd show gru-triton-n20` (and 7rj, 4m6) status is closed with a resolution note citing the fix commit + regression test
    - Source: `bd show gru-triton-in0` (and the other 8 ACCEPTED-DIVERGENCE issues) status is closed with a resolution note referencing the TF32 root cause + AUDIT-REPORT residual section
    - Source: `bd show gru-triton-e0l` and `bd show gru-triton-u00` are closed with their INDIVIDUAL dispositions
    - Source: `.planning/REQUIREMENTS.md` v2 section records a bd ref for any v2-deferred remediation (e.g. e0l kernel-tiling)
  </acceptance_criteria>
  <done>All 14 bd issues closed with disposition-appropriate resolution notes; `bd ready` empty; v2 deferrals recorded in REQUIREMENTS.md.</done>
</task>

</tasks>

<threat_model>
Phase 7 hardens input validation (7rj) and fixes a silent quantizer-correctness bug (n20); no new attack surface — net risk reduction. This plan is pure issue-tracker closure + a documentation edit; no code, no security surface.
</threat_model>

<verification>
- `bd ready` returns zero issues; `bd list --status=open` is empty.
- All 14 `bd show <id>` show closed status with resolution notes.
- `.planning/phases/07-audit-report-findings-handling/07-git-log-audit.txt` exists with the test-before-fix ordering table.
- `.planning/REQUIREMENTS.md` v2 section records any deferred bd ref.
- No git history was rewritten (D-09).
</verification>

<success_criteria>
- ROADMAP criterion #4 met: `bd ready` empty, every one of the 14 issues closed or v2-deferred with a bd ref.
- ROADMAP criterion #1 inputs ready: the git-log test-before-fix audit is complete and documented for plan 07-04.
- ACCEPTED-DIVERGENCE issues are CLOSED (not left open), each with a resolution note.
</success_criteria>

<output>
After completion, create `.planning/phases/07-audit-report-findings-handling/07-03-SUMMARY.md`.
</output>
