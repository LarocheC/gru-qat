---
phase: 07-audit-report-findings-handling
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/gru_qat/triton_kernels/scan.py
  - src/gru_qat/triton_kernels/scan_diagonal.py
  - src/gru_qat/triton_kernels/scan_monarch.py
  - src/gru_qat/triton_kernels/scan_butterfly.py
  - src/gru_qat/ste.py
  - src/gru_qat/structure.py
  - src/gru_qat/gru_layer.py
  - src/gru_qat/gru_cell.py
  - src/gru_qat/quantizers.py
  - tests/test_scan_wrapper_validation.py
  - tests/test_triton_scan.py
  - tests/test_structure.py
  - tests/test_butterfly_dispatch.py
  - tests/test_triton_monarch.py
  - tests/test_parity.py
  - pyproject.toml
autonomous: true
requirements: [RPT-01, RPT-02]
must_haves:
  truths:
    - "gru_scan* wrappers raise ValueError/RuntimeError (not bare assert) on malformed shape/dtype, surviving python -O"
    - "mypy exits 0 against src/gru_qat (was ~145 errors)"
    - "ruff check src tests exits 0 (was ~23 errors)"
    - "The 7rj failing-test commit (Commit A) precedes the 7rj fix commit (Commit B) in git log"
  artifacts:
    - path: "tests/test_scan_wrapper_validation.py"
      provides: "Wrapper shape/dtype validation tests for all 4 gru_scan* entry points"
      contains: "pytest.raises"
    - path: "pyproject.toml"
      provides: "mypy third-party overrides + ruff per-file-ignores for the lint cleanup"
  key_links:
    - from: "src/gru_qat/triton_kernels/scan.py gru_scan wrapper"
      to: "ValueError on bad shape"
      via: "if ... raise ValueError mirroring GRULayer.forward guard"
      pattern: "raise (ValueError|RuntimeError)"
---

<objective>
Close `gru-triton-7rj` (assert→ValueError hardening of the 4 `gru_scan*` wrappers) and `gru-triton-4m6` (the ~145-mypy / ~23-ruff lint debt) in a single plan, because all three work items (7rj, mypy, ruff) converge on the 4 `scan*.py` Triton-kernel files and must not be written concurrently (the `gru-triton-u00` parallel-write race lesson).

Purpose: The audit must not report itself as "not green" — mypy/ruff green and hardened internal-API validation are milestone-close hygiene (D-06). The 7rj fix is a genuine FIX-bucket bug (bare `assert` is stripped under `python -O`, turning a malformed shape into a kernel deadlock/OOB instead of a clear error).
Output: Hardened `gru_scan*` wrappers, a new `tests/test_scan_wrapper_validation.py`, mypy 0 / ruff 0, `pyproject.toml` config (mypy overrides + ruff per-file-ignores).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/07-audit-report-findings-handling/07-CONTEXT.md
@.planning/phases/07-audit-report-findings-handling/07-RESEARCH.md

<interfaces>
<!-- The 7rj fix mirrors the Phase 6 GRULayer.forward guard. Convention to copy: -->
From src/gru_qat/gru_layer.py (GRULayer.forward, EDG-04 guard, ~lines 169-176):
  `if T == 0: raise ValueError("...T...")` style — `if <bad condition>: raise ValueError("<field name> ... <constraint>")`.

The 4 wrapper entry points to harden (current behavior: bare `assert is_cuda`, `assert dtype == float32`, shape asserts):
  - src/gru_qat/triton_kernels/scan.py — `gru_scan`, `gru_scan_persistent` (~lines 1624, 1661-1662)
  - src/gru_qat/triton_kernels/scan_diagonal.py — `gru_scan_diagonal` (~line 661)
  - src/gru_qat/triton_kernels/scan_monarch.py — `gru_scan_monarch`
  - src/gru_qat/triton_kernels/scan_butterfly.py — `gru_scan_butterfly_triton`

mypy baseline (measured live 2026-05-15): 145 errors / 10 files. ~124 (86%) in the 4 scan*.py — 3 root patterns:
  untyped @triton.jit/@triton.autotune decorators, missing kernel-fn annotations, torch-structured Module/Tensor union narrowing.
ruff baseline: 23 errors — E402 (9, pytest.importorskip idiom), F841 (8, intentional kernel bias loads), F401 (4), E731 (2).
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: 7rj — wrapper shape/dtype validation hardening (two-commit)</name>
  <read_first>
    - .planning/phases/07-audit-report-findings-handling/07-CONTEXT.md (D-02 FIX bucket, D-37/D-50 two-commit discipline)
    - .planning/phases/07-audit-report-findings-handling/07-RESEARCH.md (triage row 2: 7rj; "Don't Hand-Roll" — mirror GRULayer.forward)
    - src/gru_qat/triton_kernels/scan.py, scan_diagonal.py, scan_monarch.py, scan_butterfly.py (the 4 gru_scan* wrappers)
    - src/gru_qat/gru_layer.py (the EDG-04 GRULayer.forward ValueError guard convention to mirror)
    - bd show gru-triton-7rj (root cause + the exact malformed-shape cases to test)
  </read_first>
  <behavior>
    - Test: gru_scan called with a non-CUDA tensor raises ValueError/RuntimeError naming the offending input (not an AssertionError, not a deadlock)
    - Test: gru_scan called with a non-float32 dtype raises ValueError naming dtype
    - Test: each of the 4 wrappers (gru_scan, gru_scan_diagonal, gru_scan_monarch, gru_scan_butterfly_triton) rejects a malformed shape with a clear ValueError naming the offending dimension
    - Test: the validation still fires under `python -O` semantics (i.e. it is an `if ... raise`, not an `assert`) — verified by asserting the raised type is ValueError/RuntimeError, never AssertionError
  </behavior>
  <action>
    Commit A (failing test FIRST): create `tests/test_scan_wrapper_validation.py` with `pytest.raises(ValueError)` (or RuntimeError) cases for all 4 `gru_scan*` wrappers covering non-CUDA tensor, non-float32 dtype, and a malformed shape. Gate the file with the existing `pytest.importorskip("triton")` + `cuda_only` idiom where a wrapper needs CUDA to reach the guard; where the guard fires before any CUDA op, keep that case CPU-runnable. Commit message: `test(07-01): add failing wrapper-validation tests for gru-triton-7rj`.
    Commit B (fix): in `scan.py`, `scan_diagonal.py`, `scan_monarch.py`, `scan_butterfly.py`, convert every bare `assert` used for shape/dtype validation in the `gru_scan*` wrapper entry points to `if <bad condition>: raise ValueError(...)` (or RuntimeError for state misuse), mirroring the `GRULayer.forward` EDG-04 guard convention — message names the offending field and constraint. Do NOT touch `assert`s inside `@triton.jit` kernel bodies or internal-dispatch-contract asserts (those are programmer-error invariants, per the project error-handling convention). Commit message: `fix(07-01): harden gru_scan wrapper shape validation (gru-triton-7rj)`.
  </action>
  <verify>
    <automated>uv run pytest tests/test_scan_wrapper_validation.py -q</automated>
  </verify>
  <acceptance_criteria>
    - Behavior: `uv run pytest tests/test_scan_wrapper_validation.py -q` exits 0
    - Behavior: every wrapper rejects a malformed shape with ValueError/RuntimeError (never AssertionError)
    - Source: `git log --oneline` shows the `test(07-01)` commit SHA precedes the `fix(07-01)` commit SHA for 7rj
    - Source: `grep -nE 'raise (ValueError|RuntimeError)' src/gru_qat/triton_kernels/scan*.py` shows the new guards in all 4 files
  </acceptance_criteria>
  <done>All 4 gru_scan* wrappers validate shape/dtype via `if ... raise`, the new test file passes, and Commit A precedes Commit B in git log.</done>
</task>

<task type="auto">
  <name>Task 2: 4m6 — mypy 0 + ruff 0 lint cleanup</name>
  <read_first>
    - .planning/phases/07-audit-report-findings-handling/07-CONTEXT.md (D-06 — both src and tests green; do NOT weaken `strict = true`)
    - .planning/phases/07-audit-report-findings-handling/07-RESEARCH.md (the mypy/ruff cleanup section: per-code error tables, Pitfall 3 ordering, Don't-Hand-Roll overrides row)
    - src/gru_qat/triton_kernels/scan.py, scan_diagonal.py, scan_monarch.py, scan_butterfly.py, ste.py, structure.py, gru_layer.py, gru_cell.py (the 10-file mypy surface)
    - pyproject.toml ([tool.mypy], [tool.ruff] current config)
    - bd show gru-triton-4m6 (the tracked debt)
  </read_first>
  <action>
    Close `gru-triton-4m6`. mypy (strict, config-scoped to `src/gru_qat`): clear all ~145 errors. Per RESEARCH.md the bulk is 3 systematic patterns in the 4 `scan*.py` files — add `[[tool.mypy.overrides]] ignore_missing_imports = true` for the `triton` / `torch_structured` modules (their `import-untyped` errors), annotate or `# type: ignore[misc]` the `@triton.jit`/`@triton.autotune` decorated kernels, and narrow the `torch_structured` Module/Tensor unions in `scan_butterfly.py` with `assert isinstance(...)` / `cast()`. Apply per-line fixes for the ~30 `ste.py`/`structure.py`/`gru_layer.py`/`gru_cell.py` one-offs. Do NOT weaken `strict = true` — use scoped overrides for missing third-party stubs only.
    ruff (`ruff check src tests`): clear all ~23 errors. Add `[tool.ruff.lint.per-file-ignores]` for `E402` (the `pytest.importorskip` idiom) and `F841` (intentional kernel bias loads) — these are deliberate patterns the project endorses; delete the 4 genuine `F401` unused imports; convert the 2 `E731` lambda assignments to `def`.
    PITFALL (RESEARCH.md Pitfall 3): fix each underlying typed error BEFORE removing the now-redundant `# type: ignore` (15 `unused-ignore` errors) — wrong order re-introduces real errors. Fix-and-delete the same line atomically.
    Commit when both gates are green: `fix(07-01): clear mypy/ruff debt to 0/0 (gru-triton-4m6)`.
  </action>
  <verify>
    <automated>uv run mypy && uv run ruff check src tests</automated>
  </verify>
  <acceptance_criteria>
    - Behavior: `uv run mypy` exits 0
    - Behavior: `uv run ruff check src tests` exits 0
    - Source: `pyproject.toml` still contains `strict = true` under `[tool.mypy]` (not weakened)
    - Source: `pyproject.toml` contains `[tool.ruff.lint.per-file-ignores]` for E402/F841 and/or `[[tool.mypy.overrides]]` for triton/torch_structured
  </acceptance_criteria>
  <done>`uv run mypy` and `uv run ruff check src tests` both exit 0; `strict = true` preserved; gru-triton-4m6 ready to close in Wave 3.</done>
</task>

</tasks>

<threat_model>
Phase 7 hardens input validation (7rj assert→ValueError) and (in plan 07-02) fixes a silent quantizer-correctness bug (n20); no new attack surface — net risk reduction. The 7rj change strengthens an internal Python API against malformed shapes (defensive coding), not a security control. No authentication, network, or untrusted-input boundary is added or modified.
</threat_model>

<verification>
- `uv run pytest tests/test_scan_wrapper_validation.py -q` exits 0 (CUDA host for CUDA-gated cases; CPU-runnable cases pass everywhere).
- `uv run mypy` exits 0; `uv run ruff check src tests` exits 0.
- `git log --oneline` shows `test(07-01)` 7rj commit precedes `fix(07-01)` 7rj commit.
- `uv run pytest -q -m "not divergence"` still green on the touched src files (no regression introduced by the lint pass).
</verification>

<success_criteria>
- 7rj closed: 4 `gru_scan*` wrappers raise `ValueError`/`RuntimeError` on malformed input; failing test committed before fix.
- 4m6 closed: mypy 0, ruff 0, `strict = true` preserved.
- No concurrent same-file write risk: this is the only Wave-1 plan touching `scan*.py`.
</success_criteria>

<output>
After completion, create `.planning/phases/07-audit-report-findings-handling/07-01-SUMMARY.md`.
</output>
