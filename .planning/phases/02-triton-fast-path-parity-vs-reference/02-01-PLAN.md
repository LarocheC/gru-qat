---
phase: 02-triton-fast-path-parity-vs-reference
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - tests/test_triton_scan_strict.py
autonomous: true
requirements: [TRI-01, TRI-05, TRI-06]

must_haves:
  truths:
    - "tests/test_triton_scan_strict.py exists, imports cleanly, file-skips on CPU-only or no-triton machines"
    - "On CUDA: test_scan_fwd_strict_matches_reference passes < 1e-5 abs across FAST_DENSE_GRID (27 combos)"
    - "On CUDA: test_scan_bwd_strict_matches_reference passes < 1e-5 abs on (x, h0, Wh_cat, bh_cat) gradients across FAST_DENSE_GRID"
    - "On CUDA: _slow siblings (SLOW_DENSE_GRID 18 combos) pass under pytest -m slow"
    - "On CUDA: test_autotune_dWh_dbh_zero_init_across_configs passes at < 1e-5 abs across at least two autotune-bucket-distinct (T,B) shapes (TRI-05)"
    - "On CUDA: test_persistent_kernel_deterministic asserts torch.equal across 50 runs (TRI-06)"
    - "test_no_cv_cache_modifier_live_uses_in_scan_source passes at count == 0 on a CPU-only or CUDA box (static D-25 canary)"
    - "Module-scope torch.set_float32_matmul_precision('highest') is present"
    - "Existing tests/test_triton_scan.py is unmodified by this plan (verifier asserts git diff empty on that file in this commit)"
    - "tests/test_parity.py and tests/test_layer_parity.py are unmodified (D-28 LOCKED)"
  artifacts:
    - path: "tests/test_triton_scan_strict.py"
      provides: "Strict-tier dense Triton parity tests + TRI-05/TRI-06 regression tests + D-25 static .cv canary"
      contains: "test_scan_fwd_strict_matches_reference, test_scan_bwd_strict_matches_reference, test_scan_fwd_strict_matches_reference_slow, test_scan_bwd_strict_matches_reference_slow, test_autotune_dWh_dbh_zero_init_across_configs, test_persistent_kernel_deterministic, test_no_cv_cache_modifier_live_uses_in_scan_source"
  key_links:
    - from: "tests/test_triton_scan_strict.py"
      to: "src/gru_qat/triton_kernels/scan.py (gru_scan, gru_scan_forward, gru_scan_persistent)"
      via: "function call against frozen-Identity GRULayer reference"
      pattern: "gru_scan(gi, h0, Wh_cat, bh_cat).*ref_layer.*assert.*< 1e-5"
    - from: "tests/test_triton_scan_strict.py"
      to: "tests/test_triton_scan.py"
      via: "verbatim duplicate of _ref_layer helper (per D-18)"
      pattern: "def _ref_layer.*QuantizerConfig.bits=32"
---

<objective>
Create `tests/test_triton_scan_strict.py` — the strict-tier (< 1e-5 abs under `torch.set_float32_matmul_precision('highest')`) parity audit for the dense Triton scan kernel — and house the two named regression tests for TRI-05 (autotune dWh/dbh slab-zero, commit c001a8a) and TRI-06 (50-run cross-CTA-fence determinism). Also place the D-25 static canary that asserts `cache_modifier=".cv"` does not appear in any live (non-comment) code line of `src/gru_qat/triton_kernels/scan.py`.

Purpose: TRI-01 (dense Triton fwd+bwd < 1e-5), TRI-05 (autotune regression), TRI-06 (determinism regression) require strict-tier coverage that does NOT exist in `tests/test_triton_scan.py` (which is TF32/realistic-tier). This file is the new audit gate per CONTEXT D-19. The existing TF32 realistic-tier file at `tests/test_triton_scan.py` stays unmodified by this plan per D-20 (tightening lands in plan 02-05).

Output: One new test file with 7 distinct test functions (4 parity + 2 regression + 1 static canary). Skips cleanly on CPU; ready for the Plan 02-06 phase-exit GPU run.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/02-triton-fast-path-parity-vs-reference/02-CONTEXT.md
@.planning/phases/02-triton-fast-path-parity-vs-reference/02-PATTERNS.md
@.planning/phases/01-reference-path-parity-vs-nn-gru/01-CONTEXT.md
@.planning/codebase/TESTING.md
@.planning/codebase/CONVENTIONS.md
@.planning/codebase/ARCHITECTURE.md
@CLAUDE.md
@DEVELOPMENT.md
@tests/test_triton_scan.py
@tests/test_layer_parity.py
@src/gru_qat/triton_kernels/scan.py

<interfaces>
<!-- Key entry points used by this plan. Executor uses these directly. -->

From `src/gru_qat/triton_kernels/scan.py`:
- `gru_scan(gi, h0, Wh_cat, bh_cat) -> torch.Tensor` — autograd path (autotuned fwd + bwd)
- `gru_scan_forward(gi, h0, Wh_cat, bh_cat) -> torch.Tensor` — forward-only (no autograd save)
- `gru_scan_persistent(gi, h0, Wh_cat, bh_cat) -> torch.Tensor` — persistent forward (cross-CTA barrier path)
- `gru_scan_forward_persistent`, `gru_scan_backward_persistent`, `_gru_scan_backward_pytorch` — already imported by `tests/test_triton_scan.py`
- Autotune key declared at `src/gru_qat/triton_kernels/scan.py:732, :893` is `key=['T', 'B']`

From `tests/test_triton_scan.py`:
- `_ref_layer(in_dim: int, hidden: int) -> GRULayer` at lines 30-44 — fp32-Identity GRULayer with `gate_layout="fused"` and `pre_batch_input=True`. **Duplicate this helper verbatim** in the new file per D-18 (< 30 LOC).
- `cuda_only = pytest.mark.skipif(...)` at lines 25-27 — duplicate the same shape.
- Existing analog regression test at `tests/test_triton_scan.py:202-215` (`test_triton_backward_matches_pytorch` with slab-zero documentation) — single-config; TRI-05 is multi-config rotation.

From `tests/test_layer_parity.py` (Phase 1 output):
- Module-level `torch.set_float32_matmul_precision("highest")` preamble pattern — copy the shape (line 33 in the reference file).

Established conventions (CONVENTIONS.md, TESTING.md):
- `pytest.importorskip("triton")` at module top, followed by `# noqa: E402` on subsequent imports
- `cuda_only` defined per-file (not in conftest)
- `torch.manual_seed(0)` at top of every test body
- `from __future__ import annotations` first line after the docstring
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Strict-tier parity tests + module preamble + helpers</name>
  <files>tests/test_triton_scan_strict.py</files>
  <read_first>
    - tests/test_triton_scan.py (lines 1-44 for preamble + _ref_layer; lines 115-139 for fwd body shape; lines 144-215 for bwd body shape)
    - tests/test_layer_parity.py (line 33 for the 'highest' module-scope preamble)
    - .planning/phases/02-triton-fast-path-parity-vs-reference/02-PATTERNS.md (sections: "tests/test_triton_scan_strict.py", "Imports + module preamble", "Core strict-tier fwd test pattern", "Core strict-tier bwd test pattern")
    - .planning/phases/02-triton-fast-path-parity-vs-reference/02-CONTEXT.md (D-13, D-16 dense grid, D-19, D-21)
    - .planning/codebase/TESTING.md (sections "Common Patterns to Match When Adding Tests", "How CUDA / Triton Availability Is Gated")
    - .planning/codebase/CONVENTIONS.md (Import Organization, Type Annotations, Naming Patterns)
  </read_first>
  <behavior>
    - Module imports successfully on CPU-only and skips at collection time when triton is not installed
    - When CUDA is available, `test_scan_fwd_strict_matches_reference[1-1-32]` (and every other FAST_DENSE_GRID combo) passes: `(ref_out - triton_out).abs().max() < 1e-5`
    - When CUDA is available, `test_scan_bwd_strict_matches_reference[*]` passes the four-tensor gradient comparison loop (x, h0, Wh_cat, bh_cat) at `< 1e-5` absolute
    - `_slow` siblings parametrized over SLOW_DENSE_GRID with `@pytest.mark.slow` are collected but deselected under `pytest -m "not slow"`
    - Module docstring documents the strict-vs-realistic tier divergence and the absolute-error idiom
  </behavior>
  <action>
    Create `tests/test_triton_scan_strict.py` with the structure described in PATTERNS.md "tests/test_triton_scan_strict.py" section. Concrete components:

    1. **Module docstring**: explain strict tier (`'highest'`, < 1e-5 abs), divergence from realistic-tier sibling `tests/test_triton_scan.py`, and that this file also hosts TRI-05, TRI-06, and the D-25 static canary. Mention `tests/test_parity.py` and `tests/test_layer_parity.py` are LOCKED per D-28 and not duplicated here.

    2. **Imports**: `from __future__ import annotations`, then stdlib (`pathlib` — needed by the canary in Task 3, declare here so all tasks reuse), then `pytest`, `torch`, `torch.nn as nn`, then `triton = pytest.importorskip("triton")`, then the `gru_qat.*` imports from `tests/test_triton_scan.py:14-23` (mirror that exact import list — `GRULayer`, `QuantizerConfig`, `QuantRecipe`, and from `gru_qat.triton_kernels.scan`: `gru_scan`, `gru_scan_forward`, `gru_scan_forward_persistent`, `gru_scan_backward_persistent`, `gru_scan_persistent`, `_gru_scan_backward_pytorch`). Apply `# noqa: E402` to every import after `pytest.importorskip`.

    3. **Module-scope `torch.set_float32_matmul_precision("highest")`** with a 3-line comment block explaining this is the strict-tier marker (cite `tests/test_triton_scan.py` as the realistic-tier sibling).

    4. **`cuda_only`** definition — duplicate the shape of `tests/test_triton_scan.py:25-27` verbatim.

    5. **`_ref_layer(in_dim: int, hidden: int) -> GRULayer`** — verbatim copy of `tests/test_triton_scan.py:30-44`. Add a comment "duplicated per D-18 (< 30 LOC, inline beats shared module)" above the helper.

    6. **Shape grids**: define module-level `FAST_DENSE_GRID = [(T, B, H) for T in (1, 8, 64) for B in (1, 4, 32) for H in (32, 128, 512)]` (27 cases) and `SLOW_DENSE_GRID` with T in (512, 1024) (18 cases). Cite D-16.

    7. **`test_scan_fwd_strict_matches_reference(T: int, B: int, H: int) -> None`** — `@cuda_only` + `@pytest.mark.parametrize("T,B,H", FAST_DENSE_GRID)`. Body per PATTERNS.md "Core strict-tier fwd test pattern": seed 0, build `_ref_layer(IN=H, H).to(device).eval()`, build random `x` and `h0` with `torch.randn`, run reference under `torch.no_grad()` to get `ref_out`, extract `w = layer.cell.quantize_weights()` and `gi = layer.cell.input_projection(x, w)`, assert `w.Wh_cat is not None and w.bh_cat is not None`, call `gru_scan_forward(gi, h0, w.Wh_cat, w.bh_cat)`, compare `(ref_out - triton_out).abs().max().item() < 1e-5`. Assertion message must include `T={T},B={B},H={H}` and the diff value formatted `.4e`.

    8. **`test_scan_fwd_strict_matches_reference_slow`** — identical body, `@pytest.mark.slow` + `@pytest.mark.parametrize("T,B,H", SLOW_DENSE_GRID)`.

    9. **`test_scan_bwd_strict_matches_reference(T: int, B: int, H: int) -> None`** — `@cuda_only` + `@pytest.mark.parametrize("T,B,H", FAST_DENSE_GRID)`. Body per PATTERNS.md "Core strict-tier bwd test pattern": build reference layer, build `x` and `h0` with `requires_grad=True`, clone for both paths so gradients are independent, run reference `ref_loss = ref_out.float().pow(2).sum(); ref_loss.backward()`, extract `Wi_cat`, `bi_cat`, `Wh_cat.requires_grad_()`, `bh_cat.requires_grad_()` from `ref_layer.cell.quantize_weights()`, compute `gi = torch.nn.functional.linear(tri_x, Wi_cat, bi_cat)`, run `out = gru_scan(gi, tri_h0, Wh_cat, bh_cat); out.float().pow(2).sum().backward()`. Build `ref_dWh_cat = torch.cat([ref_layer.cell.W_hr.grad, ref_layer.cell.W_hz.grad, ref_layer.cell.W_hn.grad], dim=0)` and `ref_dbh_cat = torch.cat([ref_layer.cell.b_hr.grad, ref_layer.cell.b_hz.grad, ref_layer.cell.b_hn.grad])`. Loop over the four `(name, ref_g, tri_g)` pairs `("x", ref_x.grad, tri_x.grad)`, `("h0", ref_h0.grad, tri_h0.grad)`, `("Wh_cat", ref_dWh_cat, Wh_cat.grad)`, `("bh_cat", ref_dbh_cat, bh_cat.grad)`. For each: `assert (ref_g - tri_g).abs().max().item() < 1e-5` with a message including `{name}`, `T,B,H`, and the diff `.4e`.

    10. **`test_scan_bwd_strict_matches_reference_slow`** — same body, `@pytest.mark.slow` + SLOW_DENSE_GRID.

    Naming follows D-21. Each test body starts with `torch.manual_seed(0)`. Use absolute (`abs().max()`), NOT the relative-error idiom — strict tier diverges from the realistic-tier pattern per CONTEXT "Established Patterns" callout. No fenced code blocks in the file body except inside docstrings; production-quality Python following CONVENTIONS.md.
  </action>
  <verify>
    <automated>cd /home/claroche/gru-triton &amp;&amp; uv run python -c "import ast; ast.parse(open('tests/test_triton_scan_strict.py').read())" &amp;&amp; uv run pytest tests/test_triton_scan_strict.py --collect-only -q 2>&amp;1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - `python -c "import ast; ast.parse(...)"` exits 0 (syntactic validity)
    - `pytest tests/test_triton_scan_strict.py --collect-only -q` lists at least 4 parametrized parity test functions (`test_scan_fwd_strict_matches_reference`, `_slow`, `test_scan_bwd_strict_matches_reference`, `_slow`) with combined parametrization count >= 27 + 18 + 27 + 18 = 90 test ids
    - `pytest tests/test_triton_scan_strict.py -q` runs cleanly on CPU (every test collected and SKIPPED via `@cuda_only`; exit code 0; `-ra` shows skip reasons)
    - `grep -c "torch.set_float32_matmul_precision(\"highest\")" tests/test_triton_scan_strict.py` returns 1 (module-scope only)
    - `grep -c "abs().max()" tests/test_triton_scan_strict.py` returns at least 4 (one fwd + four bwd-tensor assertions; absolute-error idiom)
    - `grep -v '^\s*#' tests/test_triton_scan_strict.py | grep -c "torch.set_float32_matmul_precision(\"high\")"` returns 0 (no realistic-tier override leaked in)
    - `ruff check tests/test_triton_scan_strict.py` exits 0
  </acceptance_criteria>
  <done>
    File `tests/test_triton_scan_strict.py` exists, syntactically valid, lint-clean, collects on CPU with all CUDA tests skipped, contains the four parametrized parity tests with `< 1e-5` absolute-error assertions, FAST/SLOW grid split per D-16, helpers duplicated verbatim per D-18.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: TRI-05 and TRI-06 regression tests appended to the strict file</name>
  <files>tests/test_triton_scan_strict.py</files>
  <read_first>
    - tests/test_triton_scan_strict.py (the file just created in Task 1)
    - tests/test_triton_scan.py:202-215 (single-config slab-zero regression — analog for TRI-05)
    - src/gru_qat/triton_kernels/scan.py:732 and :893 (autotune key=['T','B'] declarations)
    - src/gru_qat/triton_kernels/scan.py:184-208 (persistent fwd release/acquire barrier comment block — D-24 rationale)
    - DEVELOPMENT.md (sections on .cv anti-pattern, ~lines 131-143)
    - .planning/phases/02-triton-fast-path-parity-vs-reference/02-PATTERNS.md (sections "TRI-05 regression", "TRI-06 regression")
    - .planning/phases/02-triton-fast-path-parity-vs-reference/02-CONTEXT.md (D-23, D-24)
  </read_first>
  <behavior>
    - On CUDA: `test_autotune_dWh_dbh_zero_init_across_configs` builds a reference layer at two (T, B) shape buckets that the autotune key=['T','B'] discriminates, runs both through `gru_scan` in one process, and asserts the second run's `(dx, dh0, dWh_cat, dbh_cat)` match reference at `< 1e-5` absolute. Pre-c001a8a code would fail on the second iteration's `dWh_cat` / `dbh_cat`.
    - On CUDA: `test_persistent_kernel_deterministic` runs `gru_scan_persistent` 50 times on bit-identical inputs and asserts `torch.equal(out_0, out_i)` for all i in 1..49. Pre-fix code would diverge on at least one run.
    - Both tests use the strict-tier `'highest'` module-scope precision set by Task 1.
    - Both tests skip on CPU via `@cuda_only`.
  </behavior>
  <action>
    Append two test functions to `tests/test_triton_scan_strict.py` (created in Task 1). Concrete details:

    1. **`test_autotune_dWh_dbh_zero_init_across_configs() -> None`** — `@cuda_only`. Multi-line docstring per PATTERNS.md skeleton citing commit `c001a8a`, the existing single-config regression at `tests/test_triton_scan.py:202-215`, and the autotune `key=['T','B']` at `src/gru_qat/triton_kernels/scan.py:732,893`.

    Body:
    - `torch.manual_seed(0); device = torch.device("cuda")`
    - Define two shape tuples that hit different autotune buckets per `key=['T','B']`: `shapes = [(16, 16, 64), (32, 32, 64)]`. The shapes must be different in BOTH T and B so the autotune key generates distinct buckets.
    - For each `(T, B, H)`:
      - Build `ref_layer = _ref_layer(H, H).to(device)` (fresh per iteration so reference gradients are independent)
      - Build random `x` with `requires_grad=True` and random `h0` with `requires_grad=True` (clone shapes per iteration; use `torch.manual_seed(idx)` inside the loop to keep iterations independent but reproducible)
      - Run reference: `ref_out, _ = ref_layer(ref_x, ref_h0); ref_out.float().pow(2).sum().backward()`
      - Extract `Wh_cat`, `bh_cat`, `Wi_cat`, `bi_cat` per the same shape as `test_scan_bwd_strict_matches_reference` body
      - Build `gi`, run `gru_scan`, backprop
      - Build `ref_dWh_cat`, `ref_dbh_cat` via the same `torch.cat` of per-gate grads
      - Loop the four-tensor comparison (`x`, `h0`, `Wh_cat`, `bh_cat`); each must satisfy `< 1e-5` absolute. Assertion message must include the shape index (`f"iter={idx} shape={(T,B,H)} {name} ..."`) so the second-iteration failure (the stale-slab bug) is unambiguous in pytest output.

    2. **`test_persistent_kernel_deterministic() -> None`** — `@cuda_only`. Docstring per PATTERNS.md citing commit `0e26193` (mentioned in REQUIREMENTS.md TRI-06), `src/gru_qat/triton_kernels/scan.py:184-208` (the release/acquire comment block), and `DEVELOPMENT.md:131-143`.

    Body:
    - `torch.manual_seed(0); device = torch.device("cuda")`
    - `T, B, H = 64, 16, 128` (per PATTERNS.md skeleton — fast on a modern GPU)
    - Build `gi = torch.randn(T, B, 3 * H, device=device).contiguous()`
    - Build `h0 = torch.randn(B, H, device=device).contiguous()`
    - Build `Wh = (torch.randn(3 * H, H, device=device) * 0.1).contiguous()`
    - Build `bh = (torch.randn(3 * H, device=device) * 0.1).contiguous()`
    - `out0 = gru_scan_persistent(gi, h0, Wh, bh)`
    - `for i in range(1, 50): out_i = gru_scan_persistent(gi, h0, Wh, bh); assert torch.equal(out0, out_i), f"persistent run {i} diverged: max abs diff {(out0 - out_i).abs().max().item():.4e}"`
    - Use `torch.equal` (strict bit-equality) NOT `torch.allclose` — D-24 says determinism is bit-identical even if TF32 noise is present, because reduction order is fixed per kernel.
    - Inputs are allocated ONCE before the loop and NOT re-randomized; CTA scheduling is the only varying factor.

    Both tests are appended to the END of the strict file (after the four parity tests from Task 1). Use module-level helpers / imports declared in Task 1; do not re-declare anything.
  </action>
  <verify>
    <automated>cd /home/claroche/gru-triton &amp;&amp; uv run pytest tests/test_triton_scan_strict.py --collect-only -q 2>&amp;1 | grep -E "test_autotune_dWh_dbh_zero_init_across_configs|test_persistent_kernel_deterministic" | wc -l</automated>
  </verify>
  <acceptance_criteria>
    - `pytest --collect-only -q tests/test_triton_scan_strict.py` lists both `test_autotune_dWh_dbh_zero_init_across_configs` and `test_persistent_kernel_deterministic` (exactly 2 matches from the grep)
    - `pytest tests/test_triton_scan_strict.py -q` exits 0 on CPU with both regression tests SKIPPED (`-ra` summary lists the skip reasons)
    - `grep -c "torch.equal(out0, out_i)" tests/test_triton_scan_strict.py` returns 1 (the determinism assertion uses bit-equality, not allclose)
    - `grep -c "for i in range(1, 50)" tests/test_triton_scan_strict.py` returns 1 (50-run loop)
    - `grep -c "key=\['T', 'B'\]\|key=\['T','B'\]" tests/test_triton_scan_strict.py` returns at least 1 (TRI-05 docstring references the autotune key)
    - `ruff check tests/test_triton_scan_strict.py` exits 0
  </acceptance_criteria>
  <done>
    Two regression tests appended; both collected; both skip on CPU; bit-equality assertion uses `torch.equal`; TRI-05 docstring cites c001a8a + autotune `key=['T','B']`; TRI-06 docstring cites the release/acquire pattern.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: D-25 static .cv canary baselined at 0 live uses</name>
  <files>tests/test_triton_scan_strict.py</files>
  <read_first>
    - tests/test_triton_scan_strict.py (file from Tasks 1 + 2)
    - src/gru_qat/triton_kernels/scan.py (lines 184-208, 425-440, 620-635 — verify all three `cache_modifier=".cv"` occurrences are inside `# ` comment lines)
    - DEVELOPMENT.md ("What the agent should NOT do" section)
    - .planning/phases/02-triton-fast-path-parity-vs-reference/02-PATTERNS.md (section "D-25 static `.cv` grep canary" — note the ambiguity it flagged)
    - .planning/phases/02-triton-fast-path-parity-vs-reference/02-CONTEXT.md (D-25)
  </read_first>
  <behavior>
    - Test reads `src/gru_qat/triton_kernels/scan*.py` (the four files `scan.py`, `scan_diagonal.py`, `scan_monarch.py`, `scan_butterfly.py`) line by line, strips comment lines (any line whose lstrip starts with `#`), and counts `cache_modifier=".cv"` substring matches in the remainder.
    - The asserted baseline is `count == 0` — at the time this plan executes, all three matches in `scan.py` (lines 192, 431, 625) are inside `#`-comment lines, and the other three scan_*.py files have zero matches. **Verified at plan-write time.** If any live (non-comment) `.cv` use is reintroduced in any scan*.py file, the test fails with a message listing the offending file+line.
    - Test runs on CPU (no CUDA needed, no `@cuda_only` decorator). Pure file-I/O via `pathlib`. Should complete in < 100 ms.
  </behavior>
  <action>
    Append a third test function `test_no_cv_cache_modifier_live_uses_in_scan_source()` to `tests/test_triton_scan_strict.py`. Concrete shape:

    1. **Docstring** — multi-paragraph, cites D-25, `DEVELOPMENT.md` "What the agent should NOT do", and the source comment block at `src/gru_qat/triton_kernels/scan.py:184-208`. Explicitly notes: "The three current occurrences at scan.py:192, 431, 625 are ALL inside `#`-comment lines (documenting why the pattern is forbidden); the live-code baseline is 0. If a future commit reintroduces `cache_modifier=\".cv\"` outside a comment, this canary fails with the file path + line number."

    2. **Body** (pure-Python, pathlib, no subprocess per CONVENTIONS.md):
    - `import pathlib` (already done at top via Task 1)
    - `src_dir = pathlib.Path(__file__).resolve().parent.parent / "src" / "gru_qat" / "triton_kernels"`
    - `assert src_dir.is_dir(), f"expected {src_dir} to exist"`
    - `forbidden = 'cache_modifier=".cv"'`
    - `live_hits: list[tuple[str, int, str]] = []`  (file, line_number, line_text)
    - `for path in sorted(src_dir.glob("scan*.py")): for line_no, raw in enumerate(path.read_text().splitlines(), start=1): stripped = raw.lstrip(); if stripped.startswith("#"): continue; if forbidden in stripped: live_hits.append((path.name, line_no, raw.rstrip()))`
    - `assert live_hits == [], f"Live (non-comment) cache_modifier=\".cv\" uses found in scan*.py: {live_hits}. See DEVELOPMENT.md anti-pattern note + tests/test_triton_scan_strict.py::test_persistent_kernel_deterministic for the dynamic guard."`

    3. The line-stripping rule MUST be `raw.lstrip().startswith("#")` — this correctly classifies indented Triton-jit comment lines (which start with whitespace then `#`). Do NOT use `raw.startswith("#")` (would miss indented comments and re-introduce false positives). Do NOT use `grep -c` semantics (which would miscount per the "Grep gate hygiene" rule the planner is following).

    No `@cuda_only` — pure-Python file inspection. No `torch.manual_seed`. Fast.
  </action>
  <verify>
    <automated>cd /home/claroche/gru-triton &amp;&amp; uv run pytest tests/test_triton_scan_strict.py::test_no_cv_cache_modifier_live_uses_in_scan_source -v 2>&amp;1 | tail -10</automated>
  </verify>
  <acceptance_criteria>
    - `pytest tests/test_triton_scan_strict.py::test_no_cv_cache_modifier_live_uses_in_scan_source -v` exits 0 (test PASSES at the live baseline of 0)
    - `grep -c "lstrip().startswith" tests/test_triton_scan_strict.py` returns at least 1 (comment-stripping uses lstrip, not raw startswith)
    - `grep -c "scan\*.py\|scan\\*\\.py\|glob(\"scan\\*.py\")" tests/test_triton_scan_strict.py` returns at least 1 (test scans all four scan*.py files, not only scan.py — per D-25 wording that says "scan*.py")
    - `grep -c "subprocess" tests/test_triton_scan_strict.py` returns 0 (no shell-out per CONVENTIONS.md)
    - Confirm at execute-time: `grep -v '^\s*#' src/gru_qat/triton_kernels/scan.py | grep -c 'cache_modifier=".cv"'` returns 0 — if non-zero, the planner's baseline assumption was wrong and the executor must escalate before merging.
    - `ruff check tests/test_triton_scan_strict.py` exits 0
  </acceptance_criteria>
  <done>
    Static canary appended; passes at the verified live baseline of 0; scans all `scan*.py` files; comment-strip via `lstrip().startswith("#")`; no subprocess; explicit error message lists offending file+line if regression occurs.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| test ↔ Triton kernel | Test feeds untrusted-from-the-test's-perspective (random) inputs to the kernel; kernel must produce numerically-correct output |
| test ↔ source-file filesystem read | D-25 canary reads `src/gru_qat/triton_kernels/scan*.py` via pathlib; trust boundary is "does the file exist on disk" |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-01 | Tampering | `cache_modifier=".cv"` reintroduction in scan*.py | mitigate | D-25 static canary (Task 3) asserts count == 0 in live (non-comment) code; CI runs it on every commit |
| T-02-02 | Information disclosure | Test reads source files via pathlib | accept | Tests already have full repo read access; no PII; no secrets in src/ |
| T-02-03 | Denial of service | 50-run determinism test (Task 2) on slow GPU | accept | T=64,B=16,H=128 — < 30s on a modern GPU per D-24; doesn't block CI |
| T-02-04 | Repudiation | Test claims kernel matches reference but uses wrong reference | mitigate | `_ref_layer` is the Phase-1-validated Identity-quantizer dense layer (LOCKED via D-28); duplicated verbatim from `tests/test_triton_scan.py` |
| T-02-05 | Elevation of privilege | n/a | accept | No privilege boundary in unit tests |
</threat_model>

<verification>
After all three tasks land, run:
- `uv run pytest tests/test_triton_scan_strict.py --collect-only -q` — must list ≥ 7 distinct test functions (4 parity + 2 regression + 1 canary), with parity tests showing ≥ 90 total parametrized ids
- `uv run pytest tests/test_triton_scan_strict.py -q -m "not slow"` — on CPU: every CUDA test skipped, static canary PASSES, exit 0
- `uv run ruff check tests/test_triton_scan_strict.py` — exit 0
- `git diff HEAD~3 -- tests/test_parity.py tests/test_layer_parity.py tests/test_triton_scan.py` — empty (D-28 LOCKED + D-20 isolation)
- `grep -v '^\s*#' src/gru_qat/triton_kernels/scan.py | grep -c 'cache_modifier=".cv"'` — must return 0 (canary baseline still holds)
</verification>

<success_criteria>
- File exists at `tests/test_triton_scan_strict.py`
- ≥ 6 test functions defined (4 parity + 2 regression + 1 canary)
- Module-scope `torch.set_float32_matmul_precision("highest")`
- Absolute-error assertions throughout (no relative-error idiom)
- `_ref_layer` duplicated verbatim from `tests/test_triton_scan.py:30-44`
- FAST_DENSE_GRID = 27 cases, SLOW_DENSE_GRID = 18 cases per D-16
- Slow tests gated behind `@pytest.mark.slow`
- All Triton tests gated behind `@cuda_only`
- D-25 canary uses `lstrip().startswith("#")` comment-strip and scans `scan*.py`
- D-25 canary PASSES on CPU at baseline 0
- pre-existing files `tests/test_parity.py`, `tests/test_layer_parity.py`, `tests/test_triton_scan.py` are untouched
</success_criteria>

<output>
After completion, create `.planning/phases/02-triton-fast-path-parity-vs-reference/02-01-SUMMARY.md` covering:
- Files created (`tests/test_triton_scan_strict.py`) and final LOC count
- Test function inventory (names + decorators + parametrize cardinality)
- Confirmation that CPU collection passes
- D-25 canary baseline verification (`grep -v '^\s*#' .../scan.py | grep -c '.cv'` output)
- Any deviations from the plan and rationale
</output>
