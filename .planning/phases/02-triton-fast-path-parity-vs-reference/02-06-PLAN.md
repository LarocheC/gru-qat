---
phase: 02-triton-fast-path-parity-vs-reference
plan: 06
type: execute
wave: 2
depends_on: [01, 02, 03, 04, 05]
files_modified:
  - .planning/phases/02-triton-fast-path-parity-vs-reference/02-FINDINGS.md
autonomous: false
requirements: [TRI-01, TRI-02, TRI-03, TRI-04, TRI-05, TRI-06]

must_haves:
  truths:
    - "User runs the full Phase 2 test suite on a CUDA box and reports results (checkpoint:human-verify)"
    - "Every failure observed in the GPU run has a corresponding bd issue created via bd create per D-11/D-27"
    - "Every finding follows the two-commit failing-test-before-fix discipline (Commit A: new failing test isolating the bug, Commit B: src/ fix; per D-27)"
    - "No @pytest.mark.xfail introduced anywhere (D-27)"
    - "On phase-exit: pytest tests/test_triton_*_strict.py -q passes ALL strict-tier parity tests at < 1e-5 abs on CUDA"
    - "Existing butterfly OOB regression at tests/test_butterfly_dispatch.py:164 still passes (D-22)"
    - "TRI-05 autotune dWh/dbh regression test passes (TRI-05)"
    - "TRI-06 50-run determinism test passes with torch.equal across all 50 runs (TRI-06)"
    - "Plan 02-05 realistic-tier tightenings are validated under CUDA (re-run if deferred during CPU-only authoring)"
    - "Locked file gates hold: git diff over tests/test_parity.py + tests/test_layer_parity.py across all Phase 2 commits is empty (D-28)"
    - "D-25 static .cv canary still passes (count == 0 live uses in scan*.py)"
    - "Phase-exit SUMMARY at 02-SUMMARY.md exists, documents pass/fail per requirement TRI-01..06, and lists all bd issue IDs created during this phase"
  artifacts:
    - path: ".planning/phases/02-triton-fast-path-parity-vs-reference/02-FINDINGS.md"
      provides: "Per-finding record: which test failed, bd issue ID, root cause hypothesis, fix commit hash, regression-test path"
      contains: "Markdown table with one row per finding; empty (with a 'no findings' note) if GPU run is fully green"
  key_links:
    - from: ".planning/phases/02-triton-fast-path-parity-vs-reference/02-FINDINGS.md"
      to: "bd issues (external)"
      via: "bd issue ID per finding (looked up via bd show <id>)"
      pattern: "bd-[a-z0-9]+ — Commit A: <hash> — Commit B: <hash>"
---

<objective>
Phase-exit audit kickoff. The four strict-tier test files (Plans 02-01..04) plus the realistic-tier tightenings (Plan 02-05) are authored against the planner's understanding but NOT yet validated on actual hardware. This plan drives the CUDA-box validation:

1. **Checkpoint:human-verify** task where the user runs the full Phase 2 test suite on a GPU machine
2. **Findings triage** task where Claude reads the GPU-run output, files `bd create` issues for any failures, and drives the Commit A (failing test isolating the bug) → bd issue → Commit B (src/ fix) two-commit discipline per D-27
3. **Plan 02-05 CUDA verification** — if the realistic-tier tightenings were deferred (CPU-only authoring), they're re-run and either committed or reverted per D-20 here
4. **Phase-exit SUMMARY** writing — `.planning/phases/02-triton-fast-path-parity-vs-reference/02-SUMMARY.md`

Same pattern as Phase 1's `01-05-PLAN.md` (the audit-kickoff plan per CONTEXT D-26).

Purpose: TRI-01..06 phase verification, audit findings triage, fix-in-milestone discipline per D-27.

Output: Phase-exit SUMMARY + bd issues + (if any findings) fix commits + 02-FINDINGS.md.

**This plan is NOT autonomous** — it pauses at the GPU checkpoint.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/02-triton-fast-path-parity-vs-reference/02-CONTEXT.md
@.planning/phases/02-triton-fast-path-parity-vs-reference/02-PATTERNS.md
@.planning/phases/02-triton-fast-path-parity-vs-reference/02-01-PLAN.md
@.planning/phases/02-triton-fast-path-parity-vs-reference/02-02-PLAN.md
@.planning/phases/02-triton-fast-path-parity-vs-reference/02-03-PLAN.md
@.planning/phases/02-triton-fast-path-parity-vs-reference/02-04-PLAN.md
@.planning/phases/02-triton-fast-path-parity-vs-reference/02-05-PLAN.md
@.planning/phases/01-reference-path-parity-vs-nn-gru/01-05-PLAN.md
@CLAUDE.md

<interfaces>
<!-- The Phase 2 test surface that the GPU run must exercise -->

Strict-tier files (Plans 02-01..04):
- `tests/test_triton_scan_strict.py` (dense; 4 parity tests + 2 regression tests + 1 static canary)
- `tests/test_triton_diagonal_strict.py` (diagonal; 4 parity tests)
- `tests/test_triton_monarch_strict.py` (monarch; 4 parity tests)
- `tests/test_triton_butterfly_strict.py` (butterfly; 4 parity tests)

Realistic-tier (Plan 02-05):
- `tests/test_triton_diagonal.py` (modified)
- `tests/test_triton_monarch.py` (modified)

Locked files (must remain unchanged across Phase 2 per D-28):
- `tests/test_parity.py`
- `tests/test_layer_parity.py`

OOB regression (per D-22, MUST still pass):
- `tests/test_butterfly_dispatch.py::test_butterfly_triton_forward_scratch_oob_regression` at line 164

`bd` workflow per CLAUDE.md:
- `bd ready` — list available work
- `bd create <title>` — file new issue
- `bd update <id> --claim` / `bd update <id> --notes "..."` — annotate
- `bd close <id>` — mark complete
- Do NOT use TodoWrite; do NOT use MEMORY.md
</interfaces>
</context>

<tasks>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 1: User runs Phase 2 test suite on a CUDA box</name>
  <what-built>
    Five plans (02-01..05) have authored:
    - 4 new strict-tier test files (`tests/test_triton_*_strict.py`) with < 1e-5 absolute parity assertions
    - 1 TRI-05 autotune regression test in `test_triton_scan_strict.py`
    - 1 TRI-06 50-run determinism regression test in `test_triton_scan_strict.py`
    - 1 D-25 static `.cv` canary in `test_triton_scan_strict.py` (verified at baseline 0 in CPU collection)
    - Realistic-tier tolerance tightenings in `tests/test_triton_diagonal.py` and `tests/test_triton_monarch.py` (may be deferred to this plan if no CUDA was available during authoring)

    On CPU these all collect and skip cleanly. On CUDA they must execute and pass.
  </what-built>
  <how-to-verify>
    On a CUDA box (the gru-triton repo has `uv` installed; CUDA + Triton + torch_structured present), run the following commands and capture the full output. Paste back into chat:

    **1. Strict-tier suite (fast — should be < 5 min on a modern GPU)**:
    ```bash
    cd /home/claroche/gru-triton
    uv run pytest tests/test_triton_scan_strict.py tests/test_triton_diagonal_strict.py tests/test_triton_monarch_strict.py tests/test_triton_butterfly_strict.py -v -m "not slow" 2>&amp;1 | tail -60
    ```
    Expected: all tests PASS. If any FAIL, copy the failing assertion message verbatim — it will include the (T, B, H[, nblocks]) shape, the max abs diff, and the test name.

    **2. Strict-tier slow suite (T ∈ {512, 1024} — may take 5–15 min)**:
    ```bash
    uv run pytest tests/test_triton_scan_strict.py tests/test_triton_diagonal_strict.py tests/test_triton_monarch_strict.py tests/test_triton_butterfly_strict.py -v -m slow 2>&amp;1 | tail -40
    ```
    Expected: all tests PASS.

    **3. TRI-05, TRI-06, D-25 canary** (already covered in step 1 but called out for clarity):
    ```bash
    uv run pytest tests/test_triton_scan_strict.py::test_autotune_dWh_dbh_zero_init_across_configs tests/test_triton_scan_strict.py::test_persistent_kernel_deterministic tests/test_triton_scan_strict.py::test_no_cv_cache_modifier_live_uses_in_scan_source -v 2>&amp;1 | tail -15
    ```
    Expected: 3 passed. The determinism test should report no output divergence across 50 runs.

    **4. Butterfly OOB regression (D-22 — must still pass)**:
    ```bash
    uv run pytest tests/test_butterfly_dispatch.py::test_butterfly_triton_forward_scratch_oob_regression -v 2>&amp;1 | tail -10
    ```
    Expected: 1 passed.

    **5. Realistic-tier tightenings (Plan 02-05; this is where deferred-CPU authoring gets validated)**:
    ```bash
    uv run pytest tests/test_triton_diagonal.py tests/test_triton_monarch.py -v -m "not slow" 2>&amp;1 | tail -30
    ```
    Expected: all PASS. If any tightened tolerance fails, Claude reverts THAT constant only per D-20 (Plan 02-05 revert protocol).

    **6. Full Phase 2 hygiene (locked files unchanged)**:
    ```bash
    git diff $(git merge-base HEAD claude/unzip-and-prepare-repo-7JFrN 2>/dev/null || git rev-list --max-parents=0 HEAD | tail -1)..HEAD -- tests/test_parity.py tests/test_layer_parity.py
    ```
    Expected: empty output (D-28 LOCKED).

    **7. D-25 canary against live source**:
    ```bash
    grep -v '^\s*#' src/gru_qat/triton_kernels/scan.py | grep -c 'cache_modifier=".cv"' || echo "0"
    ```
    Expected: 0 (no live uses of `.cv` cache modifier).

    Paste the output of all 7 commands. If anything fails, also paste the full pytest stack trace (use `-v --tb=short`).
  </how-to-verify>
  <resume-signal>
    Reply with the output of commands 1-7. Use:
    - `approved` (all green; proceed to Task 2 with no findings to file)
    - `findings: <description>` (one or more tests failed; Claude triages in Task 2)
    - `defer: <reason>` (no CUDA box accessible right now; pause until later)
  </resume-signal>
</task>

<task type="auto">
  <name>Task 2: Findings triage — bd issues + Commit A → Commit B per finding</name>
  <files>.planning/phases/02-triton-fast-path-parity-vs-reference/02-FINDINGS.md</files>
  <read_first>
    - GPU run output from Task 1 (provided in user's resume signal)
    - .planning/phases/02-triton-fast-path-parity-vs-reference/02-CONTEXT.md (D-14, D-27)
    - .planning/phases/01-reference-path-parity-vs-nn-gru/01-CONTEXT.md (D-10..12 — Phase 1's two-commit discipline, carried forward as D-27)
    - CLAUDE.md (bd workflow section)
  </read_first>
  <behavior>
    - If Task 1 resume signal is `approved`: create `02-FINDINGS.md` with a single line "No findings — all strict-tier and realistic-tier tests passed on CUDA at <timestamp>." and skip to Task 3.
    - If Task 1 resume signal is `findings: ...`: parse each failure from the pytest output, file one `bd create` issue per distinct failure, and drive the Commit A → Commit B cycle per finding.
    - If Task 1 resume signal is `defer: ...`: halt this plan; the orchestrator pauses Phase 2 until CUDA is available.
  </behavior>
  <action>
    **Branch on the Task 1 resume signal:**

    **Path A: `approved` (no findings)**

    1. Create `.planning/phases/02-triton-fast-path-parity-vs-reference/02-FINDINGS.md` with:
       ```
       # Phase 2 Findings

       **Phase:** 02-triton-fast-path-parity-vs-reference
       **GPU run completed:** <timestamp from Task 1>
       **Status:** No findings.

       All strict-tier tests passed at < 1e-5 abs across the per-kernel grids.
       All realistic-tier tightenings landed without regression.
       Butterfly OOB regression (test_butterfly_triton_forward_scratch_oob_regression) still passes.
       D-25 .cv canary baseline (0 live uses) holds.
       Locked files (tests/test_parity.py, tests/test_layer_parity.py) unchanged.
       ```
    2. Proceed to Task 3.

    **Path B: `findings: <details>` — one or more failures**

    For each distinct failure in the pytest output:

    1. **Identify the finding**: extract the test name, parametrize id (`T,B,H[,nblocks]`), the assertion message (max abs diff, name, etc.), and which kernel module is suspected (look at the file under test: `scan.py` for dense, `scan_diagonal.py` for diagonal, etc.).

    2. **Confirm two-commit eligibility** per D-27:
       - The failing test IS the Commit A artifact (already landed in Plans 02-01..04). NO new test needs to be created — the strict-tier test that failed IS the regression detector.
       - If the failure is from Plan 02-05 (realistic-tier tightening), the failing assertion IS the artifact; the executor follows the Plan 02-05 revert protocol: revert THAT constant only with a `D-20 revert protocol` comment, then re-commit. This becomes its own bd issue documenting why the bound couldn't be tightened.

    3. **`bd create`**: file an issue with:
       - Title: `[Phase 2 / TRI-NN] <test_function_name> fails at <T=...,B=...,H=...> with max abs diff <value>`
       - Body (via `bd update <id> --notes`): the pytest tail (cut at `-v --tb=short` length), the file path of the failing test, a one-line root-cause hypothesis if obvious (e.g., "likely TF32 leak in autotune config bucket" or "diagonal kernel edge case at H=1 — BLOCK assumption violated"), and a reference to the relevant src file (`src/gru_qat/triton_kernels/scan_*.py`).
       - Capture the returned bd issue ID.

    4. **Investigate and fix (Commit B)**: open the relevant `src/gru_qat/triton_kernels/scan*.py` file, identify the bug, write the fix. Do NOT modify the test that surfaced the bug.

    5. **Verify the fix on CUDA** (this requires the user to re-run pytest):
       - Ask the user (in chat): "Fix candidate for <bd-id> is at <commit-hash> on branch <current-branch>. Please run `cd /home/claroche/gru-triton && uv run pytest <test-path>::<test-name> -v` and paste the output."
       - Iterate fix → user-runs-test → commit until the test passes.

    6. **`bd close <id>`** once the test passes. Record in `02-FINDINGS.md` the row: `<test_name> | <bd-id> | <root cause> | Commit A: <existing-test-commit-from-Plans-02-01..04> | Commit B: <fix-commit-hash> | <reg-test-path>`.

    7. Repeat per distinct finding.

    **Build `02-FINDINGS.md`** as a markdown table at the end of Path B with columns: Test | bd-id | Root cause | Commit A | Commit B | Reg-test path. Prepend a summary paragraph with the count and a one-line per-finding summary.

    Rules:
    - **No `@pytest.mark.xfail`** anywhere — D-27 forbids it; failures are either fixed or bd-tracked as known-but-accepted (rare, requires user confirmation).
    - **No silent tolerance loosening** — if a strict-tier < 1e-5 bound is genuinely unachievable (e.g., a kernel passes at 1.5e-5 due to documented IEEE-vs-IEEE drift), surface this as a finding with a bd issue and require explicit user confirmation per CONTEXT D-14 / D-15 (TF32 deviation is not a finding when strict passes; this is the opposite case). If user accepts, the test gets a docstring sentence documenting the accepted bound + bd ID, NOT an `xfail`.
    - **Locked files MUST NOT be touched**: `tests/test_parity.py`, `tests/test_layer_parity.py`. Verifier asserts this in Task 3.

    **Path C: `defer: <reason>`**

    1. Halt this plan. Write a one-line note to STATE.md describing the deferral and CUDA-box-needed status. The orchestrator pauses Phase 2 until the user re-invokes `/gsd-execute-phase 02-triton-fast-path-parity-vs-reference --resume`.
  </action>
  <verify>
    <automated>cd /home/claroche/gru-triton &amp;&amp; test -f .planning/phases/02-triton-fast-path-parity-vs-reference/02-FINDINGS.md &amp;&amp; echo "FINDINGS file exists"</automated>
  </verify>
  <acceptance_criteria>
    - `.planning/phases/02-triton-fast-path-parity-vs-reference/02-FINDINGS.md` exists
    - On Path A (no findings): file contains "No findings" with timestamp
    - On Path B (findings): file contains a markdown table with one row per distinct failure, each with a `bd-` issue ID matched by `bd show <id>` returning a valid record
    - For every Commit B (src/ fix) referenced in the FINDINGS table, `git log <commit-hash>` shows the commit exists and modifies a file under `src/gru_qat/triton_kernels/`
    - For every Commit A reference, `git log <commit-hash>` shows the commit exists in Plans 02-01..04 history (i.e., adds a `tests/test_triton_*_strict.py` test)
    - `git log --oneline` for Phase 2 shows test-commit precedes fix-commit for every finding (Commit A before Commit B chronologically — verifiable via `git log --reverse`)
    - `grep -r "xfail" tests/test_triton_*_strict.py` returns nothing (D-27)
    - `git diff <phase-2-base>..HEAD -- tests/test_parity.py tests/test_layer_parity.py` returns empty (D-28)
    - On Path A: `pytest tests/test_triton_*_strict.py -q -m "not slow"` exits 0 on CUDA (verified by Task 1 resume)
    - On Path B: re-running the full strict-tier suite after all fixes shows green
  </acceptance_criteria>
  <done>
    All Phase 2 findings (if any) have a bd issue, a Commit A test, a Commit B fix, and a row in `02-FINDINGS.md`. The strict-tier suite is fully green on CUDA. No xfails introduced. Locked files untouched.
  </done>
</task>

<task type="auto">
  <name>Task 3: Phase-exit SUMMARY</name>
  <files>.planning/phases/02-triton-fast-path-parity-vs-reference/02-SUMMARY.md</files>
  <read_first>
    - .planning/phases/02-triton-fast-path-parity-vs-reference/02-FINDINGS.md (from Task 2)
    - .planning/phases/02-triton-fast-path-parity-vs-reference/02-01-SUMMARY.md (from Plan 02-01)
    - .planning/phases/02-triton-fast-path-parity-vs-reference/02-02-SUMMARY.md (from Plan 02-02)
    - .planning/phases/02-triton-fast-path-parity-vs-reference/02-03-SUMMARY.md (from Plan 02-03)
    - .planning/phases/02-triton-fast-path-parity-vs-reference/02-04-SUMMARY.md (from Plan 02-04)
    - .planning/phases/02-triton-fast-path-parity-vs-reference/02-05-SUMMARY.md (from Plan 02-05)
    - .planning/phases/01-reference-path-parity-vs-nn-gru/01-VERIFICATION.md (Phase 1 SUMMARY template)
    - .planning/REQUIREMENTS.md (TRI-01..06 entries)
    - $HOME/.claude/get-shit-done/templates/summary.md (standard SUMMARY template)
  </read_first>
  <action>
    Write `.planning/phases/02-triton-fast-path-parity-vs-reference/02-SUMMARY.md` following the Phase 1 template (`01-VERIFICATION.md`). Required sections:

    1. **YAML frontmatter** with `phase`, `verified`, `status` (`passed` if Path A or all Path B fixes green; `partial` if any open findings; `failed` if blockers remain), `score` (truths verified / total), `re_verification: false`.

    2. **Phase Goal** — restate ROADMAP Phase 2 goal verbatim (pin every Triton variant at strict tier).

    3. **Goal Achievement table** with the 12 must-have truths from this plan's `must_haves.truths` block (above) and a Status column (VERIFIED / NOT-VERIFIED) and Evidence column (test names, commit hashes, grep outputs).

    4. **Requirement Coverage table**:
       | REQ-ID | Statement | Test Function(s) | Status |
       | TRI-01 | Dense Triton fwd+bwd < 1e-5 | test_scan_fwd_strict_matches_reference (+ _slow + bwd variants) | SATISFIED |
       | TRI-02 | Diagonal Triton fwd+bwd < 1e-5 | test_diagonal_fwd_strict_matches_reference (+ _slow + bwd variants) | SATISFIED |
       | TRI-03 | Monarch Triton fwd+bwd < 1e-5 across nblocks | test_monarch_fwd_strict_matches_reference (+ _slow + bwd variants) | SATISFIED |
       | TRI-04 | Butterfly Triton fwd+bwd < 1e-5 (incl. OOB regression) | test_butterfly_fwd_strict_matches_reference (+ _slow + bwd) + tests/test_butterfly_dispatch.py:164 OOB regression (referenced, not duplicated, per D-22) | SATISFIED |
       | TRI-05 | Autotune dWh/dbh slab-zero across configs | test_autotune_dWh_dbh_zero_init_across_configs in test_triton_scan_strict.py | SATISFIED |
       | TRI-06 | Cross-CTA determinism (50-run torch.equal) | test_persistent_kernel_deterministic in test_triton_scan_strict.py | SATISFIED |

    5. **Tolerance Contract Verification** — confirm strict tier passed at < 1e-5 abs; cite cell-parity locked-status and layer-parity locked-status (D-28); cite D-25 canary at count == 0.

    6. **Findings** section — link to `02-FINDINGS.md` and summarize counts (e.g., "0 findings" or "3 findings, all closed with bd issues bd-abc, bd-def, bd-ghi"). For each finding: bd ID, root cause, Commit A and Commit B hashes.

    7. **Realistic-tier tightenings** section — list which lines in `test_triton_diagonal.py` and `test_triton_monarch.py` were tightened, which (if any) were reverted with the D-20 protocol, and final tolerance values.

    8. **Phase 2 Hygiene** section — confirm `git diff` for the locked Phase 1 files is empty; confirm no `xfail` markers were introduced; confirm bd issue count matches finding count.

    9. **Next Steps** — point at Phase 3 (Structured PyTorch fallback parity) per ROADMAP.

    Append per-plan SUMMARY references (02-01-SUMMARY.md..02-05-SUMMARY.md + 02-FINDINGS.md).

    After writing, commit:
    ```bash
    git add .planning/phases/02-triton-fast-path-parity-vs-reference/02-SUMMARY.md .planning/phases/02-triton-fast-path-parity-vs-reference/02-FINDINGS.md
    git commit -m "docs(02): phase-exit SUMMARY + FINDINGS for Triton fast-path parity audit"
    ```
  </action>
  <verify>
    <automated>cd /home/claroche/gru-triton &amp;&amp; test -f .planning/phases/02-triton-fast-path-parity-vs-reference/02-SUMMARY.md &amp;&amp; grep -c "TRI-01\|TRI-02\|TRI-03\|TRI-04\|TRI-05\|TRI-06" .planning/phases/02-triton-fast-path-parity-vs-reference/02-SUMMARY.md</automated>
  </verify>
  <acceptance_criteria>
    - File `.planning/phases/02-triton-fast-path-parity-vs-reference/02-SUMMARY.md` exists
    - `grep -c "TRI-01\|TRI-02\|TRI-03\|TRI-04\|TRI-05\|TRI-06" 02-SUMMARY.md` returns at least 6 (all six requirements addressed)
    - SUMMARY frontmatter has `status` set to one of `passed` / `partial` / `failed` based on findings outcome
    - SUMMARY references `02-FINDINGS.md` and every per-plan SUMMARY (02-01..02-05)
    - SUMMARY's Goal Achievement table has 12 truth rows matching this plan's `must_haves.truths`
    - SUMMARY's Requirement Coverage table has 6 rows (TRI-01..06), each marked SATISFIED on Path A (or with a deferred/blocked annotation if a finding remains open)
    - Commit landed: `git log --oneline -1 .planning/phases/02-triton-fast-path-parity-vs-reference/02-SUMMARY.md` shows a docs(02) commit
  </acceptance_criteria>
  <done>
    Phase 2 SUMMARY written, FINDINGS file populated, all 6 TRI-* requirements traced to specific test functions and statuses, the document is committed.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| user ↔ Claude during GPU run | User runs commands on a CUDA box and pastes outputs; Claude trusts the pasted text faithfully |
| Claude ↔ bd issue tracker | `bd create` / `bd update` / `bd close` is the canonical task tracker for this project (CLAUDE.md) |
| Commit A (test) ↔ Commit B (fix) ordering | git log must show test-commit chronologically precedes fix-commit per D-27 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-23 | Spoofing | User pastes fabricated GPU output | accept | This is a trust boundary inherent to remote-execution workflows; verifier asserts the commit chain via `git log` not user claims |
| T-02-24 | Tampering | Phase 2 fix silently loosens cell-parity or layer-parity | mitigate | Task 2 acceptance criteria includes `git diff` over tests/test_parity.py and tests/test_layer_parity.py == empty (D-28) |
| T-02-25 | Repudiation | A finding lands without bd issue or without Commit A | mitigate | 02-FINDINGS.md table cross-references both; acceptance criteria asserts bd-id-matches-bd-show and git-log-shows-test-before-fix |
| T-02-26 | Information disclosure | bd issue body contains repo paths and stack traces | accept | bd is project-local; no PII; no secrets in stack traces |
| T-02-27 | Denial of service | Phase 2 GPU run takes hours | accept | Phase 2 test budget is ~30-60 min on a modern GPU per CONTEXT D-24 |
| T-02-28 | Elevation of privilege | Claude commits to main without user approval | mitigate | Per CLAUDE.md: NEVER push to main without explicit user request; Task 3 commits land on the current branch only |
</threat_model>

<verification>
After all three tasks complete:
- `.planning/phases/02-triton-fast-path-parity-vs-reference/02-SUMMARY.md` exists with all 12 truths and all 6 requirements marked
- `.planning/phases/02-triton-fast-path-parity-vs-reference/02-FINDINGS.md` exists (either "no findings" or table with bd-id-traced rows)
- On Path A (no findings): full strict-tier + realistic-tier suite passes on CUDA
- On Path B (findings): every finding has bd-id + Commit A + Commit B; full suite passes after all Commit B fixes
- `git log --oneline phase-2-base..HEAD` shows: per-plan commits (02-01..05) → fix commits (if any) → docs(02) SUMMARY commit
- `git diff phase-2-base..HEAD -- tests/test_parity.py tests/test_layer_parity.py` returns empty (D-28 LOCKED)
- `grep -r "xfail" tests/test_triton_*_strict.py` returns no matches (D-27)
- `bd ready` shows no open Phase-2 issues (all closed or accepted)
</verification>

<success_criteria>
- `02-SUMMARY.md` exists with all 12 truths from `must_haves.truths` traced to evidence
- All 6 TRI-* requirements marked SATISFIED (or with explicit blocker reference + bd ID)
- `02-FINDINGS.md` exists with appropriate content for Path A or Path B
- Every Phase 2 finding has a bd issue, Commit A test, Commit B fix
- Cell-parity and layer-parity locked files are unchanged across the entire phase
- D-25 canary holds: count == 0 live `cache_modifier=".cv"` uses in scan*.py
- No `@pytest.mark.xfail` introduced
- Phase 2 SUMMARY and FINDINGS committed
- ROADMAP.md Phase 2 entry checkbox flipped from [ ] to [x] with completion date (if all SATISFIED)
</success_criteria>

<output>
After completion, `.planning/phases/02-triton-fast-path-parity-vs-reference/02-SUMMARY.md` and `.planning/phases/02-triton-fast-path-parity-vs-reference/02-FINDINGS.md` are the phase-exit artifacts. The orchestrator inspects them to mark Phase 2 complete in `STATE.md` and `ROADMAP.md`. Optionally, STATE.md "Pending Todos" may carry forward any deferred items (e.g., realistic-tier tightenings in `test_butterfly_dispatch.py` that weren't tackled this phase — per Plan 02-05 scope decision).
</output>
