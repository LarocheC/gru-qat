---
phase: 02-triton-fast-path-parity-vs-reference
plan: 05
type: execute
wave: 1
depends_on: []
files_modified:
  - tests/test_triton_diagonal.py
  - tests/test_triton_monarch.py
autonomous: true
requirements: [TRI-02, TRI-03]

must_haves:
  truths:
    - "tests/test_triton_diagonal.py realistic-tier tolerances are tightened at the candidates identified in PATTERNS.md and the file still passes pytest on CUDA"
    - "tests/test_triton_monarch.py realistic-tier tolerances are tightened at the candidates identified in PATTERNS.md and the file still passes pytest on CUDA"
    - "Per D-20 commit discipline: tightenings land as separate, atomic commits per file (one commit for diagonal, one for monarch)"
    - "If a tightening fails on CUDA, the executor reverts THAT constant only with a docstring sentence explaining why (per D-20)"
    - "Existing test docstrings citing 'TF32 mantissa' / 'STE rounding' / 'documented rationale' are NOT touched (PATTERNS.md hard rule)"
    - "tests/test_triton_scan.py is unmodified (its only candidate is borderline and dense+TF32 noise dominates per PATTERNS.md)"
    - "tests/test_butterfly_dispatch.py is unmodified (its candidates at lines 315, 340 require CUDA-box verification before tightening; deferred to a follow-on commit if Plan 02-06 GPU run confirms the bounds are tightenable)"
    - "Locked files tests/test_parity.py and tests/test_layer_parity.py are unmodified (D-28)"
  artifacts:
    - path: "tests/test_triton_diagonal.py"
      provides: "Tighter realistic-tier tolerances at the tightenable candidates (lines 121, 156, 194, 268, 331, 339 per PATTERNS.md — list is exact; executor confirms each is tightenable in source by checking the inline rationale before touching)"
      contains: "Modified tolerance constants per the PATTERNS.md table; per-line comments where a tightening was attempted but reverted"
    - path: "tests/test_triton_monarch.py"
      provides: "Tighter realistic-tier tolerances at lines 248, 287, 288, 404, 409, 414 per PATTERNS.md"
      contains: "Modified tolerance constants; per-line revert comments where applicable"
  key_links:
    - from: "tests/test_triton_diagonal.py (modified)"
      to: "tests/test_triton_diagonal.py (unchanged Stage A at line 93 and STE rationale at line 238)"
      via: "untouched test functions (no changes to the locked / documented-rationale bounds)"
      pattern: "test_diagonal_pytorch_forward_matches_cell.*1e-5.*test_diagonal_triton_qat_backward_matches_pytorch.*1e-2"
---

<objective>
Tighten realistic-tier (TF32 / `'high'`) tolerance constants in `tests/test_triton_diagonal.py` and `tests/test_triton_monarch.py` per the PATTERNS.md tightening inventory (D-13 second bullet). Each tightening is a **separate atomic commit per file** (one commit for diagonal, one for monarch — NOT per-constant per D-20). If any tightening fails CI on a CUDA box, the executor reverts THAT constant only with a docstring sentence explaining why.

Per PATTERNS.md "Realistic-Tier Tightening Candidates":
- **`tests/test_triton_diagonal.py`** has ~5 candidates: line 121 (`rel < 1e-4` → try `< 1e-5`), line 156 (`rel < 1e-3` → try `< 1e-4`), line 194 (`rel < 1e-3` → try `< 1e-4`), line 268 (`rel < 1e-4` → try `< 1e-5`), lines 331+339 (`rel < 1e-3` → try `< 1e-4`).
- **`tests/test_triton_monarch.py`** has ~5 candidates: line 248 (`rel < 5e-2` → try `< 1e-2`), lines 287+288 (`rel < 5e-2` → try `< 5e-3`), lines 404+409+414 (`rel < 1e-4` → try `< 1e-5`).
- **NOT TIGHTENED** (documented rationale in inline comment per PATTERNS.md hard rule): test_triton_diagonal.py:93 (Stage A, already < 1e-5), :238 (STE rationale); test_triton_monarch.py:101 (Stage A), :127, :162, :210 (documented TF32 / QAT regime).
- **`tests/test_triton_scan.py`** (dense): only line 112 is conditionally tightenable, and PATTERNS.md notes it's "load-bearing" as a slab-zero regression detector. This plan does NOT touch dense.
- **`tests/test_butterfly_dispatch.py`**: lines 315 and 340 are conditionally tightenable. Deferred to a follow-on commit AFTER Plan 02-06 GPU run confirms the bounds hold; the strict-tier file 02-04 also exists, so the realistic-tier already has strict-tier supervision. Not in this plan's scope.

Purpose: TRI-02 (diagonal) and TRI-03 (monarch) coverage is improved at the realistic tier without weakening any documented rationale; supports D-13 second bullet ("tighten existing realistic-tier tolerances where they pass at < 1e-4").

Output: 2 modified test files with tighter tolerance constants; one git commit per file.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/02-triton-fast-path-parity-vs-reference/02-CONTEXT.md
@.planning/phases/02-triton-fast-path-parity-vs-reference/02-PATTERNS.md
@.planning/codebase/TESTING.md
@.planning/codebase/CONVENTIONS.md
@CLAUDE.md
@tests/test_triton_diagonal.py
@tests/test_triton_monarch.py

<interfaces>
<!-- Tightening map from PATTERNS.md "Realistic-Tier Tightening Candidates" -->

`tests/test_triton_diagonal.py` candidates:
- Line 121 (`test_diagonal_triton_forward_matches_pytorch`): rel < 1e-4 → try < 1e-5
- Line 156 (`test_diagonal_triton_qat_forward_matches_pytorch`): rel < 1e-3 → try < 1e-4
- Line 194 (`test_diagonal_triton_backward_matches_pytorch`): rel < 1e-3 → try < 1e-4
- Line 268 (`test_diagonal_dispatch_matches_per_step`): rel < 1e-4 → try < 1e-5
- Lines 331, 339 (`test_diagonal_dispatch_grad_matches_per_step`): rel < 1e-3 → try < 1e-4 (both)

`tests/test_triton_monarch.py` candidates:
- Line 248 (`test_monarch_triton_backward_matches_pytorch`): rel < 5e-2 → try < 1e-2
- Lines 287, 288 (`test_grulayer_use_triton_matches_pytorch_path`): rel < 5e-2 → try < 5e-3 (both)
- Lines 404, 409, 414 (`test_monarch_pytorch_backward_matches_cell`): rel < 1e-4 → try < 1e-5 (all three)

**Do NOT touch** (documented rationale, PATTERNS.md hard rule):
- `tests/test_triton_diagonal.py` lines 93, 238
- `tests/test_triton_monarch.py` lines 101, 127, 162, 210

The line numbers above are from PATTERNS.md's analysis. The executor MUST `grep -n` the actual current content of each file before editing to confirm the line numbers still match (file may have shifted slightly since 2026-05-13). If a line is no longer at the documented position, the executor finds the matching test function by name and locates the tolerance constant in its body.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Tighten realistic-tier tolerances in tests/test_triton_diagonal.py</name>
  <files>tests/test_triton_diagonal.py</files>
  <read_first>
    - tests/test_triton_diagonal.py (full file; locate each candidate by function name from PATTERNS.md table)
    - .planning/phases/02-triton-fast-path-parity-vs-reference/02-PATTERNS.md (section "tests/test_triton_diagonal.py" candidate table)
    - .planning/phases/02-triton-fast-path-parity-vs-reference/02-CONTEXT.md (D-13 second bullet, D-20)
  </read_first>
  <action>
    Apply the tightening map from the `<interfaces>` block for `tests/test_triton_diagonal.py`:

    1. Use `grep -n` to find the current line numbers of each function and confirm the tolerance constant matches the PATTERNS.md baseline. Functions to locate by name:
       - `test_diagonal_triton_forward_matches_pytorch` — change `< 1e-4` → `< 1e-5` on the rel-comparison assertion
       - `test_diagonal_triton_qat_forward_matches_pytorch` — change `< 1e-3` → `< 1e-4`
       - `test_diagonal_triton_backward_matches_pytorch` — change `< 1e-3` → `< 1e-4`
       - `test_diagonal_dispatch_matches_per_step` — change `< 1e-4` → `< 1e-5`
       - `test_diagonal_dispatch_grad_matches_per_step` — change both `< 1e-3` constants → `< 1e-4` (PATTERNS.md notes two: at line 331 and line 339)

    2. For each constant changed, the change is **literal-only**: `< 1e-4` becomes `< 1e-5`, etc. Do NOT change the surrounding error idiom (still relative-error with `1e-6` floor), do NOT change docstrings or comments unless explicitly required by a revert step below.

    3. Do NOT touch:
       - Line 93 (`test_diagonal_pytorch_forward_matches_cell`) — already at `< 1e-5`, Stage A algebraic equality (LOCKED per PATTERNS.md)
       - Line 238 (`test_diagonal_triton_qat_backward_matches_pytorch`) — STE rationale in docstring is load-bearing (PATTERNS.md hard rule)

    4. **Verification gate before committing**: the executor MUST run `pytest tests/test_triton_diagonal.py -q` on a machine with CUDA (or skip this plan to Plan 02-06 if no CUDA available — see the alt path below).

       **If no CUDA available at execute-time**: the executor stops here, commits NOTHING, and surfaces this to the Plan 02-06 phase-exit checkpoint with a note: "Plan 02-05 realistic-tier tightenings deferred to GPU run; planner: see PATTERNS.md tightening map." The plan is then re-executed AFTER the CUDA verification.

       **If CUDA available**: run the full test file. If ALL tests pass at the tightened bounds, commit (one commit, one file).

       **If any test fails**: revert THAT specific constant only (back to its original value) and add a one-line comment immediately above the assertion: `# Tightening to <new-bound> attempted in Plan 02-05, failed; kept at <original> per D-20 revert protocol.` Then re-run; commit only after the modified file passes.

    5. Commit message: `test(02-diagonal): tighten realistic-tier tolerances per D-13` with a body listing each constant changed (`< 1e-4` → `< 1e-5` at line ..., etc.).

    Per D-27 / D-20: no `@pytest.mark.xfail`, no comments out of failing assertions, no `try/except` wrapping. Either the tightening passes or it's reverted with a comment.
  </action>
  <verify>
    <automated>cd /home/claroche/gru-triton &amp;&amp; uv run python -c "import ast; ast.parse(open('tests/test_triton_diagonal.py').read())" &amp;&amp; uv run pytest tests/test_triton_diagonal.py --collect-only -q 2>&amp;1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - `python -c "import ast; ast.parse(...)"` exits 0
    - `pytest tests/test_triton_diagonal.py --collect-only -q` lists the same number of tests as before the change (no test added or removed; only tolerance constants modified)
    - For each candidate function in the inventory, EITHER the tolerance constant is tightened to the target value, OR a `D-20 revert protocol` comment is present immediately above the assertion explaining why (with a `git blame` showing the executor's deliberate decision)
    - `grep -c "test_diagonal_pytorch_forward_matches_cell" tests/test_triton_diagonal.py` returns at least 1 AND the Stage A test still asserts `< 1e-5` (unchanged per PATTERNS.md)
    - `grep -c "STE rounding can flip mask bits" tests/test_triton_diagonal.py` returns at least 1 (line 238 docstring rationale preserved)
    - On CUDA box: `pytest tests/test_triton_diagonal.py -q -m "not slow"` exits 0 — gated via Plan 02-06 phase-exit verification. On CPU-only execution, the executor stops before commit and surfaces the deferral; this acceptance criterion is treated as PENDING and verified by 02-06.
    - `ruff check tests/test_triton_diagonal.py` exits 0
    - `git diff HEAD~1 -- tests/test_parity.py tests/test_layer_parity.py` returns empty (D-28 LOCKED)
  </acceptance_criteria>
  <done>
    Tolerance constants in `tests/test_triton_diagonal.py` tightened per the PATTERNS.md inventory (or reverted with D-20 protocol comments); one atomic commit landed; locked-rationale tests untouched; D-28-locked files unchanged.
  </done>
</task>

<task type="auto">
  <name>Task 2: Tighten realistic-tier tolerances in tests/test_triton_monarch.py</name>
  <files>tests/test_triton_monarch.py</files>
  <read_first>
    - tests/test_triton_monarch.py (full file; locate each candidate by function name)
    - .planning/phases/02-triton-fast-path-parity-vs-reference/02-PATTERNS.md (section "tests/test_triton_monarch.py" candidate table)
    - .planning/phases/02-triton-fast-path-parity-vs-reference/02-CONTEXT.md (D-13 second bullet, D-20)
  </read_first>
  <action>
    Apply the tightening map from the `<interfaces>` block for `tests/test_triton_monarch.py`:

    1. Use `grep -n` to find current line numbers. Functions to locate by name:
       - `test_monarch_triton_backward_matches_pytorch` — change `< 5e-2` → `< 1e-2`
       - `test_grulayer_use_triton_matches_pytorch_path` — change both `< 5e-2` constants → `< 5e-3` (PATTERNS.md notes two: out and hT at lines 287, 288)
       - `test_monarch_pytorch_backward_matches_cell` — change all three `< 1e-4` constants → `< 1e-5` (PATTERNS.md notes three: dh0, dWh, dbh at lines 404, 409, 414)

    2. Literal-only changes. Do NOT touch:
       - Line 101 (`test_monarch_pytorch_forward_matches_cell`) — Stage A, already `< 1e-5`, LOCKED
       - Line 127 (`test_monarch_triton_forward_matches_pytorch`) — `< 5e-3` with documented "TF32 matmul + T-step compounding" comment at line 126 (PATTERNS.md hard rule)
       - Line 162 (`test_monarch_triton_qat_forward_matches_pytorch`) — QAT regime, < 1e-1 (PATTERNS.md hard rule)
       - Line 210 (`test_monarch_triton_qat_backward_matches_pytorch`) — QAT regime, < 1e-1

    3. Same verification gate as Task 1: requires CUDA to validate; if no CUDA available, the executor stops without committing and defers to Plan 02-06.

    4. Same revert protocol as Task 1: on per-constant CI failure, revert THAT constant only with a `D-20 revert protocol` comment.

    5. Commit message: `test(02-monarch): tighten realistic-tier tolerances per D-13` with the per-constant body.

    Tasks 1 and 2 land as **separate commits** per D-20 (one per file). Both files modified within the same plan, but each commit is atomic.
  </action>
  <verify>
    <automated>cd /home/claroche/gru-triton &amp;&amp; uv run python -c "import ast; ast.parse(open('tests/test_triton_monarch.py').read())" &amp;&amp; uv run pytest tests/test_triton_monarch.py --collect-only -q 2>&amp;1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - `python -c "import ast; ast.parse(...)"` exits 0
    - `pytest tests/test_triton_monarch.py --collect-only -q` lists the same number of tests as before (no add/remove)
    - For each candidate function in the inventory, EITHER the tolerance is tightened to the target value, OR a `D-20 revert protocol` comment is present
    - `grep -c "test_monarch_pytorch_forward_matches_cell" tests/test_triton_monarch.py` returns at least 1 AND Stage A still asserts `< 1e-5` (unchanged)
    - `grep -c "TF32 matmul + T-step compounding" tests/test_triton_monarch.py` returns at least 1 (line 126 rationale preserved)
    - On CUDA box: `pytest tests/test_triton_monarch.py -q -m "not slow"` exits 0 — gated via Plan 02-06 phase-exit verification (PENDING in CPU-only run)
    - `ruff check tests/test_triton_monarch.py` exits 0
    - `git log --oneline -n 2 -- tests/test_triton_monarch.py tests/test_triton_diagonal.py` shows TWO separate commits per D-20 (one for diagonal, one for monarch — NOT one combined commit)
    - `git diff HEAD~2 -- tests/test_parity.py tests/test_layer_parity.py` returns empty (D-28 LOCKED)
  </acceptance_criteria>
  <done>
    Tolerance constants in `tests/test_triton_monarch.py` tightened per the PATTERNS.md inventory (or reverted with D-20 protocol comments); separate atomic commit landed; locked-rationale tests untouched; both file commits are separated per D-20.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| tightening edit ↔ existing test rationale | Each candidate may have inline rationale that makes the tightening unsound; executor must preserve docstring rationale and only revert with explicit comment |
| executor decision ↔ CUDA verification | Without CUDA, the tightening cannot be validated; this is a deliberate gate that requires Plan 02-06 to close the loop |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-20 | Tampering | Tightening introduces silent test regression that masks future bugs | mitigate | Per D-20: tightening is reverted (with comment) if it fails CI; never xfail'd or silently loosened |
| T-02-21 | Repudiation | Untracked CUDA verification — tightening lands without GPU run | mitigate | Acceptance criteria treat the CUDA-run check as PENDING until Plan 02-06 verifies it; planner explicitly calls this out |
| T-02-22 | Tampering | Documented-rationale tolerance loosened by accident | mitigate | "Do not touch" list explicitly enumerates protected lines (PATTERNS.md hard rule); acceptance criteria grep-confirm rationale strings are still present |
</threat_model>

<verification>
After both tasks land (and after Plan 02-06 GPU run confirms tightenings hold):
- `git log --oneline tests/test_triton_diagonal.py tests/test_triton_monarch.py` shows TWO separate Phase 2 commits per D-20
- `git diff HEAD~2 -- tests/test_parity.py tests/test_layer_parity.py tests/test_triton_scan.py tests/test_butterfly_dispatch.py` returns empty (Phase 2 scope hygiene)
- `uv run ruff check tests/test_triton_diagonal.py tests/test_triton_monarch.py` exits 0
- All currently-tightened constants are at the target value OR reverted with `D-20 revert protocol` comments
- Plan 02-06 phase-exit GPU run executes both files on CUDA and confirms full pass

**Special note for executor**: if no CUDA box is available at execute-time, this plan halts after the AST-validity + collection check; commits are deferred to a follow-on phase-exit re-run after Plan 02-06's checkpoint. The orchestrator (Plan 02-06) is responsible for re-invoking this plan if needed.
</verification>

<success_criteria>
- Two file modifications (`tests/test_triton_diagonal.py`, `tests/test_triton_monarch.py`)
- Two atomic git commits (one per file) per D-20
- Each tightening either landed or reverted with the `D-20 revert protocol` comment
- All locked rationale lines untouched (line 93, 238 in diagonal; 101, 127, 162, 210 in monarch)
- `tests/test_parity.py`, `tests/test_layer_parity.py`, `tests/test_triton_scan.py`, `tests/test_butterfly_dispatch.py` unchanged
- `pytest --collect-only` on each modified file returns same number of tests as before
- Ruff clean
- On CUDA: both files pass under `pytest -m "not slow"` (verified by Plan 02-06)
</success_criteria>

<output>
After completion, create `.planning/phases/02-triton-fast-path-parity-vs-reference/02-05-SUMMARY.md` covering:
- For each candidate line in the inventory: tightened (with new value) or reverted (with rationale)
- Commit hashes for the two atomic commits
- CUDA verification status (PENDING / PASSED — set by Plan 02-06 follow-up)
- Any deviations from the plan and rationale
</output>
