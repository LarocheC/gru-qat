---
phase: 02-triton-fast-path-parity-vs-reference
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - tests/test_triton_monarch_strict.py
autonomous: true
requirements: [TRI-03]

must_haves:
  truths:
    - "tests/test_triton_monarch_strict.py exists, imports cleanly, file-skips on CPU-only or missing-torch_structured machines"
    - "On CUDA: test_monarch_fwd_strict_matches_reference passes < 1e-5 abs across FAST_MONARCH_GRID (nblocks ∈ {2, 4, 8} parametrized)"
    - "On CUDA: test_monarch_bwd_strict_matches_reference passes < 1e-5 abs on (dgi, dh0, dWh_struct, dbh) gradients across FAST_MONARCH_GRID"
    - "On CUDA: _slow siblings pass under pytest -m slow"
    - "Module-scope torch.set_float32_matmul_precision('highest') is present"
    - "Grid pruning rule H % nblocks == 0 is enforced in the parametrize list"
    - "Existing tests/test_triton_monarch.py is unmodified by this plan"
    - "Locked files tests/test_parity.py and tests/test_layer_parity.py are unmodified (D-28)"
  artifacts:
    - path: "tests/test_triton_monarch_strict.py"
      provides: "Strict-tier Monarch Triton kernel parity tests, parametrized over nblocks ∈ {2, 4, 8} per D-16"
      contains: "test_monarch_fwd_strict_matches_reference, test_monarch_bwd_strict_matches_reference, test_monarch_fwd_strict_matches_reference_slow, test_monarch_bwd_strict_matches_reference_slow"
  key_links:
    - from: "tests/test_triton_monarch_strict.py"
      to: "src/gru_qat/triton_kernels/scan_monarch.py (gru_scan_monarch_forward_triton, gru_scan_monarch_backward_triton, gru_scan_monarch_forward_pytorch, gru_scan_monarch_backward_pytorch)"
      via: "kernel call + pytorch reference call with shared gi + Wh_struct"
      pattern: "gru_scan_monarch_forward_triton.*gru_scan_monarch_forward_pytorch.*assert.*< 1e-5"
    - from: "tests/test_triton_monarch_strict.py"
      to: "tests/test_triton_monarch.py"
      via: "verbatim duplicate of _make_monarch_layer + _build_gi_from_cell helpers (per D-18)"
      pattern: "def _make_monarch_layer.*StructureConfig.kind=\"monarch\""
---

<objective>
Create `tests/test_triton_monarch_strict.py` — the strict-tier (< 1e-5 abs under `torch.set_float32_matmul_precision('highest')`) parity audit for the Monarch (block-diagonal) Triton scan kernel. Unlike diagonal, Monarch has block-matmuls on the hidden side, so strict-tier may surface TF32-vs-fp32 reduction-order mismatches that the realistic tier file at `tests/test_triton_monarch.py` deliberately tolerates. Per D-14: if any (T, B, H, nblocks) combo fails < 1e-5 abs, that's a finding (bd issue + Commit A → Commit B per D-27).

Parametrized over `nblocks ∈ {2, 4, 8}` per D-16. Grid pruned by `H % nblocks == 0` (Monarch requires H divisible by nblocks per `src/gru_qat/structure.py` shape validators).

Purpose: TRI-03 (Monarch Triton fwd+bwd at < 1e-5 across nblocks ∈ {2, 4, 8}) needs strict-tier coverage.

Output: One new test file with 4 test functions. Skips cleanly on CPU or missing-torch_structured. Ready for Plan 02-06 GPU run.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/02-triton-fast-path-parity-vs-reference/02-CONTEXT.md
@.planning/phases/02-triton-fast-path-parity-vs-reference/02-PATTERNS.md
@.planning/codebase/TESTING.md
@.planning/codebase/CONVENTIONS.md
@CLAUDE.md
@tests/test_triton_monarch.py
@src/gru_qat/triton_kernels/scan_monarch.py

<interfaces>
<!-- Key entry points used by this plan. Executor uses these directly. -->

From `src/gru_qat/triton_kernels/scan_monarch.py`:
- `extract_monarch_factors(cell) -> tuple` — extracts the Monarch Wh factors + bh_cat
- `gru_scan_monarch_forward_pytorch(gi, h0, Wh_struct, bh_cat) -> torch.Tensor` — pure-PyTorch reference (pinned at < 1e-5 against the cell's structured forward in `tests/test_triton_monarch.py:80-104` Stage A)
- `gru_scan_monarch_forward_triton(gi, h0, Wh_struct, bh_cat) -> torch.Tensor` — kernel under test
- `gru_scan_monarch_backward_pytorch(...)` and `gru_scan_monarch_backward_triton(...)`

From `tests/test_triton_monarch.py`:
- `_make_monarch_layer(in_size: int, hid: int, nblocks: int = 4) -> GRULayer` at lines 35-51 — **duplicate verbatim** per D-18
- `_build_gi_from_cell(layer: GRULayer, x: torch.Tensor) -> torch.Tensor` at lines 54-77 — **duplicate verbatim** per D-18
- `cuda_only` at lines 30-32
- Module-top `torch_structured = pytest.importorskip("torch_structured")` at line 17 — strict file needs BOTH `pytest.importorskip("triton")` AND `pytest.importorskip("torch_structured")` per PATTERNS.md "pytest.importorskip at module top"
- Existing fwd parity at line 127 (rel < 5e-3) and bwd at line 248 (rel < 5e-2) — the strict tier uses absolute < 1e-5 instead

Existing TF32 idiom (NOT what strict-tier uses): `max_diff = (ref - tri).abs().max().item(); rel = max_diff / max(ref.abs().max().item(), 1e-6); assert rel < TOL` — strict file uses `max_diff < 1e-5` directly.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Strict-tier monarch fwd parity over T x B x H x nblocks grid</name>
  <files>tests/test_triton_monarch_strict.py</files>
  <read_first>
    - tests/test_triton_monarch.py (lines 1-77 for preamble + helpers; lines 106-127 for fwd body shape)
    - src/gru_qat/triton_kernels/scan_monarch.py (signatures of gru_scan_monarch_forward_triton / _pytorch, including Wh_struct shape conventions)
    - .planning/phases/02-triton-fast-path-parity-vs-reference/02-PATTERNS.md (section "tests/test_triton_monarch_strict.py")
    - .planning/phases/02-triton-fast-path-parity-vs-reference/02-CONTEXT.md (D-16 monarch grid, D-19, D-21)
    - .planning/codebase/CONVENTIONS.md (type annotations, naming)
  </read_first>
  <behavior>
    - Module imports successfully on CPU; SKIPS if `torch_structured` or `triton` missing
    - On CUDA, `test_monarch_fwd_strict_matches_reference[T-B-H-nblocks]` passes `< 1e-5` absolute across FAST_MONARCH_GRID
    - Grid is pruned to `H % nblocks == 0`
    - `_slow` sibling passes under `pytest -m slow`
    - Module docstring notes that Monarch's hidden-side block matmul makes the strict-tier test the primary stressor of `tl.dot` reduction order; if any combo fails, that's a finding per D-14
  </behavior>
  <action>
    Create `tests/test_triton_monarch_strict.py`. Concrete components:

    1. **Module docstring** — strict tier, `'highest'`, < 1e-5 abs. Mirror the diagonal_strict template but add: "Monarch's hidden-side block matmul (3 gates × tl.dot per timestep) is the primary stressor of `tl.dot` reduction order under IEEE fp32. The realistic-tier sibling at `tests/test_triton_monarch.py:127` deliberately tolerates TF32 noise at rel < 5e-3; strict tier eliminates that source of noise by setting `'highest'` and asserts < 1e-5 abs. If a (T, B, H, nblocks) combo fails strict-tier, that's a finding per D-14 — Commit A failing test → bd issue → Commit B fix in src/."

    2. **Imports**:
    - `from __future__ import annotations`
    - `import warnings; warnings.filterwarnings("ignore", message=".*different CUDA versions.*")` — mirror `tests/test_triton_monarch.py:11-12`
    - `import pytest`, `import torch`
    - **BOTH** module-level skips at the top: first `triton = pytest.importorskip("triton")`, then `torch_structured = pytest.importorskip("torch_structured")` (order matters — Triton is the kernel framework; torch_structured is the H×H factor provider)
    - Internal imports with `# noqa: E402`: `from gru_qat import GRULayer, QuantRecipe, QuantizerConfig, StructureConfig` and `from gru_qat.gru_cell import GRUCellQuant` and `from gru_qat.triton_kernels.scan_monarch import extract_monarch_factors, gru_scan_monarch_backward_pytorch, gru_scan_monarch_backward_triton, gru_scan_monarch_forward_pytorch, gru_scan_monarch_forward_triton`

    3. **Module-scope `torch.set_float32_matmul_precision("highest")`** with strict-tier comment block.

    4. **`cuda_only`** — duplicate from `tests/test_triton_monarch.py:30-32`.

    5. **`_make_monarch_layer`** — verbatim copy of `tests/test_triton_monarch.py:35-51` with comment "duplicated per D-18".

    6. **`_build_gi_from_cell`** — verbatim copy of `tests/test_triton_monarch.py:54-77`.

    7. **Shape grid**:
    ```
    FAST_MONARCH_GRID = [
        (T, B, H, nblocks)
        for T in (1, 8, 64)
        for B in (1, 4, 32)
        for H in (32, 128, 512)
        for nblocks in (2, 4, 8)
        if H % nblocks == 0  # Monarch requires H divisible by nblocks
    ]
    SLOW_MONARCH_GRID = [
        (T, B, H, nblocks)
        for T in (512, 1024)
        for B in (1, 4, 32)
        for H in (32, 128, 512)
        for nblocks in (2, 4, 8)
        if H % nblocks == 0
    ]
    ```
    All H values in {32, 128, 512} are divisible by all nblocks values in {2, 4, 8}, so the filter is a safety check that doesn't prune — but it documents the invariant and protects against future grid extensions adding non-divisible combos.

    8. **`test_monarch_fwd_strict_matches_reference(T: int, B: int, H: int, nblocks: int) -> None`** — `@cuda_only` + `@pytest.mark.parametrize("T,B,H,nblocks", FAST_MONARCH_GRID)`. Body mirrors `tests/test_triton_monarch.py:106-127`:
    - `torch.manual_seed(0); device = torch.device("cuda")`
    - Build `layer = _make_monarch_layer(H, H, nblocks=nblocks).to(device).eval()` (input and hidden both size H)
    - Extract factors: `Wh_struct, bh_cat = extract_monarch_factors(layer.cell)` (tuple unpacking shape matches `tests/test_triton_monarch.py` — confirm exact return shape when reading the file)
    - Build `x = torch.randn(T, B, H, device=device); h0 = torch.randn(B, H, device=device)`
    - Compute `gi = _build_gi_from_cell(layer, x)` (shape `[T, B, 3H]`)
    - `ref = gru_scan_monarch_forward_pytorch(gi, h0, Wh_struct, bh_cat)`
    - `tri = gru_scan_monarch_forward_triton(gi, h0, Wh_struct, bh_cat)`
    - `max_diff = (ref - tri).abs().max().item()`
    - `assert max_diff < 1e-5, f"max abs diff {max_diff:.4e} (T={T},B={B},H={H},nblocks={nblocks})"`

    9. **`test_monarch_fwd_strict_matches_reference_slow`** — same body, `@pytest.mark.slow` + SLOW_MONARCH_GRID.

    All tests start with `torch.manual_seed(0)`. Type annotations on every parameter. Absolute-error idiom only. No relative-error / `1e-6` floor.
  </action>
  <verify>
    <automated>cd /home/claroche/gru-triton &amp;&amp; uv run python -c "import ast; ast.parse(open('tests/test_triton_monarch_strict.py').read())" &amp;&amp; uv run pytest tests/test_triton_monarch_strict.py --collect-only -q 2>&amp;1 | tail -15</automated>
  </verify>
  <acceptance_criteria>
    - `python -c "import ast; ast.parse(...)"` exits 0
    - `pytest --collect-only -q tests/test_triton_monarch_strict.py` lists ≥ 2 fwd test functions with combined parametrization ≥ 27 × 3 + 18 × 3 = 81 + 54 = 135 ids (3 nblocks per T,B,H slot)
    - `pytest tests/test_triton_monarch_strict.py -q` exits 0 on CPU with all CUDA tests SKIPPED
    - `grep -c "torch.set_float32_matmul_precision(\"highest\")" tests/test_triton_monarch_strict.py` returns 1
    - `grep -c "if H % nblocks == 0" tests/test_triton_monarch_strict.py` returns 2 (both grids enforce divisibility)
    - `grep -c "torch_structured = pytest.importorskip" tests/test_triton_monarch_strict.py` returns 1
    - `grep -c "abs().max()" tests/test_triton_monarch_strict.py` returns at least 1
    - `grep -c "/ max(ref.abs().max()" tests/test_triton_monarch_strict.py` returns 0 (no relative-error idiom)
    - `ruff check tests/test_triton_monarch_strict.py` exits 0
  </acceptance_criteria>
  <done>
    Monarch fwd parity tests created with nblocks parametrization, grid pruning by divisibility, absolute-error idiom, both `triton` and `torch_structured` importorskip gates, helpers duplicated verbatim per D-18.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Strict-tier monarch bwd parity</name>
  <files>tests/test_triton_monarch_strict.py</files>
  <read_first>
    - tests/test_triton_monarch_strict.py (file from Task 1)
    - tests/test_triton_monarch.py:215-248 (existing bwd test, rel < 5e-2; strict file uses absolute < 1e-5)
    - src/gru_qat/triton_kernels/scan_monarch.py (gru_scan_monarch_backward_pytorch, gru_scan_monarch_backward_triton signatures and return-tuple order)
    - .planning/phases/02-triton-fast-path-parity-vs-reference/02-PATTERNS.md (section "Core strict-tier fwd / bwd bodies" for monarch)
  </read_first>
  <behavior>
    - On CUDA, `test_monarch_bwd_strict_matches_reference[T-B-H-nblocks]` runs the backward against the PyTorch reference and asserts `(dgi, dh0, dWh_struct, dbh)` match at `< 1e-5` absolute across FAST_MONARCH_GRID
    - `_slow` sibling passes under `pytest -m slow`
    - Per D-14: if Monarch bwd fails strict at any (T, B, H, nblocks), bd issue + Commit A → Commit B
  </behavior>
  <action>
    Append two test functions to `tests/test_triton_monarch_strict.py`:

    1. **`test_monarch_bwd_strict_matches_reference(T: int, B: int, H: int, nblocks: int) -> None`** — `@cuda_only` + `@pytest.mark.parametrize("T,B,H,nblocks", FAST_MONARCH_GRID)`. Body mirrors `tests/test_triton_monarch.py:215-248`:
    - `torch.manual_seed(0); device = torch.device("cuda")`
    - Build the same layer + tensors as the fwd test
    - Run fwd to get `out_ref = gru_scan_monarch_forward_pytorch(...)`
    - Build `dy = torch.randn(T, B, H, device=device)`
    - Call `dgi_ref, dh0_ref, dWh_struct_ref, dbh_ref = gru_scan_monarch_backward_pytorch(dy, out_ref, gi, h0, Wh_struct, bh_cat)` — confirm the exact return-tuple order from `src/gru_qat/triton_kernels/scan_monarch.py` and from `tests/test_triton_monarch.py:215-248` before writing this; if the existing test uses autograd (`out.sum().backward()`), mirror that pattern instead and compare grads via `Wh_struct.grad`, `bh_cat.grad`, etc.
    - Call `dgi_tri, dh0_tri, dWh_struct_tri, dbh_tri = gru_scan_monarch_backward_triton(...)` with the same inputs
    - Loop the four gradient comparisons:
      - For `name, ref_g, tri_g` in `[("dgi", dgi_ref, dgi_tri), ("dh0", dh0_ref, dh0_tri), ("dWh_struct", dWh_struct_ref, dWh_struct_tri), ("dbh", dbh_ref, dbh_tri)]`:
        - `max_diff = (ref_g - tri_g).abs().max().item()`
        - `assert max_diff < 1e-5, f"{name} max abs diff {max_diff:.4e} (T={T},B={B},H={H},nblocks={nblocks})"`

    2. **`test_monarch_bwd_strict_matches_reference_slow`** — same body, `@pytest.mark.slow` + SLOW_MONARCH_GRID.

    If the bwd reference function signature in `src/gru_qat/triton_kernels/scan_monarch.py` differs from the diagonal kernel's (e.g., Monarch's gradient over a structured factor returns a multi-tensor Wh_struct grad instead of a single tensor), unpack the tuple accordingly — read the source signature before writing the unpack pattern. The four-tensor comparison loop's exact tensor count is whatever the function returns; the test must compare ALL returned gradients element-wise.
  </action>
  <verify>
    <automated>cd /home/claroche/gru-triton &amp;&amp; uv run pytest tests/test_triton_monarch_strict.py --collect-only -q 2>&amp;1 | grep -E "test_monarch_bwd_strict_matches_reference" | wc -l</automated>
  </verify>
  <acceptance_criteria>
    - `pytest --collect-only -q tests/test_triton_monarch_strict.py` lists both `test_monarch_bwd_strict_matches_reference` and `_slow` (≥ 135 combined parametrized ids)
    - `pytest tests/test_triton_monarch_strict.py -q` exits 0 on CPU with all CUDA tests SKIPPED
    - `grep -E '(dgi|dh0|dWh_struct|dbh)' tests/test_triton_monarch_strict.py | wc -l` returns ≥ 8 (each gradient name appears in declaration + assertion message)
    - `grep -c "abs().max()" tests/test_triton_monarch_strict.py` returns at least 5 (1 fwd + 4 bwd-tensor assertions)
    - `ruff check tests/test_triton_monarch_strict.py` exits 0
  </acceptance_criteria>
  <done>
    Monarch bwd parity tests appended with four-tensor gradient loop, absolute-error assertions, FAST + SLOW siblings, full nblocks parametrization, all CUDA tests skip on CPU.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| test ↔ Monarch Triton kernel | Test feeds random `gi/h0/Wh_struct/bh_cat` to kernel; kernel must produce numerically-correct output |
| test ↔ torch_structured | Optional dependency; module-level skip if missing |
| test ↔ Monarch PyTorch reference | The reference is pinned at < 1e-5 to the cell's structured forward at `tests/test_triton_monarch.py:80-104` Stage A |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-10 | Tampering | Monarch kernel `tl.dot` reduction order | mitigate | Strict-tier parity at < 1e-5 abs over 27 + 18 fast/slow shape combos × 3 nblocks |
| T-02-11 | Information disclosure | Random test inputs | accept | No PII / secrets |
| T-02-12 | Denial of service | torch_structured import on slow path | accept | `pytest.importorskip` short-circuits; no measurable overhead |
| T-02-13 | Repudiation | Monarch reference may itself be wrong | accept | Stage A algebraic equality at `tests/test_triton_monarch.py:80-104` pins reference < 1e-5 to the cell; if reference is wrong, Stage A fails first |
| T-02-14 | Tampering | Grid pruning `H % nblocks == 0` is silently violated | mitigate | Filter is in the list comprehension; pytest collection time will fail if list is empty for a particular nblocks; assertion is explicit in the grid definition |
</threat_model>

<verification>
After both tasks land:
- `uv run pytest tests/test_triton_monarch_strict.py --collect-only -q` — ≥ 4 distinct test functions
- `uv run pytest tests/test_triton_monarch_strict.py -q -m "not slow"` — exits 0 on CPU (all SKIP)
- `uv run ruff check tests/test_triton_monarch_strict.py` — exit 0
- `git diff HEAD~2 -- tests/test_triton_monarch.py tests/test_parity.py tests/test_layer_parity.py` — empty (D-28 + D-20)
</verification>

<success_criteria>
- File exists at `tests/test_triton_monarch_strict.py`
- 4 test functions (`test_monarch_fwd_strict_matches_reference`, `_slow`, `test_monarch_bwd_strict_matches_reference`, `_slow`)
- Module-scope `torch.set_float32_matmul_precision("highest")`
- Both `pytest.importorskip("triton")` AND `pytest.importorskip("torch_structured")` at module top
- Grid parametrized over nblocks ∈ {2, 4, 8} with `H % nblocks == 0` filter
- Absolute-error assertions only
- `_make_monarch_layer` + `_build_gi_from_cell` duplicated verbatim per D-18
- Slow tests gated `@pytest.mark.slow`; Triton tests gated `@cuda_only`
- `tests/test_triton_monarch.py`, `tests/test_parity.py`, `tests/test_layer_parity.py` unchanged
</success_criteria>

<output>
After completion, create `.planning/phases/02-triton-fast-path-parity-vs-reference/02-03-SUMMARY.md` covering:
- File created and final LOC count
- Test function inventory with parametrize cardinality (per-nblocks breakdown)
- Whether bwd uses autograd or direct kernel call (depends on `tests/test_triton_monarch.py:215-248` pattern)
- Notes on Wh_struct gradient shape (single tensor or multi-tensor unpack)
- Any deviations from the plan
</output>
