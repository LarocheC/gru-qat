---
phase: 02-triton-fast-path-parity-vs-reference
plan: 04
type: execute
wave: 1
depends_on: []
files_modified:
  - tests/test_triton_butterfly_strict.py
autonomous: true
requirements: [TRI-04]

must_haves:
  truths:
    - "tests/test_triton_butterfly_strict.py exists, imports cleanly, file-skips on CPU-only or missing-torch_structured machines"
    - "On CUDA: test_butterfly_fwd_strict_matches_reference passes < 1e-5 abs across FAST_BFLY_GRID (H ∈ {32, 128, 512} — all powers of 2)"
    - "On CUDA: test_butterfly_bwd_strict_matches_reference passes < 1e-5 abs across FAST_BFLY_GRID"
    - "On CUDA: _slow siblings pass under pytest -m slow"
    - "Module-scope torch.set_float32_matmul_precision('highest') is present"
    - "Module docstring references (does NOT duplicate) the existing OOB regression at tests/test_butterfly_dispatch.py:164 per D-22"
    - "Existing tests/test_butterfly_dispatch.py:164 OOB regression still passes (verifier confirms in Plan 02-06)"
    - "Existing tests/test_butterfly_dispatch.py is unmodified by this plan"
    - "Locked files tests/test_parity.py and tests/test_layer_parity.py are unmodified (D-28)"
  artifacts:
    - path: "tests/test_triton_butterfly_strict.py"
      provides: "Strict-tier Butterfly Triton kernel parity tests; references but does not duplicate the existing OOB regression"
      contains: "test_butterfly_fwd_strict_matches_reference, test_butterfly_bwd_strict_matches_reference, test_butterfly_fwd_strict_matches_reference_slow, test_butterfly_bwd_strict_matches_reference_slow"
  key_links:
    - from: "tests/test_triton_butterfly_strict.py"
      to: "src/gru_qat/triton_kernels/scan_butterfly.py (gru_scan_butterfly_forward_triton, gru_scan_butterfly_backward_triton, gru_scan_butterfly, extract_butterfly_twiddles)"
      via: "kernel call + CUDA-op per-step reference"
      pattern: "gru_scan_butterfly_forward_triton.*assert.*< 1e-5"
    - from: "tests/test_triton_butterfly_strict.py"
      to: "tests/test_butterfly_dispatch.py"
      via: "verbatim duplicate of _make_layer helper (per D-18); module-docstring reference to OOB regression at line 164 (per D-22)"
      pattern: "def _make_layer.*StructureConfig.kind=\"butterfly\""
---

<objective>
Create `tests/test_triton_butterfly_strict.py` — the strict-tier (< 1e-5 abs under `torch.set_float32_matmul_precision('highest')`) parity audit for the Butterfly Triton scan kernel. Butterfly requires H to be a power of 2 per D-16; the grid uses H ∈ {32, 128, 512}.

Per D-22: the existing OOB regression at `tests/test_butterfly_dispatch.py:164` (function `test_butterfly_triton_forward_scratch_oob_regression`) is sufficient. The strict file **references it in the module docstring** but does NOT duplicate. Phase-exit verification in Plan 02-06 confirms the OOB regression still passes.

Purpose: TRI-04 (Butterfly Triton fwd+bwd at < 1e-5, including OOB regression reference) needs strict-tier coverage that doesn't exist in `tests/test_butterfly_dispatch.py` (which is TF32 / rel < 1e-1).

Output: One new test file with 4 test functions + a module docstring referencing the OOB regression. Skips cleanly on CPU or missing torch_structured. Ready for Plan 02-06 GPU run.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/02-triton-fast-path-parity-vs-reference/02-CONTEXT.md
@.planning/phases/02-triton-fast-path-parity-vs-reference/02-PATTERNS.md
@.planning/codebase/TESTING.md
@.planning/codebase/CONVENTIONS.md
@CLAUDE.md
@tests/test_butterfly_dispatch.py
@src/gru_qat/triton_kernels/scan_butterfly.py

<interfaces>
<!-- Key entry points used by this plan. Executor uses these directly. -->

From `src/gru_qat/triton_kernels/scan_butterfly.py`:
- `extract_butterfly_factors(cell) -> tuple` and `extract_butterfly_twiddles(cell) -> tuple` — factor extraction helpers
- `gru_scan_butterfly(...)` — public autograd wrapper, calls CUDA-op per-step path (this is the REFERENCE for the kernel under test, per PATTERNS.md note about butterfly having no pure-PyTorch reference distinct from the CUDA-op path)
- `gru_scan_butterfly_forward_triton(gi, h0, twiddles, bh_cat) -> torch.Tensor` — kernel under test
- `gru_scan_butterfly_backward_triton(...)` — backward kernel

From `tests/test_butterfly_dispatch.py`:
- `_make_layer(H: int, *, use_triton: bool, hidden_bits: int = 32) -> GRULayer` at lines 36-48 — **duplicate verbatim** per D-18; the strict file always uses `hidden_bits=32` (fp32-Identity per CONTEXT)
- `cuda_only` at lines 31-33 — reason string is "butterfly dispatch path is CUDA-only"; preserve this exact wording per PATTERNS.md "cuda_only per-file gate" section
- Module-top `torch_structured = pytest.importorskip("torch_structured")` at line 19
- Existing fwd test at lines 133-160 (`test_butterfly_triton_forward_matches_per_step`, rel < 2e-2) — strict file replaces this idiom with absolute < 1e-5
- Existing bwd test at lines 218-315 (`test_butterfly_triton_backward_matches_autograd`, rel < 5e-2) — strict file replaces with absolute < 1e-5
- **OOB regression at line 164** (`test_butterfly_triton_forward_scratch_oob_regression`) — D-22: reference in docstring only, do NOT duplicate

Established conventions: butterfly's reference path uses `gru_scan_butterfly` (CUDA-op per-step) — NOT a pure-PyTorch closed-form — because the butterfly factorization is built from `butterfly_multiply` in `torch_structured`. The strict file follows this pattern: ref = `gru_scan_butterfly`, tri = `gru_scan_butterfly_forward_triton` / `_backward_triton`.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Strict-tier butterfly fwd parity over T x B x H (powers of 2) grid</name>
  <files>tests/test_triton_butterfly_strict.py</files>
  <read_first>
    - tests/test_butterfly_dispatch.py (lines 1-48 for preamble + _make_layer; lines 133-160 for fwd body shape; line 164 for the OOB regression reference)
    - src/gru_qat/triton_kernels/scan_butterfly.py (signatures of gru_scan_butterfly, gru_scan_butterfly_forward_triton, extract_butterfly_twiddles)
    - .planning/phases/02-triton-fast-path-parity-vs-reference/02-PATTERNS.md (section "tests/test_triton_butterfly_strict.py")
    - .planning/phases/02-triton-fast-path-parity-vs-reference/02-CONTEXT.md (D-16 butterfly grid, D-19, D-21, D-22)
    - .planning/codebase/CONVENTIONS.md (Type annotations, Imports)
  </read_first>
  <behavior>
    - Module imports successfully on CPU; SKIPS if `torch_structured` or `triton` missing
    - On CUDA, `test_butterfly_fwd_strict_matches_reference[T-B-H]` passes `< 1e-5` absolute against the CUDA-op per-step path (`gru_scan_butterfly`) across FAST_BFLY_GRID (H ∈ {32, 128, 512} — all powers of 2)
    - `_slow` sibling passes under `pytest -m slow`
    - Module docstring includes the D-22 reference to `tests/test_butterfly_dispatch.py:164` OOB regression — that test runs at `(T=16, B=32, H=512)` under TF32 with `rel < 5e-2`; the strict file does NOT duplicate it, and Plan 02-06 verifies it still passes
  </behavior>
  <action>
    Create `tests/test_triton_butterfly_strict.py`. Concrete components:

    1. **Module docstring** — strict tier, `'highest'`, < 1e-5 abs. Mirror the diagonal/monarch_strict templates and ADD the D-22 OOB-regression reference paragraph: "Note: the per-program scratch-OOB regression for the butterfly fwd kernel (commit d8218d4, finding TRI-04) is covered at `tests/test_butterfly_dispatch.py:164` (`test_butterfly_triton_forward_scratch_oob_regression`). That test runs at (T=16, B=32, H=512) under TF32 with rel < 5e-2; this strict file does NOT duplicate it per D-22. Phase-exit verification (Plan 02-06) confirms the OOB regression still passes; if it regresses, the bug surfaces there, not here."

    2. **Imports**:
    - `from __future__ import annotations`
    - `import warnings; warnings.filterwarnings("ignore", message=".*different CUDA versions.*")` — mirror `tests/test_butterfly_dispatch.py:13-14`
    - `import pytest`, `import torch`
    - BOTH module-level skips: `triton = pytest.importorskip("triton")`, then `torch_structured = pytest.importorskip("torch_structured")`
    - Internal imports with `# noqa: E402`: `from gru_qat import GRULayer, QuantizerConfig, QuantRecipe, StructureConfig` and `from gru_qat.triton_kernels.scan_butterfly import extract_butterfly_factors, extract_butterfly_twiddles, gru_scan_butterfly, gru_scan_butterfly_backward_triton, gru_scan_butterfly_forward_triton`

    3. **Module-scope `torch.set_float32_matmul_precision("highest")`** with strict-tier comment block.

    4. **`cuda_only`** — duplicate from `tests/test_butterfly_dispatch.py:31-33` (preserve "butterfly dispatch path is CUDA-only" reason string).

    5. **`_make_layer`** — verbatim copy of `tests/test_butterfly_dispatch.py:36-48` with comment "duplicated per D-18". The strict file callers always pass `hidden_bits=32` (the default).

    6. **Shape grid**:
    ```
    FAST_BFLY_GRID = [
        (T, B, H)
        for T in (1, 8, 64)
        for B in (1, 4, 32)
        for H in (32, 128, 512)  # powers of 2; butterfly requires 2^k
    ]
    SLOW_BFLY_GRID = [
        (T, B, H)
        for T in (512, 1024)
        for B in (1, 4, 32)
        for H in (32, 128, 512)
    ]
    ```
    Add a comment line: `# Butterfly requires H to be a power of 2 per src/gru_qat/structure.py shape validators. Per D-16: H in {32, 128, 512}.`

    7. **`test_butterfly_fwd_strict_matches_reference(T: int, B: int, H: int) -> None`** — `@cuda_only` + `@pytest.mark.parametrize("T,B,H", FAST_BFLY_GRID)`. Body mirrors `tests/test_butterfly_dispatch.py:133-160`:
    - `torch.manual_seed(0); device = torch.device("cuda")`
    - Build two layers with shared state for parity:
      - `pt_layer = _make_layer(H, use_triton=False).to(device)`
      - `fast_layer = _make_layer(H, use_triton=True).to(device)`
      - `fast_layer.load_state_dict(pt_layer.state_dict())`
    - Build inputs: `x = torch.randn(T, B, H, device=device) * 0.1; h0 = torch.randn(B, H, device=device) * 0.1`
    - Run reference (CUDA-op per-step, butterfly path): `pt_out, _ = pt_layer(x, h0)`
    - Run kernel: `fast_out, _ = fast_layer(x, h0)`
    - `max_diff = (pt_out - fast_out).abs().max().item()`
    - `assert max_diff < 1e-5, f"max abs diff {max_diff:.4e} (T={T},B={B},H={H})"`

    Note: butterfly has NO pure-PyTorch reference distinct from the CUDA-op path — the reference path goes through `butterfly_multiply` from `torch_structured`. This is consistent with PATTERNS.md: "Reference path stays `gru_scan_butterfly` (CUDA-op per-step path) — butterfly has no pure-PyTorch reference distinct from the kernel under test, so the CUDA-op path serves as ground truth."

    8. **`test_butterfly_fwd_strict_matches_reference_slow`** — same body, `@pytest.mark.slow` + SLOW_BFLY_GRID.

    Every test starts with `torch.manual_seed(0)`. Type annotations everywhere. Absolute-error idiom only.
  </action>
  <verify>
    <automated>cd /home/claroche/gru-triton &amp;&amp; uv run python -c "import ast; ast.parse(open('tests/test_triton_butterfly_strict.py').read())" &amp;&amp; uv run pytest tests/test_triton_butterfly_strict.py --collect-only -q 2>&amp;1 | tail -15</automated>
  </verify>
  <acceptance_criteria>
    - `python -c "import ast; ast.parse(...)"` exits 0
    - `pytest --collect-only -q tests/test_triton_butterfly_strict.py` lists ≥ 2 fwd test functions with combined parametrization ≥ 27 + 18 = 45 ids
    - `pytest tests/test_triton_butterfly_strict.py -q` exits 0 on CPU with all CUDA tests SKIPPED
    - `grep -c "torch.set_float32_matmul_precision(\"highest\")" tests/test_triton_butterfly_strict.py` returns 1
    - `grep -c "test_butterfly_triton_forward_scratch_oob_regression" tests/test_triton_butterfly_strict.py` returns at least 1 (module docstring references the OOB regression per D-22)
    - `grep -cE "def test_.*scratch_oob" tests/test_triton_butterfly_strict.py` returns 0 (NO duplicated OOB regression function definition)
    - `grep -c "torch_structured = pytest.importorskip" tests/test_triton_butterfly_strict.py` returns 1
    - `grep -c "abs().max()" tests/test_triton_butterfly_strict.py` returns at least 1
    - `grep -c "/ max(pt_out.abs().max()" tests/test_triton_butterfly_strict.py` returns 0 (no relative-error idiom)
    - `ruff check tests/test_triton_butterfly_strict.py` exits 0
  </acceptance_criteria>
  <done>
    Butterfly fwd parity tests created with H ∈ {32, 128, 512} grid (powers of 2 per D-16), absolute-error idiom, both `triton` and `torch_structured` importorskip gates, helper duplicated verbatim per D-18, OOB regression referenced in docstring per D-22.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Strict-tier butterfly bwd parity</name>
  <files>tests/test_triton_butterfly_strict.py</files>
  <read_first>
    - tests/test_triton_butterfly_strict.py (file from Task 1)
    - tests/test_butterfly_dispatch.py:218-315 (existing bwd test — uses autograd through `gru_scan_butterfly` with `butterfly_multiply` closure as reference; rel < 5e-2)
    - src/gru_qat/triton_kernels/scan_butterfly.py (gru_scan_butterfly_backward_triton signature)
    - .planning/phases/02-triton-fast-path-parity-vs-reference/02-PATTERNS.md (section "Core strict-tier bwd test" for butterfly — notes the elaborate ref_scan closure pattern)
  </read_first>
  <behavior>
    - On CUDA, `test_butterfly_bwd_strict_matches_reference[T-B-H]` runs the backward (via autograd through `gru_scan_butterfly` and `gru_scan_butterfly_forward_triton`) and asserts gradients match at `< 1e-5` absolute across FAST_BFLY_GRID
    - `_slow` sibling passes under `pytest -m slow`
    - The bwd test follows the same dual-layer (`pt_layer` and `fast_layer` with shared state) shape as the fwd test in Task 1, using autograd through `loss.backward()` on each layer's forward
  </behavior>
  <action>
    Append two test functions to `tests/test_triton_butterfly_strict.py`:

    1. **`test_butterfly_bwd_strict_matches_reference(T: int, B: int, H: int) -> None`** — `@cuda_only` + `@pytest.mark.parametrize("T,B,H", FAST_BFLY_GRID)`. Body uses the **autograd-through-dual-layers** pattern (mirror `tests/test_butterfly_dispatch.py:218-315`, with adjustments for strict-tier):
    - `torch.manual_seed(0); device = torch.device("cuda")`
    - Build two layers with shared state:
      - `pt_layer = _make_layer(H, use_triton=False).to(device)`
      - `fast_layer = _make_layer(H, use_triton=True).to(device)`
      - `fast_layer.load_state_dict(pt_layer.state_dict())`
    - Build inputs with `requires_grad=True`:
      - `x_pt = torch.randn(T, B, H, device=device, requires_grad=True) * 0.1` — actually, `* 0.1` won't preserve `requires_grad` on the result; build first, then `.requires_grad_()`. Pattern: `x_pt = (torch.randn(T, B, H, device=device) * 0.1).requires_grad_()`
      - Same for `h0_pt`, and clones for `x_tri`, `h0_tri` (`x_pt.detach().clone().requires_grad_()`)
    - Run both forwards: `pt_out, _ = pt_layer(x_pt, h0_pt); pt_out.float().pow(2).sum().backward()`
    - And `tri_out, _ = fast_layer(x_tri, h0_tri); tri_out.float().pow(2).sum().backward()`
    - Compare gradients for `(x, h0)` AND for every learnable parameter in the layer's `state_dict` (mirror `tests/test_butterfly_dispatch.py:218-315` shape — read it to confirm the exact parameter-iteration pattern):
      - `for name, p_pt in pt_layer.named_parameters():`
      - `    p_tri = dict(fast_layer.named_parameters())[name]`
      - `    if p_pt.grad is None and p_tri.grad is None: continue`
      - `    max_diff = (p_pt.grad - p_tri.grad).abs().max().item()`
      - `    assert max_diff < 1e-5, f"{name} grad max abs diff {max_diff:.4e} (T={T},B={B},H={H})"`
    - And for x and h0:
      - `for name, ref_g, tri_g in [("x", x_pt.grad, x_tri.grad), ("h0", h0_pt.grad, h0_tri.grad)]:`
      - `    max_diff = (ref_g - tri_g).abs().max().item()`
      - `    assert max_diff < 1e-5, f"{name} grad max abs diff {max_diff:.4e} (T={T},B={B},H={H})"`

    2. **`test_butterfly_bwd_strict_matches_reference_slow`** — same body, `@pytest.mark.slow` + SLOW_BFLY_GRID.

    Note: if `tests/test_butterfly_dispatch.py:218-315` uses a more elaborate pattern (e.g., a `ref_scan` closure built directly from `butterfly_multiply` rather than going through `gru_scan_butterfly`), mirror that pattern but **only change the assertion bound to absolute < 1e-5**. The structure of how the reference is constructed must match the realistic-tier sibling so the strict tier is genuinely testing "same math, tighter precision regime," not a different math path.
  </action>
  <verify>
    <automated>cd /home/claroche/gru-triton &amp;&amp; uv run pytest tests/test_triton_butterfly_strict.py --collect-only -q 2>&amp;1 | grep -E "test_butterfly_bwd_strict_matches_reference" | wc -l</automated>
  </verify>
  <acceptance_criteria>
    - `pytest --collect-only -q tests/test_triton_butterfly_strict.py` lists both `test_butterfly_bwd_strict_matches_reference` and `_slow` (≥ 45 combined parametrized ids)
    - `pytest tests/test_triton_butterfly_strict.py -q` exits 0 on CPU with all CUDA tests SKIPPED
    - `grep -c "named_parameters()" tests/test_triton_butterfly_strict.py` returns at least 2 (gradient iteration over learnable params)
    - `grep -c "abs().max()" tests/test_triton_butterfly_strict.py` returns at least 3 (1 fwd + ≥ 2 bwd assertions: parameters loop + x/h0 loop)
    - `ruff check tests/test_triton_butterfly_strict.py` exits 0
  </acceptance_criteria>
  <done>
    Butterfly bwd parity tests appended; autograd-through-dual-layers pattern; per-parameter gradient comparison; absolute-error assertions; FAST + SLOW siblings; module imports cleanly; CPU collection passes with all CUDA tests skipped.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| test ↔ Butterfly Triton kernel | Test feeds random `gi/h0/twiddles/bh_cat` to kernel; kernel must produce numerically-correct output |
| test ↔ torch_structured (butterfly_multiply) | Optional dependency; module-level skip if missing |
| test ↔ Butterfly CUDA-op per-step reference | The CUDA-op path IS ground truth per PATTERNS.md — no pure-PyTorch reference distinct from the kernel under test |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-15 | Tampering | Butterfly kernel last-program / pid_b OOB | mitigate | Existing OOB regression at `tests/test_butterfly_dispatch.py:164` (D-22); referenced in strict-file docstring; Plan 02-06 confirms it still passes |
| T-02-16 | Tampering | Butterfly fwd/bwd parity at strict tier | mitigate | New 45-combo grid at < 1e-5 abs (FAST) + 18 slow combos |
| T-02-17 | Information disclosure | Random test inputs | accept | No PII / secrets |
| T-02-18 | Denial of service | H=512 + T=1024 slow path | accept | Gated behind `@pytest.mark.slow`; not run by default |
| T-02-19 | Repudiation | Butterfly CUDA-op reference may itself be wrong | accept | The CUDA-op path goes through `butterfly_multiply` from `torch_structured`; if that library has bugs they're upstream — out of scope for this audit phase |
</threat_model>

<verification>
After both tasks land:
- `uv run pytest tests/test_triton_butterfly_strict.py --collect-only -q` — ≥ 4 distinct test functions
- `uv run pytest tests/test_triton_butterfly_strict.py -q -m "not slow"` — exits 0 on CPU (all SKIP)
- `uv run ruff check tests/test_triton_butterfly_strict.py` — exit 0
- `git diff HEAD~2 -- tests/test_butterfly_dispatch.py tests/test_parity.py tests/test_layer_parity.py` — empty (D-22 + D-28)
- Verify D-22 reference: `grep -c "test_butterfly_triton_forward_scratch_oob_regression\|tests/test_butterfly_dispatch.py:164" tests/test_triton_butterfly_strict.py` returns ≥ 1
</verification>

<success_criteria>
- File exists at `tests/test_triton_butterfly_strict.py`
- 4 test functions (`test_butterfly_fwd_strict_matches_reference`, `_slow`, `test_butterfly_bwd_strict_matches_reference`, `_slow`)
- Module-scope `torch.set_float32_matmul_precision("highest")`
- Both `pytest.importorskip("triton")` AND `pytest.importorskip("torch_structured")`
- Grid restricted to H ∈ {32, 128, 512} (powers of 2)
- Absolute-error assertions only
- `_make_layer` duplicated verbatim per D-18
- Module docstring references OOB regression per D-22 (does NOT duplicate)
- Slow tests gated `@pytest.mark.slow`; Triton tests gated `@cuda_only`
- `tests/test_butterfly_dispatch.py`, `tests/test_parity.py`, `tests/test_layer_parity.py` unchanged
</success_criteria>

<output>
After completion, create `.planning/phases/02-triton-fast-path-parity-vs-reference/02-04-SUMMARY.md` covering:
- File created and final LOC count
- Test function inventory with parametrize cardinality
- Verification that the D-22 OOB regression reference is present in the docstring
- Whether the bwd test pattern uses dual-layer-with-shared-state autograd or the more elaborate `ref_scan` closure (depends on what `tests/test_butterfly_dispatch.py:218-315` does)
- Any deviations from the plan
</output>
