---
phase: 01-reference-path-parity-vs-nn-gru
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - tests/test_layer_parity.py
autonomous: true
requirements:
  - REF-05
must_haves:
  truths:
    - "tests/test_layer_parity.py exists and is importable (no SyntaxError, no ImportError)."
    - "Module-level helpers _translate_cell_to_nn_gru, _translate_nn_gru_to_cell, _make_dense_fp32_layer are defined and fully type-annotated."
    - "test_gate_order_r_only, test_gate_order_z_only, test_n_gate_asymmetry pass on CPU."
    - "test_round_trip_nn_gru_to_cell passes on CPU — proves the inverse helper is self-consistent."
    - "torch.set_float32_matmul_precision('highest') is set at module scope (D-07)."
  artifacts:
    - path: "tests/test_layer_parity.py"
      provides: "Translation helpers + 3 micro-tests + 1 round-trip smoke test"
      contains: "_translate_cell_to_nn_gru"
      min_lines: 120
  key_links:
    - from: "tests/test_layer_parity.py::_translate_cell_to_nn_gru"
      to: "src/gru_qat/gru_cell.py:GRUCellQuant (W_ir, W_iz, W_in, b_ir, ..., b_hn)"
      via: "torch.cat([cell.W_ir, cell.W_iz, cell.W_in], dim=0) into nn.GRU.weight_ih_l0"
      pattern: "torch\\.cat\\(\\[.*W_ir.*W_iz.*W_in.*\\], *dim=0\\)"
    - from: "tests/test_layer_parity.py::_translate_nn_gru_to_cell"
      to: "tests/test_parity.py:_copy_weights (cell-level chunk pattern)"
      via: "gru.weight_ih_l0.chunk(3, dim=0) → cell.W_ir/W_iz/W_in"
      pattern: "chunk\\(3, *dim=0\\)"
---

<objective>
Lay the foundation for the Phase 1 layer-parity audit: create `tests/test_layer_parity.py` with the two-direction translation helpers (cell↔nn.GRU), the fp32-Identity layer-builder helper, three focused gate-ordering / n-gate-asymmetry micro-tests, and a single round-trip smoke test.

Purpose: Every parametrized parity test in Plans 02–04 depends on these helpers. Building them first (and proving them correct via the 4 small tests) means later plans can focus on the math audit instead of helper plumbing.

Output: New test file `tests/test_layer_parity.py` with helpers + 4 tests (3 micro + 1 round-trip). No `src/` changes.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-reference-path-parity-vs-nn-gru/01-CONTEXT.md
@.planning/phases/01-reference-path-parity-vs-nn-gru/01-PATTERNS.md
@.planning/codebase/TESTING.md
@.planning/codebase/CONVENTIONS.md
@CLAUDE.md
@tests/test_parity.py
@src/gru_qat/gru_cell.py
@src/gru_qat/gru_layer.py

<interfaces>
<!-- Key contracts from src/. Use these directly — no exploration needed. -->

From src/gru_qat/gru_cell.py (GRUCellQuant, dense path):
- Constructor: GRUCellQuant(input_size: int, hidden_size: int, recipe: QuantRecipe, *, gate_layout="split", bias=True, structure_input=None, structure_hidden=None)
- Public attributes: cell.input_size, cell.hidden_size
- Dense weight tensors (nn.Parameter, shape [hidden_size, input_size] or [hidden_size, hidden_size]):
    cell.W_ir, cell.W_iz, cell.W_in   # input → r/z/n
    cell.W_hr, cell.W_hz, cell.W_hn   # hidden → r/z/n
- Bias tensors (nn.Parameter, shape [hidden_size]):
    cell.b_ir, cell.b_iz, cell.b_in
    cell.b_hr, cell.b_hz, cell.b_hn

From src/gru_qat/gru_layer.py (GRULayer):
- Constructor: GRULayer(input_size: int, hidden_size: int, recipe: QuantRecipe, *, batch_first=False, gate_layout="split", compile_step=False, pre_batch_input=False, structure_input=None, structure_hidden=None, use_triton="auto")
- Attribute: layer.cell (the GRUCellQuant)
- Forward: layer(x, h0=None) -> (out, h_T) where x is [T, B, IN] when batch_first=False, h0 is [B, H], out is [T, B, H], h_T is [B, H].
- use_triton="auto" resolves to False when not _fast_dispatch_eligible. For dense + no structure, it's False — CPU-runnable.

From src/gru_qat/quantizers.py:
- PRESETS["fp32"] = QuantRecipe(weight=QuantizerConfig(bits=32, name="W_id"), input_act=QuantizerConfig(bits=32, name="x_id"), hidden=QuantizerConfig(bits=32, name="h_id"))
  All three default to axis=None. This is the Identity-quantizer recipe Phase 1 audits.

From torch.nn.GRU (1-layer, unidirectional, batch_first=False):
- nn.GRU(input_size, hidden_size, num_layers=1, bidirectional=False, batch_first=False)
- Parameters (gate order is r, z, n on both sides — see https://docs.pytorch.org/docs/stable/generated/torch.nn.GRU.html):
    gru.weight_ih_l0  shape [3*hidden, input]    # concat([W_ir, W_iz, W_in], dim=0)
    gru.weight_hh_l0  shape [3*hidden, hidden]   # concat([W_hr, W_hz, W_hn], dim=0)
    gru.bias_ih_l0    shape [3*hidden]
    gru.bias_hh_l0    shape [3*hidden]
- Forward: gru(x, h0) -> (output, h_n) where x is [T, B, IN], h0 is [1, B, H], output is [T, B, H], h_n is [1, B, H].
- IMPORTANT: nn.GRU takes h0 with leading num_layers=1 axis; GRULayer takes h0 as [B, H]. The translation is `h0_nn = h0_layer.unsqueeze(0)` or `h0_layer = h0_nn.squeeze(0)`.

From tests/test_parity.py:18-44 (_copy_weights — analog for the inverse direction):
- chunk(3, dim=0) on nn.GRUCell weights → (Wir, Wiz, Win) etc.
- `with torch.no_grad(): dst.W_ir.copy_(Wir); ...`
- Module-level, underscore-prefixed, fully typed.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create test_layer_parity.py with module preamble, translation helpers, and fp32-Identity layer builder</name>
  <files>tests/test_layer_parity.py</files>
  <read_first>
    - tests/test_parity.py (full file — `_copy_weights` at lines 18-44 is the analog for the inverse helper; module docstring style at lines 1-15)
    - src/gru_qat/gru_cell.py (lines 1-15 module docstring on n-gate asymmetry; lines 88-115 GRUCellQuant.__init__; lines 130-186 weight parameter names)
    - src/gru_qat/gru_layer.py (lines 49-115 GRULayer.__init__; lines 139-200 forward signature)
    - src/gru_qat/quantizers.py:284-289 (PRESETS["fp32"] — Identity recipe)
    - .planning/phases/01-reference-path-parity-vs-nn-gru/01-PATTERNS.md (the helper code excerpts at lines 75-148; "fp32-Identity layer-builder pattern")
    - .planning/phases/01-reference-path-parity-vs-nn-gru/01-CONTEXT.md (D-01, D-02, D-03, D-05, D-07)
    - .planning/codebase/CONVENTIONS.md (lines 52-67 import order; lines 69-76 type annotations; "module-level underscore helpers")
    - .planning/codebase/TESTING.md ("No fixtures via conftest", "TF32 setup", "Module-level underscore helpers")
  </read_first>
  <action>
    Create the new file `tests/test_layer_parity.py` from scratch (it does not yet exist). The file must contain ONLY: (1) module docstring; (2) imports; (3) module-level `torch.set_float32_matmul_precision("highest")` call per D-07; (4) three module-level helpers; (5) NO test functions yet (those go in subsequent tasks of this plan).

    Module docstring (multi-paragraph, in the style of `tests/test_parity.py:1-6`): describe that this is the Phase 1 layer-level audit comparing `GRULayer` (use_triton=False, Identity quantizers, dense, no structure_hidden) against `torch.nn.GRU(num_layers=1, bidirectional=False, batch_first=False)` for fwd / bwd / h_T / h_0≠0 to < 1e-4 over a T×B×H = 5×3×5 grid. Note that "highest" matmul precision is set module-wide because we audit the math, not TF32.

    Imports — exact order per CONVENTIONS.md:
    1. `from __future__ import annotations`
    2. stdlib: none needed for this file
    3. third-party: `import pytest`, `import torch`, `import torch.nn as nn`
    4. internal: `from gru_qat.gru_layer import GRULayer`, `from gru_qat.quantizers import PRESETS`

    Module-level call (between imports and helpers, with a brief comment explaining D-07 rationale): `torch.set_float32_matmul_precision("highest")`.

    Helper 1 — `_make_dense_fp32_layer(input_size: int, hidden_size: int) -> GRULayer`. Returns `GRULayer(input_size, hidden_size, recipe=PRESETS["fp32"], batch_first=False, gate_layout="split")`. Docstring: "fp32 dense reference layer: Identity quantizers, no structure, no Triton. This is the path Phase 1 audits." No `structure_input` / `structure_hidden` args (omitted → dense). Do NOT pass `use_triton` — let it default to "auto" which resolves to False because not eligible.

    Helper 2 — `_translate_cell_to_nn_gru(layer: GRULayer) -> nn.GRU`. Builds `nn.GRU(input_size=layer.cell.input_size, hidden_size=layer.cell.hidden_size, num_layers=1, bidirectional=False, batch_first=False)`. Inside a `with torch.no_grad():` block, copy weights via `torch.cat([cell.W_ir, cell.W_iz, cell.W_in], dim=0)` into `gru.weight_ih_l0`; same pattern for `weight_hh_l0`, `bias_ih_l0` (cat of three b_i* tensors), `bias_hh_l0` (cat of three b_h* tensors). Docstring must (a) explain the gate-order contract `(r, z, n)` is shared between cell and nn.GRU, (b) include the PyTorch GRU docs URL `https://docs.pytorch.org/docs/stable/generated/torch.nn.GRU.html` per D-05, (c) note the n-gate asymmetry `r_t * (W_hn h + b_hn)` is preserved by this layout because both sides apply r_t identically (cross-reference `src/gru_qat/gru_cell.py:1-15`).

    Helper 3 — `_translate_nn_gru_to_cell(gru: nn.GRU) -> GRULayer`. Builds a fresh `GRULayer(gru.input_size, gru.hidden_size, recipe=PRESETS["fp32"], batch_first=False, gate_layout="split")` and copies inverse: `gru.weight_ih_l0.chunk(3, dim=0) → (Wir, Wiz, Win)` and assigned to `cell.W_ir/W_iz/W_in` inside a `torch.no_grad()` block. Same chunk pattern for `weight_hh_l0` → W_h*, `bias_ih_l0` → b_i*, `bias_hh_l0` → b_h*. Direct mirror of `tests/test_parity.py:18-44 _copy_weights` but at the layer level. Docstring: "Inverse of `_translate_cell_to_nn_gru` — used by the round-trip smoke test only."

    All three helpers are module-level, single-underscore-prefixed, fully type-annotated (return type, parameter types). No `conftest.py` use, no `@pytest.fixture` decorator. Variable name `bin_` (trailing underscore) is mandatory for the `chunk(3)` unpack of `bias_ih_l0` because `bin` is a Python builtin — copy the exact pattern from `tests/test_parity.py:29`.

    Do NOT modify any `src/` file. Do NOT add `@pytest.mark.xfail` anywhere (D-12). Do NOT add tests yet — that is Task 2 of this plan.
  </action>
  <verify>
    <automated>cd /home/claroche/gru-triton && python -c "import tests.test_layer_parity as m; assert hasattr(m, '_translate_cell_to_nn_gru'); assert hasattr(m, '_translate_nn_gru_to_cell'); assert hasattr(m, '_make_dense_fp32_layer'); print('OK')"</automated>
    <automated>cd /home/claroche/gru-triton && grep -n "set_float32_matmul_precision" tests/test_layer_parity.py | grep -v '^#' | grep -c highest</automated>
    <automated>cd /home/claroche/gru-triton && grep -n "docs.pytorch.org" tests/test_layer_parity.py</automated>
    <automated>cd /home/claroche/gru-triton && ruff check tests/test_layer_parity.py</automated>
  </verify>
  <acceptance_criteria>
    - `tests/test_layer_parity.py` exists, is importable, contains the three helpers as module-level functions.
    - `torch.set_float32_matmul_precision("highest")` is called at module scope (NOT inside a function).
    - `_translate_cell_to_nn_gru.__doc__` contains the PyTorch GRU docs URL.
    - `ruff check tests/test_layer_parity.py` exits 0.
    - File contains zero `xfail` occurrences and zero `src/` modifications.
  </acceptance_criteria>
  <done>
    Helpers exist, are typed, are importable, and the file passes ruff. No tests yet — those land in Task 2.
  </done>
</task>

<task type="auto">
  <name>Task 2: Add the 3 gate-ordering / n-gate-asymmetry micro-tests + 1 round-trip smoke test</name>
  <files>tests/test_layer_parity.py</files>
  <read_first>
    - tests/test_layer_parity.py (the file Task 1 just created — read in full)
    - tests/test_parity.py:73-103 (analog: `test_cell_with_zero_hidden`, `test_cell_with_zero_input`, `test_cell_with_large_magnitude` — one-shot smoke tests, not parametrized)
    - src/gru_qat/gru_cell.py:1-15 (module docstring — explains the n-gate asymmetry the test isolates)
    - .planning/phases/01-reference-path-parity-vs-nn-gru/01-PATTERNS.md (lines 299-350 — micro-test pattern + round-trip smoke test pattern, verbatim code excerpts)
    - .planning/phases/01-reference-path-parity-vs-nn-gru/01-CONTEXT.md (D-01 round-trip discipline, D-04 the three micro-tests, D-06 no third hand-rolled GRU reference)
  </read_first>
  <action>
    Append four new module-level test functions to `tests/test_layer_parity.py`. None are parametrized — these are one-shot smoke tests run BEFORE the 75-combo grid (which lives in Plan 02). Each test resets `torch.manual_seed(0)` at the top of its body (TESTING.md "Bench-Style Smoke Tests vs. Correctness Tests"). All four use the relative-error idiom from `tests/test_triton_diagonal.py:120-121`: `rel = max_diff / max(ref_abs_max, 1e-6); assert rel < 1e-4, f"<name> rel diff {rel:.4e}"`.

    Test 1 — `test_gate_order_r_only() -> None`. Per D-04 item 1. Build `layer = _make_dense_fp32_layer(input_size=4, hidden_size=4)`. Inside `with torch.no_grad():` set `cell.W_ir.fill_(1.0)`, zero `cell.W_iz`, `cell.W_in`, all three `cell.W_h*`, and all six `cell.b_*` tensors. Build `gru = _translate_cell_to_nn_gru(layer)`. Run one timestep: `x = torch.randn(1, 2, 4)` (shape [T=1, B=2, IN=4]); `h0 = torch.zeros(2, 4)` (shape [B=2, H=4]). Compute `out_ref, _ = gru(x, h0.unsqueeze(0))` and `out_ours, _ = layer(x, h0)`. Assert relative diff `< 1e-4` with a message including `(out_ref.shape=..., out_ours.shape=...)` for diagnosis. Docstring: "If the cell's gate order is wrong, the grid tests will still pass with the translation helper compensating — this micro-test isolates the gate-order assumption by activating only the r-gate."

    Test 2 — `test_gate_order_z_only() -> None`. Per D-04 item 2. Same template as Test 1 but swap which gate fires: `cell.W_iz.fill_(1.0)` and zero the others (including `W_ir` and `W_in`). Same shapes, same assertion.

    Test 3 — `test_n_gate_asymmetry() -> None`. Per D-04 item 3. Build a 4×4 layer. To force `r ≈ 0` so the n-gate reduces to `tanh(W_in x + b_in)` (without the `r * (W_hn h + b_hn)` contribution): set `cell.W_ir.zero_()` and `cell.b_ir.fill_(-100.0)` (large negative pushes sigmoid output to ~0). Keep `W_in`, `W_hn`, `b_hn` at their initialized values (do NOT zero them — the point is to verify they get gated to ~zero by the asymmetric r_t multiplier). Translate to `nn.GRU` and assert both implementations agree to < 1e-4 on a single timestep. Docstring: "Force r ~ 0 by setting b_ir to large-negative; the n-gate must reduce to tanh(W_in x + b_in). Both nn.GRU and our cell must agree on the asymmetric placement of r inside the tanh — see src/gru_qat/gru_cell.py:11-14."

    Test 4 — `test_round_trip_nn_gru_to_cell() -> None`. Per D-01 (the inverse-direction smoke test). Build `gru = nn.GRU(8, 16, num_layers=1, bidirectional=False, batch_first=False)` with `torch.manual_seed(0)` set first. Build `layer = _translate_nn_gru_to_cell(gru)`. Run a non-trivial sequence: `x = torch.randn(7, 4, 8)`, `h0_3d = torch.zeros(1, 4, 16)`. Compute `out_ref, hT_ref = gru(x, h0_3d)` and `out_ours, hT_ours = layer(x, h0_3d.squeeze(0))`. Assert both `out` and `h_T` agree to < 1e-4 (relative-error idiom). Docstring: "Build an nn.GRU first, copy its weights into a fresh GRULayer via the inverse helper, then assert layer outputs match. Catches bugs in `_translate_nn_gru_to_cell` itself before any parametrized grid runs."

    All four tests are module-level, return `-> None`, and have docstrings. No `@pytest.mark.parametrize`, no `@pytest.mark.slow`, no `@pytest.mark.xfail`. Variable name `bin_` rule from Task 1 still applies if any test needs to chunk a bias.
  </action>
  <verify>
    <automated>cd /home/claroche/gru-triton && pytest tests/test_layer_parity.py::test_gate_order_r_only tests/test_layer_parity.py::test_gate_order_z_only tests/test_layer_parity.py::test_n_gate_asymmetry tests/test_layer_parity.py::test_round_trip_nn_gru_to_cell -q</automated>
    <automated>cd /home/claroche/gru-triton && grep -c "^def test_" tests/test_layer_parity.py</automated>
    <automated>cd /home/claroche/gru-triton && grep -n "xfail" tests/test_layer_parity.py; test $? -ne 0</automated>
    <automated>cd /home/claroche/gru-triton && ruff check tests/test_layer_parity.py</automated>
  </verify>
  <acceptance_criteria>
    - All 4 tests pass on CPU (no `pytest.importorskip`, no `cuda_only`).
    - The `grep -c "^def test_"` count is exactly 4.
    - Zero `xfail` markers anywhere in the file.
    - ruff exits 0.
    - If any of the 4 tests FAILS (helper bug or cell math bug): follow D-10/D-11. Commit A = the failing test alone with `pytest --tb=short` tail saved as bd issue notes via `bd create --title "<test_function_name>"`; Commit B = fix in `src/gru_qat/gru_cell.py` or `src/gru_qat/gru_layer.py` (not in the test file, not in the helpers — those are test infrastructure). After commit B passes, `bd close <id>`.
  </acceptance_criteria>
  <done>
    Four micro/smoke tests run on CPU, pass (or have a bd issue + commit A + commit B per finding). Helpers are now proven correct in both directions — the file is ready for the parametrized grids in Plans 02–04.
  </done>
</task>

</tasks>

<verification>
After both tasks: `pytest tests/test_layer_parity.py -q` collects 4 tests (no parametrize yet) and passes. `ruff check tests/test_layer_parity.py` exits 0. The file imports cleanly via `python -c "import tests.test_layer_parity"`. `tests/test_parity.py` is untouched — `git diff tests/test_parity.py` is empty.
</verification>

<success_criteria>
- `tests/test_layer_parity.py` exists with helpers + 4 tests.
- 4 tests pass on a CPU-only machine.
- No `src/` changes unless a micro-test surfaced a real bug (in which case D-10/D-11 apply).
- The < 1e-5 cell parity contract in `tests/test_parity.py` is untouched.
</success_criteria>

<output>
Create `.planning/phases/01-reference-path-parity-vs-nn-gru/01-01-SUMMARY.md` with: helpers added, micro-tests passing, any bd issues filed per D-11 (test_function_name + bd id + commit A sha + commit B sha if applicable), notes on whether the round-trip smoke test surfaced any helper inversion bugs.
</output>
