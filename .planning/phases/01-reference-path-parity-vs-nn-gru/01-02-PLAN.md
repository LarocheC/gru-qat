---
phase: 01-reference-path-parity-vs-nn-gru
plan: 02
type: execute
wave: 2
depends_on:
  - 01-01
files_modified:
  - tests/test_layer_parity.py
autonomous: true
requirements:
  - REF-01
  - REF-04
must_haves:
  truths:
    - "test_layer_forward_matches_nn_gru is parametrized over the FAST_GRID (T ∈ {1, 8, 64}, B ∈ {1, 4, 32}, H ∈ {1, 2, 8, 64, 512}) and passes on CPU."
    - "test_layer_forward_matches_nn_gru_slow is parametrized over the SLOW_GRID (T ∈ {512, 1024}, same B/H) and is marked @pytest.mark.slow."
    - "test_layer_h_T_matches_nn_gru and test_layer_h_T_matches_nn_gru_slow exist as separate parametrized functions (D-09)."
    - "All four parametrized functions use the relative-error idiom with the 1e-6 denominator floor and 1e-4 tolerance."
  artifacts:
    - path: "tests/test_layer_parity.py"
      provides: "Forward + h_T parity over the 75-combo T×B×H grid"
      contains: "FAST_GRID"
      min_lines: 200
  key_links:
    - from: "test_layer_forward_matches_nn_gru"
      to: "torch.nn.GRU vs GRULayer (use_triton=False, dense, Identity)"
      via: "_translate_cell_to_nn_gru + relative-error assertion < 1e-4"
      pattern: "rel < 1e-4"
    - from: "test_layer_h_T_matches_nn_gru"
      to: "h_T returned by GRULayer.forward (second tuple element)"
      via: "asserts hT_ours vs hT_ref.squeeze(0) — distinct from out parity"
      pattern: "h_T rel diff"
---

<objective>
Add the first two parametrized parity-test families to `tests/test_layer_parity.py`: forward-output parity (`test_layer_forward_matches_nn_gru`) and final-hidden-state parity (`test_layer_h_T_matches_nn_gru`), each split into a fast variant and a `@pytest.mark.slow` variant per D-08. All four parametrized functions exercise the same T × B × H grid via the `_translate_cell_to_nn_gru` helper from Plan 01.

Purpose: REF-01 (fwd parity < 1e-4 across the grid) and REF-04 (h_T parity < 1e-4) are the central spec of this phase. Splitting fwd vs. h_T into separate functions per D-09 means a final-step bug surfaces with a test id that points at it directly.

Output: 4 new parametrized test functions added to the existing test file, plus the two `FAST_GRID` / `SLOW_GRID` module-level constants.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-reference-path-parity-vs-nn-gru/01-CONTEXT.md
@.planning/phases/01-reference-path-parity-vs-nn-gru/01-PATTERNS.md
@.planning/phases/01-reference-path-parity-vs-nn-gru/01-01-SUMMARY.md
@.planning/codebase/TESTING.md
@CLAUDE.md
@tests/test_layer_parity.py
@tests/test_parity.py
@tests/test_qat_smoke.py
@src/gru_qat/gru_layer.py

<interfaces>
<!-- Already established in Plan 01. Reproduced here for self-contained execution. -->

From tests/test_layer_parity.py (Plan 01 output, now committed):
- `_make_dense_fp32_layer(input_size: int, hidden_size: int) -> GRULayer`
- `_translate_cell_to_nn_gru(layer: GRULayer) -> nn.GRU`
- `_translate_nn_gru_to_cell(gru: nn.GRU) -> GRULayer`
- Module-level `torch.set_float32_matmul_precision("highest")` already set.
- Imports: pytest, torch, torch.nn as nn, GRULayer, PRESETS already present.

From GRULayer.forward signature:
- Returns `tuple[torch.Tensor, torch.Tensor]` = `(out, h_T)`.
- `out` shape: `[T, B, H]` when `batch_first=False`.
- `h_T` shape: `[B, H]` (NOT `[1, B, H]` — single trailing axis is squeezed off compared to nn.GRU).

From nn.GRU forward signature:
- Returns `tuple[torch.Tensor, torch.Tensor]` = `(output, h_n)`.
- `output` shape: `[T, B, H]` when `batch_first=False` and `num_layers=1`.
- `h_n` shape: `[num_layers=1, B, H]` — keep this in mind: `hT_ref.squeeze(0)` to compare against `h_T_ours`.
- `h0` argument shape: `[num_layers=1, B, H]`. To pass to nn.GRU when GRULayer takes h0 as `[B, H]`, use `h0_2d.unsqueeze(0)` or pass `h0=None` and let it default to zeros (both nn.GRU and GRULayer default to zero-h0).

From .planning/codebase/TESTING.md "Markers In Use":
- `slow` marker is registered in pyproject.toml:45. Use `@pytest.mark.slow` directly; no per-file registration needed.

From .planning/codebase/TESTING.md "Relative-error reporting":
- Canonical idiom: `max_diff = (ref - tri).abs().max().item(); rel = max_diff / max(ref.abs().max().item(), 1e-6); assert rel < TOL, f"<name> rel diff {rel:.4e}"`. The 1e-6 floor is non-negotiable.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add FAST_GRID / SLOW_GRID constants and the two test_layer_forward_matches_nn_gru parametrized functions</name>
  <files>tests/test_layer_parity.py</files>
  <read_first>
    - tests/test_layer_parity.py (the existing file from Plan 01 — read in full so the new code lands in the right place, AFTER the helpers and BEFORE the existing micro-tests OR after them — module-level constants conventionally near the top, after imports and before tests; place constants after the helpers and `set_float32_matmul_precision` call)
    - .planning/phases/01-reference-path-parity-vs-nn-gru/01-PATTERNS.md (lines 152-216 — "Parametrize pattern for the 75-combo grid" and "Core parity-test body pattern", verbatim code)
    - .planning/phases/01-reference-path-parity-vs-nn-gru/01-CONTEXT.md (D-07, D-08, D-09)
    - tests/test_qat_smoke.py (lines 85-95 — `@pytest.mark.slow` usage analog)
    - tests/test_triton_diagonal.py (lines 105-130 — the relative-error idiom in a parity test body)
  </read_first>
  <action>
    Add two module-level constants and two parametrized test functions to `tests/test_layer_parity.py`. Insert the constants AFTER the three helpers from Plan 01 but BEFORE any test function. Insert the new test functions AFTER the four micro/round-trip tests from Plan 01 — preserve the existing test order (micro-tests first, then grid tests). Use an ASCII rule comment divider per CONVENTIONS.md (75-char rule) to separate the "Helpers" stanza from the "Grid constants" stanza from the "Grid tests" stanza.

    Constant 1 — `FAST_GRID: list[tuple[int, int, int]]`. Built as `[(T, B, H) for T in (1, 8, 64) for B in (1, 4, 32) for H in (1, 2, 8, 64, 512)]`. Comment: "Fast grid: T in {1, 8, 64}. Runs on every `pytest -q` invocation. 3 × 3 × 5 = 45 cases per family (D-08)."

    Constant 2 — `SLOW_GRID: list[tuple[int, int, int]]`. Built as `[(T, B, H) for T in (512, 1024) for B in (1, 4, 32) for H in (1, 2, 8, 64, 512)]`. Comment: "Slow grid: T in {512, 1024}. Runs only under `pytest -m slow`. 2 × 3 × 5 = 30 cases per family (D-08)."

    Function 1 — `test_layer_forward_matches_nn_gru(T: int, B: int, H: int) -> None`. Decorator: `@pytest.mark.parametrize("T,B,H", FAST_GRID)`. Body: set `torch.manual_seed(0)`, define `IN = max(H, 1)` (keep input_size tied to H so the grid stays compact; the `max(..., 1)` guards against H=0 which is not in the grid but the floor is defensive). Build `layer = _make_dense_fp32_layer(IN, H)` and `gru = _translate_cell_to_nn_gru(layer)`. Create `x = torch.randn(T, B, IN)`. Use `h0=None` for the zero-h0 case (h0≠0 is Plan 04's territory) — pass `None` to both `gru(x)` and `layer(x)` so each implementation defaults to zeros. Call `out_ref, _ = gru(x)` (discard h_n — that's tested in Task 2) and `out_ours, _ = layer(x)`. Compute relative-error idiom on `out_ref` vs `out_ours`: `max_diff = (out_ref - out_ours).abs().max().item()`, `rel = max_diff / max(out_ref.abs().max().item(), 1e-6)`, `assert rel < 1e-4, f"out rel diff {rel:.4e} (T={T},B={B},H={H})"`. Docstring: "Forward output parity vs torch.nn.GRU across the fast grid (T in {1,8,64})."

    Function 2 — `test_layer_forward_matches_nn_gru_slow(T: int, B: int, H: int) -> None`. Identical body to Function 1, but two decorators stacked: `@pytest.mark.slow` ABOVE `@pytest.mark.parametrize("T,B,H", SLOW_GRID)` (order matters per pytest convention — `slow` outermost). Same docstring but mention "slow grid (T in {512, 1024})". The assertion message string keeps the `(T={T},B={B},H={H})` format so the test id and the failure message agree.

    Do NOT touch the helpers from Plan 01 (those are now load-bearing). Do NOT add `xfail`. Do NOT call `gru.eval()` or `layer.eval()` — Identity quantizers + Phase 1 don't need eval mode; nn.GRU's behavior is the same in eval and train.
  </action>
  <verify>
    <automated>cd /home/claroche/gru-triton && pytest tests/test_layer_parity.py::test_layer_forward_matches_nn_gru -q</automated>
    <automated>cd /home/claroche/gru-triton && pytest tests/test_layer_parity.py -m slow -k forward -q</automated>
    <automated>cd /home/claroche/gru-triton && python -c "from tests.test_layer_parity import FAST_GRID, SLOW_GRID; assert len(FAST_GRID) == 45; assert len(SLOW_GRID) == 30; print('grid counts OK')"</automated>
    <automated>cd /home/claroche/gru-triton && ruff check tests/test_layer_parity.py</automated>
  </verify>
  <acceptance_criteria>
    - `FAST_GRID` has exactly 45 tuples; `SLOW_GRID` has exactly 30 tuples.
    - `test_layer_forward_matches_nn_gru` is collected with 45 parametrize ids; all 45 pass on CPU at `< 1e-4` relative.
    - `test_layer_forward_matches_nn_gru_slow` is collected with 30 ids and only runs under `pytest -m slow`; the default `pytest -q` does NOT execute these 30.
    - ruff exits 0.
    - On failure: D-10/D-11 discipline — Commit A is the failing parametrized test (already in the file), Commit B is the fix in `src/gru_qat/gru_cell.py` or `gru_layer.py`. `bd create --title "test_layer_forward_matches_nn_gru[<failing-id>]"` before commit A lands.
  </acceptance_criteria>
  <done>
    Forward parity grid (45 fast + 30 slow) lands; 45 pass on CPU in default `pytest -q`. Slow variant gates behind `-m slow`. Any failure has a bd issue + the failing-test-first commit ordering.
  </done>
</task>

<task type="auto">
  <name>Task 2: Add the two test_layer_h_T_matches_nn_gru parametrized functions (separate from forward — D-09)</name>
  <files>tests/test_layer_parity.py</files>
  <read_first>
    - tests/test_layer_parity.py (now contains the forward-parity tests from Task 1)
    - .planning/phases/01-reference-path-parity-vs-nn-gru/01-PATTERNS.md (lines 268-274 — "h_T parity pattern": split from forward parity per D-09 so failure messages point at the right family)
    - .planning/phases/01-reference-path-parity-vs-nn-gru/01-CONTEXT.md (D-09 — four separate parametrized functions, don't fuse)
    - src/gru_qat/gru_layer.py:139-200 (forward returns `(out, h)` — h_T has shape `[B, H]`, no leading num_layers axis)
  </read_first>
  <action>
    Append two new parametrized test functions to `tests/test_layer_parity.py`, AFTER the forward-parity tests from Task 1. Same module-level placement, same import surface — no new imports needed.

    Function 1 — `test_layer_h_T_matches_nn_gru(T: int, B: int, H: int) -> None`. Decorator: `@pytest.mark.parametrize("T,B,H", FAST_GRID)`. Body: set `torch.manual_seed(0)`, `IN = max(H, 1)`. Build `layer = _make_dense_fp32_layer(IN, H)` and `gru = _translate_cell_to_nn_gru(layer)`. Create `x = torch.randn(T, B, IN)` and pass `h0=None` to both implementations. Call `_, hT_ref = gru(x)` and `_, hT_ours = layer(x)`. CRITICAL shape detail: `hT_ref` has shape `[1, B, H]` from nn.GRU; `hT_ours` has shape `[B, H]` from GRULayer. Compare via `hT_ref.squeeze(0)` vs `hT_ours`. Relative-error idiom: `max_diff = (hT_ref.squeeze(0) - hT_ours).abs().max().item()`, `rel = max_diff / max(hT_ref.abs().max().item(), 1e-6)`, `assert rel < 1e-4, f"h_T rel diff {rel:.4e} (T={T},B={B},H={H})"`. Docstring per D-09: "h_T parity: distinct test family from forward-output parity so a final-step bug surfaces alone. Both nn.GRU's h_n and our h_T are the hidden state after T steps."

    Function 2 — `test_layer_h_T_matches_nn_gru_slow(T: int, B: int, H: int) -> None`. Identical body to Function 1 but decorated `@pytest.mark.slow` above `@pytest.mark.parametrize("T,B,H", SLOW_GRID)`. Same docstring but note the slow grid.

    Discard the `out` from both `gru(x)` and `layer(x)` (use `_`) — that's Task 1's territory. Splitting like this means: if `pytest -q` reports `test_layer_forward_matches_nn_gru` passing but `test_layer_h_T_matches_nn_gru` failing, the bug is in the final-step write path or in the time-loop's last iteration, not in the per-step math.

    Do NOT fuse the two assertions into one function (that defeats D-09). Do NOT add `xfail`. The 1e-6 floor on the denominator is mandatory (TESTING.md "Relative-error reporting"). Use `hT_ref.abs().max()` not `hT_ref.squeeze(0).abs().max()` — equivalent under squeeze of a size-1 dim, but reads more directly.
  </action>
  <verify>
    <automated>cd /home/claroche/gru-triton && pytest tests/test_layer_parity.py::test_layer_h_T_matches_nn_gru -q</automated>
    <automated>cd /home/claroche/gru-triton && pytest tests/test_layer_parity.py -m slow -k h_T -q</automated>
    <automated>cd /home/claroche/gru-triton && grep -c "^def test_layer_" tests/test_layer_parity.py</automated>
    <automated>cd /home/claroche/gru-triton && pytest tests/test_layer_parity.py -q 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - `test_layer_h_T_matches_nn_gru` collects 45 ids and all pass on CPU at `< 1e-4` relative.
    - `test_layer_h_T_matches_nn_gru_slow` collects 30 ids under `-m slow`.
    - `grep -c "^def test_layer_"` returns at least 4 (forward, forward_slow, h_T, h_T_slow); 4 micro-tests don't start with `def test_layer_` so they aren't counted here.
    - `pytest tests/test_layer_parity.py -q` total count is `4 micro + 45 fwd + 45 h_T = 94` collected, all passing.
    - On any failure: bd create per failing parametrize id, Commit A is the failing test (already landed), Commit B is the src fix.
  </acceptance_criteria>
  <done>
    Forward + h_T grids are independent, both pass, and slow variants are correctly gated. Plan 03 can now add the gradient family without entangling with forward-only diagnostics.
  </done>
</task>

</tasks>

<verification>
After both tasks: `pytest tests/test_layer_parity.py -q` collects 94 tests (4 micro + 45 fwd + 45 h_T) and all pass on a CPU machine. `pytest tests/test_layer_parity.py -m slow -q` collects 60 tests (30 fwd_slow + 30 h_T_slow) and all pass. `grep -c "^def test_" tests/test_layer_parity.py` returns 8 (4 micro + 4 grid functions). ruff exits 0. `tests/test_parity.py` and all `src/` files are untouched UNLESS a parity failure triggered a D-10/D-11 fix commit.
</verification>

<success_criteria>
- REF-01 (forward parity at < 1e-4 over T × B × H) is exercised by `test_layer_forward_matches_nn_gru` + `_slow`.
- REF-04 (h_T at < 1e-4) is exercised by `test_layer_h_T_matches_nn_gru` + `_slow`.
- Both families pass in default `pytest -q` on CPU.
- Slow variants gate correctly behind `-m slow`.
- `< 1e-5` cell parity in `tests/test_parity.py` is untouched.
</success_criteria>

<output>
Create `.planning/phases/01-reference-path-parity-vs-nn-gru/01-02-SUMMARY.md` listing: parametrize counts (45 fast + 30 slow per family), any failing-test ids found, bd issue ids opened, commit-A and commit-B shas for each fix, and a note on whether the failure surfaced an unrolled-math bug vs. a translation-helper bug.
</output>
