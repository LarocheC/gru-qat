---
phase: quick-260515-t9m
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - pyproject.toml
  - src/gru_qat/structure.py
autonomous: true
requirements: [QUICK-T9M]
must_haves:
  truths:
    - "pip install 'gru-qat[structured]' resolves a real optional-dependency extra"
    - "pyproject.toml comment in the optional-dependencies block reflects the new structured extra (no stale local-checkout guidance)"
    - "structure.py no longer defines the stale, unused _NEEDS_TORCH_STRUCTURED set"
    - "ruff check src tests passes"
    - "mypy passes"
  artifacts:
    - path: "pyproject.toml"
      provides: "structured optional-dependency extra pinned to torch-structured v0.4.0 git tag"
      contains: "structured ="
    - path: "src/gru_qat/structure.py"
      provides: "structured-matrix factory without the stale _NEEDS_TORCH_STRUCTURED set"
  key_links:
    - from: "src/gru_qat/structure.py:_import_torch_structured ImportError message"
      to: "pyproject.toml [project.optional-dependencies] structured extra"
      via: "the install hint 'pip install gru-qat[structured]' now resolves to a real extra"
      pattern: "structured ="
---

<objective>
Wire up a proper `structured` optional-dependency extra in `pyproject.toml` so
`pip install 'gru-qat[structured]'` actually resolves, and remove a stale,
unused data structure in `src/gru_qat/structure.py`.

Purpose: Four `ImportError` messages in `structure.py` already promise the
command `pip install 'gru-qat[structured]'`, but no such extra exists today.
This closes the gap between the promised install hint and reality, and drops a
misleading set (`_NEEDS_TORCH_STRUCTURED` lists `circulant`, which actually uses
the local `_CirculantLinear` and needs no external dep).

Output: Updated `pyproject.toml` with a `structured` extra + refreshed comment,
and `structure.py` with the stale set removed.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md

<interfaces>
<!-- Current state of the files being edited. Executor edits these directly — no exploration needed. -->

pyproject.toml — current [project.optional-dependencies] block (lines 11-23):
```toml
[project.optional-dependencies]
triton = ["triton>=2.2"]
# Structured-matrix support uses `torch-structured` which isn't on PyPI.
# Install it directly from your local checkout, e.g.:
#     uv pip install git+https://github.com/LarocheC/torch-structured
# The structure module imports it lazily so gru-qat works without it as
# long as you only use kind="dense".
dev = [
    "pytest>=7",
    "pytest-xdist>=3",
    "mypy>=1.7",
    "ruff>=0.1",
]
```

src/gru_qat/structure.py — the stale set (line 57, exactly one reference: its own definition, confirmed via grep):
```python
_NEEDS_TORCH_STRUCTURED = {"monarch", "circulant", "butterfly", "ldr"}
```
The real import gating is per-kind inside `make_structured_linear` / `_import_torch_structured`; this set is unused dead code. The blank lines around it are at lines 56 and 58-59.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add the structured extra and refresh the comment in pyproject.toml</name>
  <files>pyproject.toml</files>
  <action>
In `pyproject.toml`, edit the `[project.optional-dependencies]` block (lines 11-23).

1. Add a new `structured` extra. After the `triton = [...]` line, add:
   `structured = ["torch-structured @ git+https://github.com/LarocheC/torch-structured@v0.4.0"]`
   The `v0.4.0` tag is verified to exist on the remote (annotated tag, commit ceb76e0). Use PEP 508 direct-reference syntax (`name @ url`) — this is the form pip/uv accept inside an extra.

2. Replace the stale 5-line comment (lines 13-17: "Structured-matrix support uses `torch-structured` which isn't on PyPI. / Install it directly from your local checkout, e.g.: / uv pip install git+... / The structure module imports it lazily ...") with a short, accurate 2-line comment, e.g.:
   `# The 'structured' extra installs torch-structured from a pinned git tag`
   `# (Monarch/Butterfly/LDR support). structure.py imports it lazily, so`
   `# dense-only usage (kind="dense") works without this extra.`

Keep the `dev = [...]` extra unchanged. Do not touch `[build-system]`, `[tool.mypy]`, `[tool.ruff]`, or `[tool.pytest.ini_options]`.
  </action>
  <verify>
    <automated>grep -q 'structured = \["torch-structured @ git+https://github.com/LarocheC/torch-structured@v0.4.0"\]' pyproject.toml && ! grep -q "isn't on PyPI" pyproject.toml && python -c "import tomllib; d=tomllib.load(open('pyproject.toml','rb')); assert 'structured' in d['project']['optional-dependencies'], 'structured extra missing'; print('ok')"</automated>
  </verify>
  <done>pyproject.toml has a `structured` optional-dependency extra pinned to torch-structured `@v0.4.0`, the optional-dependencies comment no longer mentions the local checkout / "isn't on PyPI", and the file parses as valid TOML.</done>
</task>

<task type="auto">
  <name>Task 2: Remove the stale _NEEDS_TORCH_STRUCTURED set from structure.py</name>
  <files>src/gru_qat/structure.py</files>
  <action>
In `src/gru_qat/structure.py`, remove the line at line 57:
`_NEEDS_TORCH_STRUCTURED = {"monarch", "circulant", "butterfly", "ldr"}`

Remove only this single line and collapse the surrounding blank lines so the
file keeps the standard two-blank-line spacing between the `StructureConfig`
dataclass and the `_import_torch_structured` function (do not leave three
consecutive blank lines — ruff/PEP8 expects exactly two between top-level
definitions).

There is no orphaned comment attached to this line. Do NOT change
`_import_torch_structured`, `_validate_shapes`, or any other code — the set is
unused dead code (confirmed via grep: the only reference is its own definition).
Do not add a replacement; per-kind import gating already lives inside
`make_structured_linear`.
  </action>
  <verify>
    <automated>! grep -rn "_NEEDS_TORCH_STRUCTURED" src tests bench && python -c "import ast; ast.parse(open('src/gru_qat/structure.py').read()); print('parses ok')"</automated>
  </verify>
  <done>`_NEEDS_TORCH_STRUCTURED` appears nowhere in the repo, `structure.py` still parses as valid Python, and two-blank-line spacing is preserved between the dataclass and the next function.</done>
</task>

<task type="auto">
  <name>Task 3: Run quality gates</name>
  <files>(none — verification only)</files>
  <action>
Run the project quality gates per CLAUDE.md to confirm the edits are clean:
- `ruff check src tests` — must pass with no errors.
- `mypy` — must pass (strict, scoped to `src/gru_qat`).
- Optionally `pytest -q tests/test_structure.py` for a quick sanity check.
  Note: torch-structured / Triton tests skip without CUDA and the dep
  installed — skips are EXPECTED and are NOT failures. Do NOT run `uv sync`
  or attempt to install torch-structured (network + CUDA build); this change
  is purely declarative.

If ruff or mypy report errors introduced by Tasks 1-2, fix them (most likely a
blank-line spacing nit in `structure.py`); do not change unrelated code.
  </action>
  <verify>
    <automated>ruff check src tests && mypy</automated>
  </verify>
  <done>`ruff check src tests` and `mypy` both pass with no errors. Any pytest skips due to missing CUDA / torch-structured are noted as expected, not failures.</done>
</task>

</tasks>

<verification>
- `pyproject.toml` declares a `structured` extra resolving to `torch-structured @ git+https://github.com/LarocheC/torch-structured@v0.4.0`.
- The optional-dependencies comment is accurate (no stale local-checkout text).
- `_NEEDS_TORCH_STRUCTURED` is fully removed and unreferenced repo-wide.
- `ruff check src tests` passes.
- `mypy` passes.
</verification>

<success_criteria>
- `pip install 'gru-qat[structured]'` now maps to a real, pinned extra — the
  four `ImportError` install hints in `structure.py` are no longer empty promises.
- No stale `_NEEDS_TORCH_STRUCTURED` set remains.
- Quality gates (`ruff`, `mypy`) are green.
</success_criteria>

<output>
After completion, create `.planning/quick/260515-t9m-wire-up-structured-optional-dependency-e/260515-t9m-SUMMARY.md`
</output>
